# Entscheidungen und offene Fragen

**Stand 23.08.2026, 3. Durchgang. Alle gestellten Fragen sind beantwortet.** §3 hält das Protokoll der zweiten Runde, §4 nennt die einzige verbleibende inhaltliche Unklarheit.

---

## 1. Namensvorschläge

Kriterien: aussprechbar, nicht sperrig, im Deutschen und Englischen tragfähig (E8), nicht schon
von einem großen Genealogieprodukt besetzt `[unverified — vor der Festlegung prüfen]`.

### Die drei, die ich selbst nehmen würde

**Wurzelwerk** — botanisch das Wurzelgeflecht einer Pflanze. Fachlich ist das der treffendste
Name, den man vergeben kann: dein Datenbestand ist eben *kein* Baum, sondern ein Geflecht mit
Mehrfachverbindungen. Klingt gleichzeitig warm und handwerklich. Nachteil: im Englischen nicht
selbsterklärend.

**Stemma** — der wissenschaftliche Fachbegriff für ein Abstammungsdiagramm (aus der
Stemmatologie, ursprünglich griechisch für Ahnenkranz). Kurz, ungewöhnlich, funktioniert in
beiden Sprachen unverändert, klingt nach Werkzeug statt nach Hobby. Nachteil: erklärungsbedürftig.

**Herkommen** — altes deutsches Wort für Abstammung und Herkunft ("von gutem Herkommen").
Etwas aus der Mode gefallen und dadurch frei; hat einen ruhigen, erzählenden Klang, der zu einer
Software passt, die Erinnerungen sammelt.

### Weitere, nach Charakter geordnet

*Handwerklich-sachlich:* **Ahnenwerk** · **Stammwerk** · **Herkunftswerk** · **Ahnenbuch**
*Bildlich:* **Geflecht** · **Wurzelkarte** · **Verzweigt** · **Ahnengarten** · **Zweiglicht**
*Lateinisch-knapp:* **Atavus** (der Ur-ur-ur-Großvater) · **Linea** · **Prosapia** (Sippschaft) · **Radix** (Wurzel)
*Erzählend:* **Chronik** · **Familienchronik** · **Überliefert** · **Wortlaut**
*Nüchtern-modern:* **Sippenbuch** — **nicht empfohlen**: "Sippe" und "Sippenforschung" sind
durch die nationalsozialistische Ahnenforschung schwer vorbelastet und im deutschen
Genealogie-Umfeld heikel. Das solltest du wissen, bevor du es in Erwägung ziehst.

### Entschieden: **Wurzelwerk** (E18)

### Ursprüngliche Begründung
**Wurzelwerk.** Es ist der einzige Vorschlag, der die zentrale fachliche Einsicht dieses
Projekts im Namen trägt — dass Verwandtschaft ein Netz ist und kein Baum. Und der Name
funktioniert auf einem Poster über dem Wohnzimmersofa genauso wie in einem Repository.

Falls Englisch später gleichberechtigt sein soll, wäre **Stemma** die stärkere Wahl.

---

## 2. Beantwortet (Protokoll)

| # | Frage | Antwort | Wirkung |
|---|---|---|---|
| F1 | Name | offen, Vorschläge oben | — |
| F2 | Verteilung | Eigennutzung + Verwandtschaft | Lesemodus-Export statt Installer; Signierung vorerst nicht nötig → ADR-008 |
| F3 | Sprache | Deutsch, bald Englisch | Übersetzungsschicht ab Tag 1 → ADR-011 |
| F4 | Datenlage | Papier + Wissen in Köpfen | **Größte Änderung:** Erfassung aus unstrukturiertem Material wird Kernfeature; Import-Vertrag rückt in Phase 1 → ADR-010 |
| F5 | Raum | DE, PL, RU, Baltikum, Kanada; bis ~1700 | Kalender pro Datum zwingend, Ortsnamen-Historie unverzichtbar, Transliteration, Migrationskarte wird Must → 20_Domaenenwissen §13 |
| F6 | Kirchenbücher | noch nicht | Kirchenbuch-Modus → Phase 6 |
| F7 | Ahnenimplex | nicht bekannt | Architektur bleibt DAG-fähig, UI-Priorität gesenkt |
| F8 | DNA | draußen | W-03 endgültig |
| F9 | Gesundheit | strukturiert, mit Risikofaktoren | Eigener Anforderungsblock M-01…M-09, Tabellen `diagnose`/`risikofaktor`, eigene Ansicht, harte Exportsperre |
| D1 | Datumswerte | eingebettet | 50_Datenmodell §2.3 + Doppeldatierungsfelder |
| D2 | Personas | ab Phase 0 | keine Kernmigration später |
| D3 | Ereignis vs. Aussage | Vorschlag angenommen | Regel in 50_Datenmodell §5 |
| D4 | Unbekannte Eltern | Platzhalter | §2.14 mit fünf Schutzregeln |
| T1 | Repository | privates GitHub | ADR-012 |
| T2 | Entwicklung | nur Mac | **Risiko benannt**, CI-Windows-Build + Screenshots → ADR-012 |
| T3 | `.ahnen` | zugestimmt | E14 |
| T4 | GOV | siehe unten | — |
| U1 | Gestaltung | eigene, identisch | E15 |
| U2 | Referenzen | Notion, Obsidian, Claude, Figma, Apple | 70_UX §11 |
| U3 | Themen | hell und dunkel | Tokens ab Tag 1 |
| P1 | Reihenfolge | A: Erfassung zuerst | Roadmap 10_Vision §5 |
| P2 | Analysefeatures | Zeitleiste → Karte → Netzwerk | Phase 3 / Phase 5 |
| Neu | Eigene Profilfelder | gewünscht | A-18, Feld-Definitionssystem, Must |

### Antwort auf T4: Wofür brauchen wir GOV?

**GOV** ist das *Geschichtliche Ortsverzeichnis* des Vereins für Computergenealogie: eine
Datenbank von Ortsobjekten mit stabiler ID, in der jeder Ort seine **Namen mit Zeitraum** und
seine **Zugehörigkeiten mit Zeitraum** trägt — politisch und kirchlich getrennt.

Warum das bei *deinem* Forschungsraum (E9) mehr als Komfort ist, an einem Beispiel:

> Ein Ort in Ostpreußen heißt 1750 deutsch, gehört zum Kirchspiel X, Kreis Y, Provinz
> Ostpreußen, Königreich Preußen. 1871 gehört er zum Deutschen Reich. 1945 wird er polnisch
> oder sowjetisch, bekommt einen neuen Namen, einen neuen Kreis, einen neuen Staat. Ein Teil
> dieser Orte existiert heute nicht mehr.

Ohne Ortsdatenbank musst du diese Historie **für jeden Ort selbst eintippen**, und du hast
keine Möglichkeit, deine Schreibweise mit der Schreibweise anderer Forscher abzugleichen. Mit
GOV-Anbindung bekommst du: die vollständige Namens- und Zugehörigkeitshistorie geschenkt,
Koordinaten, und eine stabile ID, die deine Daten mit fremden Beständen verknüpfbar macht
(GEDCOM 7 transportiert sie offiziell seit 2024).

**Konkret gebraucht wird es für drei Dinge:** (1) der Zeitregler kann Ortsnamen "wie damals"
anzeigen, (2) die Karte weiß, wo ein untergegangener Ort lag, (3) beim Austausch mit Verwandten
oder Vereinen meint "Königsberg" nachweislich denselben Ort.

**Was zu klären ist:** Ob man GOV-Daten offline mitliefern darf oder nur online abfragen. Das ist
ausschließlich eine Lizenzfrage bei CompGen, kein technisches Problem. **Vorschlag:** Wir
verschieben das auf Phase 6 und bauen bis dahin die eigene Ortsentität so, dass eine GOV-ID
später einfach eingehängt werden kann (Tabelle `ort_externe_id` existiert bereits). Du verlierst
dadurch nichts.

---

## 3. Beantwortet, zweite Runde

| # | Frage | Antwort | Wirkung |
|---|---|---|---|
| N1 | Name | **Wurzelwerk** | E18; Repository `wurzelwerk`, Dateiendung bleibt `.ahnen` |
| N2 | Russlanddeutsche/Mennoniten? | unklar, könnte zutreffen | offen gehalten, siehe §4 |
| N3 | Audio | nur einbinden | E19, A-16; keine Aufnahmefunktion |
| N4 | Gesprächspartner/Zeitdruck | keine Rücksicht nötig | Dringlichkeitsargument in 10_Vision §5 zurückgenommen; Reihenfolge bleibt trotzdem "Erfassung zuerst", nun aus der Datenlage begründet |
| N5 | Kyrillisch | automatische Umschrift | E20, A-19, ADR-014 (ISO 9 als Standard, umkehrbar) |
| N6 | Konfidenzstufen | Vorschlag angenommen | E21; vier Stufen, "widersprüchlich" als abgeleiteter Zustand → 50_Datenmodell §2.16 |
| N7 | Platzhalter-Ersetzung | einverstanden | E22; eigene Transaktionsart im Journal |
| N8 | Windows | irgendwann möglich | E23; Windows bleibt gleichrangig, Testrunde spätestens Ende Phase 2 |
| N9 | ICD-10 | Katalog genügt | E24; M-09 gestrichen |

---

## 4. Verbleibend

**V1 — Wanderungsmuster (aus N2).** Ob die Kette Deutschland → Polen/Russland → Baltikum →
Kanada dem Muster der Russlanddeutschen oder Mennoniten folgt, ist noch offen. **Das blockiert
nichts:** Das Datenmodell ist mit zeitabhängigen Ortsobjekten, freien Ortstypen und dem
Feld-Definitionssystem (A-18) so gebaut, dass "Kolonie", "Mennonitengemeinde" oder jede andere
Kategorie später ohne Schemaänderung ergänzt werden kann.

Der Punkt klärt sich von selbst, sobald die ersten echten Orte und Gemeinden erfasst sind.
Wenn du willst, kann ich dann eine gezielte Quellenrecherche für die konkreten Regionen machen
(Wolhynien, Wolga, Schwarzmeer, Manitoba/Saskatchewan haben jeweils eigene, gut erschlossene
Bestände) — sinnvoll aber erst, wenn ein paar Ortsnamen bekannt sind.

**Damit ist die Konzeptionsphase abgeschlossen.** Für die Phasen 0 bis 3 gibt es keine
blockierenden Entscheidungen mehr.

---

## 5. Neu aus der Architekturplanung (23.08.2026)

Aus der Planung von Phase 0 und 1 (`55_Architektur.md`, `56_Import_Vertrag.md`) sind acht
Punkte offen. **Nur V2 blockiert ein Arbeitspaket** (AP-0.6), alles andere sind Vorgaben, die
schon gesetzt sind und die du bestätigen oder ändern kannst.

| # | Punkt | Vorschlag / Vorgabe | Blockiert | Wo entschieden wird |
|---|---|---|---|---|
| **V2** | **Konfidenz: 4 oder 5 Stufen?** E21 und `50_Datenmodell.md` §2.16 sagen vier; B-03 in `40_Anforderungen.md` und die Schemakommentare `1..5` sagen fünf. | **Vier** (E21 ist die jüngere, begründete Entscheidung). Geht als `CHECK (konfidenz BETWEEN 1 AND 4)` ins Schema. | **AP-0.6** — eine Änderung danach ist eine Datenmigration über alle belegtragenden Tabellen | **entschieden: vier (07.09.2026, E33)** |
| V3 | Persona-Phase: §2.8 sagt Phase 3, D2 sagt „leer bis Phase 4", B-05 sagt Phase 3 | Tabelle ab Phase 0, Befüllung Phase 3. In `50_Datenmodell.md` N.6 festgehalten. | nichts | erledigt, nur bestätigen |
| V4 | Netzwerkansicht: `70_UX_Konzept.md` §2 sagt Phase 4, C-20 und E17 sagen Phase 5 | Phase 5. `70_UX_Konzept.md` ist nachgezogen. | nichts | erledigt, nur bestätigen |
| V5 | Schwellwert für journalisierte Kleinimporte (ADR-019) | **500 geänderte Zeilen.** Darunter ein normaler Undo-Schritt, darüber Rücknahme über Schnappschuss. | nichts | wenn die ersten echten Importe da sind |
| V6 | Journal-Aufbewahrung (ADR-003 Konsequenz) | **30 Tage und mindestens 200 Transaktionen** bleiben rücknehmbar; älter wird nur `aenderung` aufgeräumt, die Historie bleibt lesbar. | nichts | wenn die Datei spürbar wächst |
| V7 | `synchronous = NORMAL` oder `FULL`? | **NORMAL** nach `50_Datenmodell.md` §3. Konkret: Ein Programmabsturz kostet keine committete Transaktion. Ein **Stromausfall** kann die letzten committeten Transaktionen kosten (die Datei bleibt intakt). `FULL` würde auch das abdecken, kostet aber je Feldänderung einen `fsync` — bei Auto-Speicherung spürbar. | nichts | hier, wenn du das anders willst |
| V8 | Sprache der Bezeichner im Code (ADR-021) | **Fachbegriffe deutsch, Technik englisch.** Begründung: Das Schema ist deutsch; eine Übersetzungsschicht zwischen Schema und Code ist die Fehlerquelle, die KI beim Umbenennen bedient. | nichts | hier, es prägt jede Datei |
| V9 | `55_Architektur.md` ist ~1.480 Zeilen und überschreitet die 800-Zeilen-Regel aus `90_Arbeitsweise_KI.md` §2 | Aufteilen, sobald es beim Arbeiten stört. Natürliche Schnittstelle: **55a** = Schichten, Ordnerstruktur, IPC, Datenzugriff (§1–3); **55b** = Journal, Undo, Zustand, Layout, Migration, Fehler (§4–13). | nichts | nach den ersten Arbeitspaketen |

### Was diese Runde geklärt hat (Protokoll)

| # | Frage | Antwort | Wirkung |
|---|---|---|---|
| A1 | Umfang Phase 1 | **wie dokumentiert**, nicht gekürzt | Die *Reihenfolge* in `57_Phase0_Arbeitspakete.md` ist so gewählt, dass nach AP-1.7 echte Daten sichtbar sind |
| A2 | Import-Vertrag oder Maske zuerst? | **Schema und Trockenlauf zuerst** | ADR-010 wörtlich; AP-1.3 bis AP-1.5 vor jeder Bearbeitungsmaske |
| A3 | Ort des Code-Repositories | **`~/Claude/Projects/Ahnenforschung/wurzelwerk`** (Konzept in `Wissen/` daneben; geändert 07.09., E49) | ADRs als Kopie in `docs/adr/`; Konzeptordner bleibt ungetrackt |

---

## 6. Neu aus der Designvorbereitung (24.08.2026)

Keiner dieser Punkte blockiert ein Arbeitspaket. V10 bis V12 klären sich durch die Designarbeit
selbst; V13 bis V16 sind Vorgaben, die ich gesetzt habe und die du bestätigen oder ändern kannst.

| # | Punkt | Vorschlag / Vorgabe | Wo entschieden wird |
|---|---|---|---|
| V10 | **Schriftfamilien** — welche drei (Oberfläche, Originalzitate, Technisches)? | Claude Design entscheidet nach den fünf Kriterien in `71_Designsystem.md` §1.3 und der Sperrliste in §4.1. Danach als E-Zeile in `00_INDEX.md` festhalten, weil es eine Lizenz- und Auslieferungsentscheidung ist | nach Welle 0 |
| V11 | **Symbolsatz** | dito. Ein einziger Satz, ~80 Symbole, kein Emoji, nicht die üblichen Verdächtigen | nach Welle 0 |
| V12 | **Konkrete Tokenwerte** | Ergebnis der Designarbeit. Die Namen sind gesetzt (E37), deshalb ist jede Iteration wertfrei für den Code | Welle 0 und 1 |
| V13 | Kompakte Dichte als Vorgabe im Interview-Modus? | **Ja**, vorgeschlagen — dort zählen Zeilen pro Bildschirm mehr als Ruhe | hier |
| V14 | Screenreader-Grenze bei der Baumansicht | **Akzeptiert:** die Listenansicht ist der barrierefreie Zugang zu denselben Daten. Ein Graph mit 2.000 Knoten ist für einen Screenreader kein sinnvolles Ziel. Steht in `71_Designsystem.md` §5 als bewusste Grenze, damit sie später nicht als Versehen erscheint | hier |
| V15 | Ist der Petrol-Akzent gesetzt oder auch offen? | Ich habe ihn als **gesetzt** behandelt (E36), weil du ihn ausgewählt hast, und den Rest der Sprache offen gelassen. Wenn auch der Akzent offen sein soll: `73_Design_Briefing.md` §5.3 und `71_Designsystem.md` §0 anpassen | hier |
| V16 | Reihenfolge Designarbeit und Phase 0 | **Unabhängig.** Phase 0 braucht kein Design (AP-0.1 bis AP-0.15 sind Fundament ohne sichtbares Feature); die Designarbeit braucht keinen Code. Beide können parallel laufen, treffen sich bei AP-1.6 | hier |

### Was diese Runde geklärt hat (Protokoll)

| # | Frage | Antwort | Wirkung |
|---|---|---|---|
| B1 | Akzentfarbe | **gedecktes Petrol / Blaugrün** | E36 |
| B2 | Schriften | Claude Design erarbeitet die Designsprache, **ohne** typische KI-Standardgriffe (kein Inter, kein Roboto) | E38, Sperrliste in `71_Designsystem.md` §4 und `73_Design_Briefing.md` §6 |
| B3 | Umfang des Prototyps | **alles bis Phase 5**, inklusive Karte, Zeitleiste und medizinischer Ansicht | E39, umgesetzt als Zweistufigkeit in `72_Screens_und_Flows.md` §0 |
| B4 | Leitfassung hell oder dunkel | **beide gleichrangig entwerfen** | jedes Artboard in beiden Themen; in `74_Prompts_Claude_Design.md` in jeder Welle verlangt |

---

## 7. Neu aus dem Funktionsabgleich (06.09.2026)

Aus dem Abgleich mit MyHeritage (Family Tree Builder), Legacy und der MyHeritage-Hilfe (`30_Markt`, `40_Anforderungen` Nachtrag 4). Nur ein neuer offener Punkt, der Rest ist gesetzt und bestätigbar.

| # | Punkt | Vorschlag / Vorgabe | Blockiert | Wo entschieden |
|---|---|---|---|---|
| V17 | **Kartengrundlage** — das Bild unter den Ortspunkten. G-02 verlangt vollständig offline, ein Online-Kachelserver fällt damit aus. | **Natural Earth (gebündelt, Public Domain)** als neutrale Basis — zeigt bewusst *keine* modernen Grenzen/Namen, damit die historischen Ortsnamen aus dem Zeitregler tragen. Optional ein **OSM/PMTiles-Regionsdownload** (MapLibre GL) für Detailwünsche; Attribution ODbL („© OpenStreetMap-Mitwirkende"). Historische Grenzen als eigene Ebene → Phase 6. | nichts (Phase 3) | hier bzw. bei Beginn Phase 3 |

**Koordinatenquellen** sind bereits geklärt und kein offener Punkt: `ort.koordinaten` + `ort_externe_id` (gov/geonames/wikidata), GOV wie in T4 (Phase 6, Lizenzfrage bei CompGen).

### Was diese Runde geklärt hat (Protokoll)

| # | Frage | Antwort | Wirkung |
|---|---|---|---|
| C1 | Foto-KI (Kolorierung/Restaurierung/Animation) | **erstmal raus** | E41, W-07; Cloud/GPU-Thema, widerspricht der Offline-Linie |
| C2 | KI-„Smartmatch" (Bestandsauflösung) | eingeführt | E42, D-14/D-15; „Vater von A ist B" verbindet beim Import, W-05 gewahrt |
| C3 | Kleinere B/C-Lücken aus dem Abgleich | alle nachgezogen | E43; A-20/21/22, C-23/24/25, E-07/08, F-10, C-16 erweitert, B-09 vorgemerkt |
| C4 | GEDCOM Im-/Export | bereits abgedeckt | D-07 (Must, Phase 4) Export+Import, D-08 (Could) 5.5.1-Import, D-09 Import-Bericht |

---

## 8. Neu aus dem Claude-Design-Abgleich (06.09.2026)

| # | Punkt | Vorschlag / Vorgabe | Blockiert | Wo entschieden |
|---|---|---|---|---|
| V18 | **Stationsarten ↔ Ereignis-/Aussagemodell** — Claude Design führt „Lebensstationen" mit konfigurierbarem Arten-Katalog (A-23) ein; der Import-Vertrag nutzt für `ereignis.typ` ein festes Enum und für Beruf/Wohnort `aussagen`. | Abbilden: eine Lebensstation ist entweder ein `ereignis` (Ort/Datum/Rollen) oder eine zeitraumfähige `aussage`; der Arten-Katalog ist ein Typ-/Felddefinitionssystem (Nähe zu A-18). Vor der Umsetzung von A-23 in `50_Datenmodell.md` klären. | A-23-**Umsetzung** (nicht die Konzeption) | bei der Datenmodell-Runde zu A-23 |

### Was diese Runde geklärt hat (Protokoll)

| # | Frage | Antwort | Wirkung |
|---|---|---|---|
| D1 | Schriften/Symbolik (V10/V11) | **Source Sans 3 / Source Serif 4 / IBM Plex Mono** | E45; SIL OFL, offline, Sperrliste-konform |
| D2 | Oberflächensprachen | **DE, RU, UK, EN ab Phase 1** | E46; G-08 erweitert |
| D3 | Lebensstationen + Arten-Katalog | in Scope (A-23) | E47; Modell-Abgleich V18 |
| D4 | „Aus Statistik ausschließen" | aufgenommen | A-24 |
| D5 | Fehlende Bildschirme | als Design-Prompt formuliert | 74_Prompts §14 (Wellen 7–9) |

---

## 9. Umsetzungsentscheidungen aus den Arbeitspaketen

| # | Punkt | Entscheidung | Grund | Wo |
|---|---|---|---|---|
| U-AP03 | **i18n-Ressourcen-Pfad** — AP-0.3 nennt als Ablageort `src/renderer/i18n/de/*.json`. | Ressourcen liegen unter **`src/shared/i18n/de/*.json`**; nur `einrichten.ts` bleibt unter `src/renderer/i18n/`. | Die Menü-Beschriftungen werden im **Main**-Prozess gebaut (`src/main/menue/menue.ts`), und **Main darf `src/renderer/` nie importieren** (CLAUDE.md §2, harte Grenze). `src/shared` ist die einzige Schicht, die Main **und** Renderer lesen dürfen. §2 gewinnt gegen die Pfadangabe der AP. | AP-0.3, PR #3 |
| U-AP08 | **Zeitstempel-Befüllung `erstellt_am`/`geaendert_am`** — Migration 0002 vermerkt „Befüllung Trigger AP-0.8". | Nicht per Trigger, sondern im **main-Befehlshandler** (AP-0.9) in **Millisekunden** (`Date.now()`) gesetzt. | SQLite-`BEFORE`-Trigger können `NEW.*` nicht setzen; eine Fill-Variante bräche die ADR-009-Redo-Bitgleichheit (Undo/Redo spielt gespeicherte JSON zurück). Die angewendete Migration 0002 bleibt unverändert (§6) — der Kommentar dort ist historisch. | AP-0.8/0.9, PR #12/#14 |
| U-AP10 | **Transaktionsklammer für Undo/Redo** — §2: „Nur `src/main/befehle/` öffnet Transaktionen". | `undo`/`redo` öffnen eine **eigene** `IMMEDIATE`-TX in `src/main/journal/undo.ts` mit kommentierter §2-Ausnahme (wie `trigger.ts`), `defer_foreign_keys=ON` in beiden Richtungen. | Undo/Redo ist keine fachliche Mutation über den Bus, sondern spielt Journalzeilen zurück; es braucht dieselbe atomare Klammer, kann aber nicht durch `befehle/` laufen (es ist kein Befehl). | AP-0.10, PR #18 |
| U-AP12 | **Fixture-Beschreibung erweitert** — AP-0.12 nennt den Umfang nur mit Personen/Namen/Elternschaften. | `FixtureBeschreibung` trägt zusätzlich optionale `ereignisse`/`beteiligungen`; Fixtures als **TS-Datenmodule** (keine committeten `.sqlite`); 20 000er-Korpus-Gesundheit im **Budget-Gate**, nicht im Per-PR-Gate. | `person`/`name`/`elternschaft` haben keine Datumsspalten — Datum lebt nur auf `ereignis`; ohne die Erweiterung wäre die geforderte Fixture „unscharfe-datumsangaben" nicht modellierbar. TS-Module sind diffbar/typgeprüft; ein 20k-`.sqlite` gehört weder in git noch in ein schnelles Gate. | AP-0.12, PR #21/#22 |
| U-AP13a | **Anzeige des Datenbestand-Berichts** — AP-0.13 nennt nur "zeigt einen Bericht", ohne Bildschirm-Vorgabe. | Bericht läuft über einen **nativen Electron-Dialog** (`dialog.showMessageBox`, `src/main/wartung/datenbestand-pruefen.ts`), bewusst **kein** eigener Renderer-Screen. | Es gibt vor dem Design-Fundament (AP-1.6) noch keinen Bildschirm/Baustein für einen Prüfbericht (CLAUDE.md §14 Fall 1); ein nativer Dialog liefert die Funktion, ohne eine neue visuelle Sprache zu erfinden. Kann nachgerüstet werden: eine `abfrage:`-IPC-Variante mit Renderer-Anzeige, sobald das Fundament steht. | AP-0.13 |
| U-AP13b | **Kein neuer Fehlercode für Prüf-Funde** — §7 verlangt für jeden `FehlerCode` einen i18n-Eintrag. | `datenbestandBericht()`/`ableitungAbweichung()` werfen nicht und bekommen **keinen** neuen Eintrag in `src/shared/fehler/codes.ts`. | Ein Fund (Integritäts-/Fremdschlüssel-/Ableitungs-/Zyklus-Abweichung) ist kein Fehlerzustand der Anwendung, sondern ein Berichtsergebnis für die Nutzerin — er verhindert/verändert nichts. Ein Fehlercode wäre semantisch falsch (§7 ist für Ausnahmen gedacht, die über IPC als `Ergebnis<T>`-Fehler laufen). | AP-0.13 |
| U-AP13c | **Laufzeit des SIGKILL-Tests** — `test/absturz/sigkill.test.ts` startet 24 echte Kindprozesse. | Test läuft vorerst im **normalen Per-PR-Gate** (`pnpm test`), lokal/macOS ~3,7 s für alle 24 Läufe. Falls die Windows-CI-Laufzeit sich als unverhältnismäßig hoch herausstellt, in die **langsamen Gates** (`test:e2e`/`test:budget`-Lauf) verschieben. | Absturzsicherheit ist laut CLAUDE.md §5 eine reguläre Testkategorie (`test/absturz/`), kein per se langsamer Gate-Kandidat; die gemessene lokale Laufzeit rechtfertigt noch keine Sonderbehandlung. Windows-Prozessstart (`node --import tsx`) ist aber unbekannt langsamer — offener Beobachtungspunkt für die erste Windows-CI-Runde. | AP-0.13 |
| U-AP15a | **Fenster-Anker der Koaleszenz** (55_Architektur.md §4.8) nennt kein festes Ankerverfahren für das 2-Sekunden-Fenster. | **Gleitendes Fenster** als Default: jede erfolgreich zusammengefasste Änderung setzt `transaktion.zeitpunkt` der verdichteten Zeile auf den Zeitpunkt der jüngsten Teiländerung — eine weitere schnelle Änderung bekommt dadurch wieder das volle Fenster. Alternative (nicht gewählt): **Anker = erste Änderung** (festes Fenster ab der ersten Teiländerung, unabhängig von Nachzüglern). | Ein gleitendes Fenster bildet fortlaufendes Tippen (z. B. in einem Notizfeld) natürlicher ab als ein hartes Zeitlimit, das mitten in einer zusammenhängenden Eingabe abreißt. Reine Implementierungsentscheidung ohne Datenmodell-/Architekturkonsequenz — bei Bedarf per Folge-AP auf den festen Anker umstellbar. | AP-0.15 |
| U-AP15b | **Hochwassermarke im SIGKILL-Test** (`test/absturz/kind-prozess.ts`, AP-0.13) zählte bisher erfolgreiche `fuehreAus()`-Aufrufe, nicht persistierte `transaktion`-Zeilen. | Umgestellt auf `COUNT(*) FROM transaktion` unmittelbar nach jedem Aufruf — die reine Aufruf-Zählung wäre seit der Koaleszenz (AP-0.15) systematisch zu hoch und hätte `sigkill.test.ts` nach jedem Merge fälschlich rot laufen lassen, obwohl kein Byte verlorenging. | Koaleszenz verdichtet mehrere erfolgreiche `person.feldSetzen(notiz)`-Aufrufe zu EINER `transaktion`-Zeile (das ist ihr Zweck) — ein Aufruf-Zähler und die tatsächlich persistierte Zeilenzahl laufen dadurch bewusst auseinander. `COUNT(*)` bleibt die richtige Grundlage für die WAL-Absturzzusage ("kein bereits committeter Stand geht verloren"), unabhängig von Koaleszenz. Kein Architektur-/Scope-Punkt, reine Testanpassung an eine erwartete Verhaltensänderung. | AP-0.15 |
| U-AP16 | **Rot-Beleg-Rezept von AP-0.16 korrigiert** — das Paket verlangt „Fenstertitel in `hauptfenster.ts` verfälschen, Job rot sehen". | Der wirksame Hebel ist der **Renderer-Dokumenttitel** (`src/renderer/index.html` `<title>`), nicht das `BrowserWindow`-`title`-Attribut. Rot-Beleg B wurde darüber geführt. | `expect(page).toHaveTitle(...)` liest den **Dokumenttitel** der geladenen Seite; das `BrowserWindow`-`title` ist davon entkoppelt, sobald der Renderer sein eigenes `<title>` lädt. Belegt: Job mit verfälschtem `hauptfenster.ts`-Titel lief **grün** durch, erst der verfälschte Dokumenttitel machte ihn rot. Kein Scope-/Datenmodellpunkt — reine Rezeptkorrektur (CLAUDE.md §14: weiterbauen, vermerken). | AP-0.16, PR #29 |
| U-AP17 | **`app.getAppPath()` im unverpackten `out/`-Lauf** — AP-0.17 verlangt „eine Auflösung für Entwicklung, Test und Paket". Der SPIKE ergab, dass `getAppPath()` bei `electron out/main/index.js` (`test:e2e`/`build`-Vorstufe) das Einstiegsverzeichnis **`out/main`** liefert, nicht das Repo-Root. | Basisverzeichnis wird **Parameter** von `migrationsDateiPfad`/`migrationsRohInhaltLesen`/`generierteTriggerAnwenden`; `schemaBasisverzeichnis()` (neu, `app.getAppPath()`+`docs/schema`) ist die einzige Laufzeit-Auflösung, `laeufer.ts` fällt für Vitest/Skripte auf `process.cwd()` zurück. `docs/schema/**` geht byte-identisch über `electron-builder.yml` `files:` in die asar; ein `electron.vite.config.ts`-Plugin (`cpSync`, roh) legt es zusätzlich nach `out/main`. `journal-trigger-anwenden.ts` mitgezogen (läuft zwingend auf dem Anlege-Pfad). | Byte-Identität (Prüfsummen in `registrierung.ts`, §6) verbietet String-Einbettung/EOL-Normalisierung — `cpSync`/electron-builder kopieren roh. **Offener Folgepunkt (Nutzer bestätigt):** den automatisierten E2E langfristig gegen das **electron-builder-Paket** statt gegen `out/` laufen lassen, damit der Verpackungsbug maschinell rot wird (schwerer: Pfad zum Binary, längere CI). Für AP-0.17 belegt ihn ein menschlich angesehener `pnpm build`-Lauf (anlegen→schließen→öffnen: ABNAHME OK). | AP-0.17, PR #30 |
| U-AP18 | **Umfangserweiterung + Einzelinstanz-Abnahme (§14)** — AP-0.18 listet als Umfang nur `index.ts`/`projekt-dienst.ts`/`projekt-dienst.test.ts`/e2e. | (a) Neues Modul `src/main/lebenszyklus.ts` (`appLebenszyklusVerdrahten`) + `test/einheit/lebenszyklus.test.ts` **zusätzlich** angelegt, damit das `before-quit`/Einzelinstanz-Wiring als **schneller, deterministischer** Einheitswächter prüfbar ist (statt allein am build-abhängigen e2e zu hängen). `index.ts` wird dünn. (b) Die Einzelinstanz-Abnahme („zweite Instanz fokussiert die erste") wird **auf Einheitsebene mit Fake-`app`** geprüft, **nicht** per zweitem Electron-Start im e2e. | (a) `index.ts` hat Import-Nebenwirkungen (`whenReady` beim Laden) und ist als Einheit schwer testbar; die Extraktion ist ein sauberer Seam ohne Architekturbruch (`src/main` darf `electron`, §2). (b) Fensterfokus zweier echter Instanzen ist OS-/Compositor-abhängig und damit flaky — eine nichtdeterministisch rote Prüfung wird von der Loop „wegoptimiert" (CLAUDE.md §13). Reine Test-/Struktur­entscheidung ohne Datenmodell-/Scope-Folge. | AP-0.18, PR #31 |
| U-AP19 | **Pauschale Fehlerabbildung im `oeffnen()`-Fehlerpfad** (hueter-Auflage AP-0.19, nicht-blockierend) — `projekt-dienst.ts` bildet in `projektUebernehmen` JEDEN Nicht-`WurzelFehler`-Wurf aus `oeffnen()` auf `DATENBANK_INTEGRITAET` ab. | **Folge-PR:** auf tatsächliche SQLite-Korruptionscodes eingrenzen (analog `sqliteCodeZuFehlerCode` in `src/main/ipc/huelle.ts`), damit `ENOSPC`/`EACCES`/`EMFILE` beim Öffnen nicht fälschlich als „Datenbankintegrität verletzt" gemeldet werden. Eigener PR mit Rot-Test (§5), nicht in AP-0.19 nachgeschoben. | Sehr seltene Fehlerklasse, eng auf `oeffnen()` begrenzt → kein Blocker für AP-0.19; die AP behebt Sperre+Leck, die Fehlertext-Präzisierung ist orthogonal und verdient einen eigenen roten Beleg. | AP-0.19, PR #32 |
| U-AP20 | **`ereignis:`-Vertrag nur senderseitig geschlossen** (hueter-Auflage AP-0.20, nicht-blockierend) — `sendeEreignis` bindet Kanal↔Nutzlast (typgeprüft), aber die Empfängerseite `window.wurzelwerk.abonnieren` in `src/renderer/brücke/global.d.ts` ist weiter `(kanal: string, hoerer: (nutzlast: unknown) => void)`. Ein Tippfehler im Kanalnamen beim Abonnieren fällt tsc nicht auf (Preload-Weißliste ignoriert ihn still als No-op). | **Folge-AP:** `abonnieren` an `EreignisKanal`/`EreignisNutzlast<K>` binden (z. B. generisch `abonnieren<K extends EreignisKanal>(kanal: K, hoerer: (n: EreignisNutzlast<K>) => void)`), Preload-Signatur + `global.d.ts` nachziehen; passt zu AP-0.25 (Werkzeug-/Vertragshygiene). | Vorbestehend (`global.d.ts` nicht im AP-0.20-Diff), außerhalb des AP-0.20-Scopes (`sendeEreignis`-Bindung) → kein Blocker; die Senderichtung — der belegte Befund „Tippfehler sendet still ins Nichts" — ist geschlossen. | AP-0.20, PR #33 |
| U-AP21 | **Deckungsgrenzen der Invariante `journal-schluessel`** (opus-hueter-Auflagen AP-0.21 PR-B, alle nicht-blockierend) — drei Punkte: (1) der Aritäts-`throw` in `datensatzIdZerlegen` (`basis.ts`) ist testweit ungedeckt; (2) der Round-Trip rekonstruiert `datensatz_id` in JS statt das reale trigger-erzeugte `aenderung.datensatz_id` zu prüfen → eine `pkSpalten`-vs-`datensatzIdSql`-Reihenfolgedivergenz bliebe unsichtbar; (3) die vier Verbund-PK-journalisierten Tabellen (`ort_externe_id`, `partnerschaft_person`, `aussage_zitat`, `medium_zuordnung`) sind im gesamten AP-0.12-Korpus leer → der reale Daten-Round-Trip deckt sie nicht ab (nur der synthetische Anti-Vakuum-Block). | **Folge-PRs (außerhalb des geschützten Pfads):** (1) Negativtest in `test/einheit/` (`datensatzIdZerlegen('a|b|c', ['x','y'])` wirft). (2)+(3) Fixture-/Generator-Korpus um Verbund-PK-Zeilen anreichern und den Round-Trip gegen das reale `aenderung.datensatz_id` einer trigger-erzeugten Zeile führen — passt zu AP-0.25 bzw. einer Korpus-Erweiterung. | Die Invariante selbst ist nachweislich nicht vakuös (Mutation `split('|')→split(':')` → rot, vom synthetischen Block gefangen); die drei Punkte sind Deckungs-/Scope-Grenzen, keine Lücke der Kernaussage → kein Blocker. | AP-0.21, PR #35 |
| U-AP1.1 | **i18n-Ressourcen ohne Renderer-Namensraum-Registrierung** — AP-1.1 legt `src/shared/i18n/de/datum.json` an (von `src/core/datum/formatierer.ts` referenziert, Schlüssel `datum:um`/`vor`/`nach`/`zwischen`/`tag_monat_jahr`/`monat_jahr`/`jahr`/`jahrzehnt`/`originaltext`), registriert den Namensraum `datum` aber **nicht** in `src/renderer/i18n/einrichten.ts` (`ns`/`resources`). | Bewusst verschoben: AP-1.1 ist reiner Kernumfang (Phase 1, kein Bildschirm) — die Registrierung ist eine Ein-Zeilen-Ergänzung, sobald ein Renderer-Baustein den `datum`-Namensraum tatsächlich braucht (frühestens AP-1.6 Design-Fundament o. ä.). | AP-1.1 PR-A |
| U-AP1.1-K | **Rechenfehler im Abnahmebeispiel korrigiert (§14)** — der AP-1.1-Abnahmetext nannte `1700-02-18 jul. = 1700-03-01 greg.`; das ist um einen Tag falsch. 1700 ist julianisches, aber kein gregorianisches Schaltjahr, der Zehn-Tage-Versatz gilt bis zum julianischen Schalttag. | Korrigiert auf **`1700-02-18 jul. = 1700-02-28 greg.`** (JDN 2342031, unabhängig gegen Anker `2000-01-01 greg. = JDN 2451545` sowie durch `hueter` nachgerechnet). `test/einheit/datum-kalender.test.ts` nutzt das korrigierte Paar plus `1700-03-01 jul. = 1700-03-12 greg.` als Beleg des Elf-Tage-Sprungs. `docs/arbeitspakete.md` im selben PR nachgezogen; die externe Quelle `../Wissen/57` wird separat angeglichen. | AP-1.1 PR-A |
| U-AP1.3a | **IMP-Codes bewusst nicht in `ALLE_FEHLERCODES` gemischt** — §7 verlangt für jeden `FehlerCode` einen i18n-Eintrag; die neue geschlossene Union `ALLE_IMP_CODES` (`src/shared/import/imp-codes.ts`, IMP-101…IMP-107) ist eine EIGENE Liste. | Getrennt gehalten: `ALLE_FEHLERCODES` bleibt die AppFehler-/IPC-`Ergebnis<T>`-Union (§7); die IMP-Codes sind Berichtsbefunde eines Prüflaufs (`pruefeStufe1`) über eine Importdatei, kein Anwendungsfehlerzustand. Eigener i18n-Namensraum `src/shared/i18n/de/import.json` (Schlüssel `import.fehler.IMP_101` … `IMP_107`, je `titel`/`beschreibung`/`was_tun` nach §5), eigener Vollständigkeitstest statt Erweiterung von `i18n-vollstaendig.test.ts`. | AP-1.3a |
| U-AP1.3a-i18n | **`import.json` (wie zuvor `datum.json`, U-AP1.1) ohne Renderer-Namensraum-Registrierung** — es gibt vor der Import-Oberfläche (frühestens AP-1.4b) noch keinen Bildschirm, der den Namensraum `import` braucht. | Bewusst verschoben: `src/renderer/i18n/einrichten.ts` bleibt unverändert; die Registrierung ist eine Ein-Zeilen-Ergänzung, sobald ein Renderer-Baustein den Namensraum tatsächlich anzeigt. | AP-1.3a |
| U-AP1.3a-cov | **Lücken in der fixturebasierten Gleichwertigkeit (Ajv/Zod)** — der Fixture-Korpus (AP-1.3a) deckt ~8 Constraint-Familien nicht ab: `koordinaten`-Grenzen (lat/lon min/max), `doppeljahr`-Muster, `kategorie: krebs` → `organ`-Pflicht, `umschrift_von` → `umschrift_norm`-Pflicht, Konfidenz-/`alter_bei_diagnose`-Grenzwerte, `externe_ids.system`-Enum, Aussage-`anyOf` (`wert_text`/`wert_zahl`/`wert_ref`). Vom `hueter` einzeln am Zod-/JSON-Schema-Quellcode geprüft und divergenzfrei befunden — aber ohne eigene Fixture bliebe eine künftige Divergenz in genau diesen Familien vom Gleichwertigkeitstest unbemerkt. | **Folge-PR:** je Constraint-Familie mindestens eine gezielte Grenz-Fixture unter `fixtures/import/v1/fehlerhaft/` (bzw. eine zusätzliche `gueltig/`-Variante an der Grenze) ergänzen, konsistent mit der bestehenden „kein Generator, handgeschriebene Fixtures"-Linie aus AP-1.3a. Kein Blocker für AP-1.3a selbst — die Fitnessfunktion ist für den bestehenden Korpus bereits scharf, die Lücke betrifft nur ungetestete zusätzliche Familien. | AP-1.3a, hueter-Review PR #55 |
| U-AP1.3b-kanal | **Kanalname für die Stufe-1/2-Prüfung** — der AP-1.3b-Auftrag nannte `befehl:import.pruefen`. | Registriert als **`abfrage:import.pruefen`** (`ein: { pfad: string }`, `aus: PruefBericht`, `src/shared/ipc/{kanaele,vertrag}.ts`, `src/main/ipc/registrierung.ts`). | Die Prüfung schreibt nichts (kein `BEGIN`, kein Journal, keine `aenderung`-Zeile) — `befehl:`-Kanäle sind laut `docs/architektur.md` §11/ADR-016 für mutierende, journalisierte Vorgänge reserviert. Ein lesender Vorgang gehört unter `abfrage:`, auch wenn er (wie hier) mehrere Prüfschritte kapselt. | AP-1.3b |

---

## 10. Neu aus dem Codereview (14.09.2026)

| ID | Frage | Stand |
|---|---|---|
| U-R1 | **Ruleset „Standard" bindet nichts.** Bypass-Actor ist die Repository-Rolle *Admin* im Modus `always`, und `required_signatures` wird von jedem Commit verletzt (keiner ist signiert). Entweder Commit-Signierung einrichten (SSH-Signing, ~5 Minuten) oder `required_signatures` aus dem Ruleset nehmen. Beides ist besser als zwölf „Bypassed rule violations" pro Kettenlauf — in dem Rauschen geht die eine echte Verletzung unter. | offen, Eigentümerentscheidung |
| U-R2 | **Merge-Gate serverseitig statt per Skript.** Die drei Jobs (`pruefen (macos-latest)`, `pruefen (windows-latest)`, `langsame Gates`) als `required_status_checks` ins Ruleset. Dann verhindert der Server den Rot-Merge, statt dass ein Mensch oder `gh pr checks` ihn verhindern soll. Macht den maschinellen Weg aus `CLAUDE.md` §9 überflüssig. | offen, Eigentümerentscheidung |
| U-R3 | **Repo ist öffentlich.** `CLAUDE.md` §9 sagte „privates Repo" — korrigiert. Personendaten liegen keine im Repo (Fixtures sind generiert), aber die Sichtbarkeit sollte eine bewusste Entscheidung sein, keine übersehene. | benannt, Entscheidung offen |

---

## 11. Design-Fundament übernehmen (Querläufer, `Wissen/58`)

Abweichungen zwischen dem Roh-Design-Export und `71_Designsystem.md` §1, beim Übertragen des Token-Vertrags nach `src/renderer/gestaltung/tokens.css` (CLAUDE.md §14: weiterbauen und vermerken, nicht stillschweigend füllen oder ignorieren).

| ID | Punkt | Stand |
|---|---|---|
| U-DF1 | **18 additive Tokens im Export, die §1 nicht benennt** — übernommen, weil sie dem Namensschema `--wz-…` folgen und von Bildschirmentwürfen gebraucht werden, ohne eine §1-Rolle umzubenennen: §1.8-Mehrsprachigkeit (`--wz-sprachen-ui`, `--wz-name-hauptname-marke`, `--wz-name-transliteration`, `--wz-name-schrift-etikett`), Typo-Helfer (`--wz-laufweite-titel-gross`, `--wz-laufweite-beschriftung`, `--wz-ziffern-tabelle`), Fokus-/Strich-Details (`--wz-rahmen-fokus-breite`, `--wz-rahmen-fokus-versatz`, `--wz-rahmen-gestrichelt-muster`), nachgetragene Datenrollen (`--wz-daten-kante`, `--wz-rahmen-verweis`, `--wz-flaeche-verweis`, `--wz-daten-spanne-belegt`, `--wz-daten-spanne-moeglich`, `--wz-rahmen-raster`, `--wz-muster-erinnerung`) und `--wz-trefferflaeche-min`. | vermerkt — Vorschlag: in `71` §1 aufnehmen |
| U-DF2 | **`--wz-beleg-*`-Tokens fehlen in der Tokenquelle.** `00_INDEX_Design.md` nennt `--wz-beleg-selbst-erlebt-*` / `--wz-beleg-hoerensagen-*` als Teil der Tokenkategorien, sie stehen aber **nicht** in der maßgeblichen `Wurzelwerk Designsprache.dc.html`, sondern nur in den Bildschirm-Dateien, und `71` §1 benannte sie nicht. Zunächst nicht übernommen (Quelle laut Auftrag ausschließlich die Designsprache-Datei). | **übernommen (PR #48, Review):** sechs Rollen `--wz-beleg-selbst-erlebt-*` / `--wz-beleg-hoerensagen-*` (je `-flaeche/-rahmen/-text`) nach `status-*`-Muster in `tokens.css` (beide Themen + `@media`) und in `71` §1.1 beider Bäume; Werte aus der Vertrauens-/Statuspalette abgeleitet (Grün = verankert, Ocker = vorläufig), Text-Kontrast ≥6:1 (WCAG AA), vom Nutzer freigegeben. Trägt Leitprinzip 1 (`10_Vision_Scope.md` §4). |
| U-DF3 | **Benennung der Abstände.** `71` §1.4 schreibt „`--wz-abstand-0` bis `--wz-abstand-24`", listet aber 13 Werte bis 96. Der Export benennt **nach Wert** (`--wz-abstand-96` …), konsistent mit der Werteliste; das Quelldokument markiert diese Benennung selbst als Abnahmepunkt. Export-Benennung übernommen. | vermerkt — Benennung nach Wert bestätigen |
| U-DF4 | **Diagnose-Kategorien folgten dem Export statt dem Schema (§14 Fall 2 — Review PR #48, „B1").** Der Design-Export (`Wurzelwerk Designsprache.dc.html`) führt `--wz-daten-diagnose-{nerven-psyche, bewegung, verdauung, sinne}`. Bindend ist aber die ausgelieferte CHECK-Bedingung `diagnose.kategorie` in `docs/schema/0002_kern.sql` (Schema v1, seit AP-0.6 auf `main`, in jeder migrierten DB aktiv): `herz_kreislauf, krebs, stoffwechsel, neuro_psych, atemwege, nieren, autoimmun, angeboren_genetisch, infektion, unfall, sonstiges`. Die Bausession hatte den Export **treu** übertragen (Fall 2, nicht Fall 1). Repo angeglichen (`neuro-psych/nieren/autoimmun/angeboren-genetisch`, Werte umgehängt); der Test leitet die Liste jetzt aus dem Schema-CHECK ab, damit künftige Divergenz rot wird (ADR-025). | **Repo behoben (PR #48).** Offen: **die Designquelle nachziehen** — sonst holt der nächste Export→Bildschirm-Bau den Fehler zurück. |
| U-DF5 | **Latent dieselbe Fehlerklasse bei `--wz-daten-beziehung-*` (Review-Nachtrag PR #48).** Tokens: `biologisch, adoptiv, stief, pflege, ehe, partnerschaft, geschieden, ungesichert`. Der ausgelieferte `elternschaft.typ`-CHECK (`0002_kern.sql:163`) führt `biologisch, adoptiv, stief, pflege, zieh, anerkannt, leihmutter, unbekannt` — `zieh/anerkannt/leihmutter/unbekannt` kommen in `70/71/72` **kein einziges Mal** vor (weder Kantenform §6 noch Farbe), und drei Tokens (`ehe/partnerschaft/geschieden`) sind keine Elternschaftstypen (Ehe/Scheidung sind Kantenformen §6 auf der Partner-/Ereignisseite). §1.2 bindet `-beziehung-` — anders als `-diagnose-` — an keine konkrete Schema-Enum, daher kein Test-Zwang. | **nicht blockierend** (Graph/Kanten erst Phase 2). Vor dem Kanten-Rendering Beziehungspalette gegen `elternschaft.typ` / `partnerschaft.typ` / §6-Kantenformen abgleichen. |
| U-DF6 | **Farbliteral-Prüfung erfasst nur `.css` und kein modernes Farbmodell (Review „D1").** `keine-literale.test.ts` fängt `#hex/rgb()/hsl()`, nicht `oklch()/oklab()/lab()/lch()/color()` und keine benannten Farben (`white`), und scannt nur `.css` — nicht `.tsx/.ts`. §1.7 Regel 2 gilt aber für „Komponentendateien", die ab AP-1.6 als `.tsx` mit möglichen Inline-Styles entstehen. | offen — **vor AP-1.6** auf `.tsx/.ts` ausweiten und um die modernen Farbmodelle + benannte Farben ergänzen. |
| U-DF7 | **`tabular-nums` beim UI-Font ist ein No-op (Review-Genauigkeit).** Source Sans 3 (`--wz-familie-ui`, trägt `--wz-schrift-zahl-tabelle`) hat **kein `tnum`-GSUB-Feature**; `--wz-ziffern-tabelle: tabular-nums` bewirkt dort nichts. Spalten tanzen trotzdem nicht, weil die Default-Ziffern von Source Sans 3 bereits gleiche Laufweite haben (Advance 472). Source Serif 4 hat echtes `tnum`, IBM Plex Mono ist monospaced. | vermerkt — Ergebnis korrekt; bei einem Font-Wechsel mit proportionalen Default-Ziffern trägt der Token die Zusage nicht. |
| U-DF8 | **Datenpaletten-Kontrast/CVD-Gate (D3) + Palette nachgestimmt.** `test/gestaltung/datenpaletten.test.ts` prüft §1.2 maschinell in beiden Themen (R1 ≥3:1 Fläche, R2 ≥4,5:1 Text, R3 ΔL*≥8 Konfidenz, R4 ΔE76≥5 unter Deuteranopie/Protanopie; Formelquellen im Kopf). Gegen die echten Werte war **R2 rot**; sechs text-tragende Hell-Flächen nachgedunkelt (`generation-6/-8`, `geschlecht-u`, `diagnose-autoimmun/-angeboren-genetisch/-sonstiges`; ΔL* 1–2, Farbton erhalten). **R2-Umfang** (Nutzerentscheid): nur text-tragende Flächen; `konfidenz` (Punkte, §2.1) und `beziehung` (Kanten, §5) tragen keinen Text und sind ausgenommen. | **Gate grün.** Offen: die **dunkle Konfidenzrampe** erfüllt R1∧R2∧R3 nicht zugleich (R1 verlangt L*≥50, R3 ≤49,8, R2 ≥56/≤44 — unlösbar ohne hellere Neuspreizung aller vier Stufen); bewusst vertagt, weil das die eingefrorene Ordinalpalette sichtbar ändert. |

**Review-Ergebnis (PR #48 — `hueter` + mechanische Verifikation):** U-DF4 (B1), die Deckung der Systemvorgabe (D2: `@media(prefers-color-scheme: dark)` deckungsgleich mit `[data-theme="dunkel"]`), die Beleg-Rollen (U-DF2) und das Datenpaletten-Gate (U-DF8/D3) sind im PR umgesetzt — je eigener Commit, Rot-Beleg vorab (eiserne Regel §5). Die Zählung im PR-Rumpf ist korrigiert: **139 §1-Rollen + 18 additiv = 157 `:root`-Deklarationen** (die frühere „140" zählte `--wz-schrift-koerper` doppelt — es ist zugleich §1.3-Typo-Rolle und §1.6-Dichtetoken).

---

## 12. Beleg-/Konfidenz-Anbindung an Schema (Kettenlauf-Halt, 17.09.2026)

| ID | Befund / Frage | Entscheidung | Konsequenz | Offen |
|---|---|---|---|---|
| **U-1.4a-beleg** | ~~Befund: Schema v1 habe kein Heim für `belege`/`konfidenz` an Person/Ereignis/Kante.~~ **GESCHLOSSEN 17.09.2026 — der Befund war falsch.** `aussage` (`0002_kern.sql:316`) nimmt alle vier Subjekttypen auf und trägt `konfidenz`; `aussage_zitat` (:344) ist die Belegverknüpfung; die abgeleitete Schicht liest die Personenkonfidenz **bereits** aus `aussage` (`0003_abgeleitet.sql:65`, gehalten von `abl_aussage_ai/au/ad` seit AP-0.7, abgesichert durch `abgeleitet-gleich`). `konfidenz`-Spalten an der Entität wären toter Ballast oder erzwängen einen Umbau von AP-0.7; eine `entity_zitat`-Tabelle legte einen zweiten Belegpfad neben `aussage_zitat`. Zudem ist `$defs/Beleg` spaltenweise die `zitat`-Tabelle, nicht die hier angenommene `typ`/`inhalt`/`quelle`-Form. **Entschieden:** keine `konfidenz`-Spalten, keine `entity_zitat`; Existenzbehauptung als Aussage (`praedikat='existenz'`, `wert_text='ja'`) — **ADR-026**, Konvention in `50` §2.7. `elternschaft.konfidenz` wird nicht mehr geschrieben (nullable, keine Migration). **Ersetzt durch U-1.4a-luecken.** | — | — | AP-1.4a, Prüfbericht 17.09.2026 |
| **U-1.4a-luecken** | ~~Die Migration, die es wirklich braucht.~~ **GESCHLOSSEN 17.09.2026.** Vier Vertragsfelder ohne Ziel im Schema — `$defs/Beleg.zeitmarke_sekunden` (8× in `beispiel-3-interview.json`), `$defs/Person.unsicherheit` (Pflicht bei `konfidenz ≤ 2`, IMP-206), `$defs/Aussage.unsicherheit` (≠ `begruendung`), `$defs/Aussage.gueltig_von`/`gueltig_bis` (A-08, benutzt in `beispiel-1-einfach.json:70/213`) — sowie ein geschlossenes CHECK auf `aussage.subjekt_typ`, das `diagnose` und `risikofaktor` aussperrte, obwohl `56` §2.3 für beide `belege` verlangt. | **Umgesetzt in AP-1.3c**, `docs/schema/0005_import_luecken.sql`: vier `ALTER TABLE ... ADD COLUMN` + CHECK-Erweiterung (6→8 Werte) per Tabellenneubau von `aussage` (Details und das dabei gefundene, sanktionierte Drei-Phasen-Neubaumuster: ADR-026-Nachtrag). Alle fünf in **einer** Migration, wie geplant. | Keine — Migration angewendet, `test/migration/import-luecken.test.ts` deckt Aufstieg, Datenerhalt und CHECK-Grenze ab. | AP-1.3c (erledigt) |
| **U-1.4a-icd10** | `diagnose.icd10` existiert als Spalte (`0002_kern.sql:497`), obwohl `56` §3.7 „**Kein ICD-10**" sagt (E24). | **Hingenommen.** Eine Spalte zu viel, die niemand füllt; sie zu entfernen wäre in SQLite ein zweiter Tabellenneubau. Bei der nächsten ohnehin fälligen Aufräummigration mitnehmen. | nicht blockierend | AP-1.3c, Prüfbericht 17.09.2026 |

---

## 13. Trockenlauf, Bericht, Plausibilität (AP-1.4a, kopflos)

| ID | Befund / Frage | Entscheidung | Konsequenz | Offen |
|---|---|---|---|---|
| **U-1.4a-imp302-beispiel2** | ~~`beispiel-2-widersprueche.json` trug für `tmp:erna` kein Geburtsdatum, wodurch die absichtlich falsche Elternkante `elternschaften[1]` (`tmp:august -> tmp:erna`) mit einer korrekten IMP-302-Implementierung nicht auslösen konnte.~~ **GELÖST — Fixture vervollständigt (Erna 1896).** Alle drei Kopien (`fixtures/import/v1/gueltig/`, `docs/56_Beispiele/`, `../Wissen/56_Beispiele/`, synchron gehalten) bekamen ein neues `ereignisse[]`-Element `tmp:e-geb-erna` (Geburt, exakt 1896 — der Sollwert aus `56_Import_Vertrag.md` §6.2: August „geb. etwa 1890" + 6 Jahre). `zusammenfassung.ereignisse` entsprechend auf 2 angehoben (IMP-105 bleibt grün, `import-schema-zod-gleich`/`import-zusammenfassung` unverändert grün). Der Trockenlauf-Bericht meldet jetzt genau EINEN Stufe-3-Fund: IMP-302 auf `elternschaften[1]` ("Elternteil wäre 6 Jahre alt gewesen") — nicht auf `elternschaften[0]` (Platzhaltervater ohne Geburtsdatum). | Keine offene Abweichung mehr. | `test/einheit/trockenlauf-bericht.test.ts` (aktualisiert), `test/einheit/plausibilitaet.test.ts` (unverändert, deckt IMP-302 zusätzlich isoliert ab). | Erledigt — Kettenlauf-Folgeauftrag, 17.09.2026 |
| **U-1.4a-blocklabel** | §14: Die sieben/acht Blocklabels des Trockenlauf-Berichts (`ZUSAMMENFASSUNG`, `WIRD ANGELEGT`, …, `GESUNDHEITSDATEN`, `src/main/import/bericht.ts`) stehen als deutsche String-Konstanten im Code, nicht über `i18next`/`src/renderer/i18n/de/`. | Wie zuvor bei `datum.json` (U-AP1.1): Es gibt vor der Import-Oberfläche (frühestens AP-1.4b) keinen Bildschirm, der diese Texte anzeigt — `bericht.ts` ist ein kopfloses Textartefakt (`alsText()`), kein UI-Baustein. | Keine Auswirkung auf AP-1.4a (keine Oberfläche in diesem AP). | Bei AP-1.4b: Blocklabels in `src/shared/i18n/de/import.json` überführen, `alsText()`/Renderer-Anzeige darauf umstellen. |
| **U-1.4a-imp-codes-erweiterung** | `ALLE_IMP_CODES` (`src/shared/import/imp-codes.ts`) wächst um IMP-301…IMP-310 (Stufe 3) und IMP-401…IMP-404 (Stufe 4) — `Befund.schweregrad` wird von der Literalunion `'fehler'` auf `'fehler' \| 'hinweis'` erweitert (additiv, s. U-AP1.3a: IMP-Codes bleiben bewusst eine von `ALLE_FEHLERCODES` getrennte Union). | Keine neue Entscheidung nötig — folgt demselben, bereits in U-AP1.3a begründeten Muster. Hier nur vermerkt, weil `pnpm test` (`import-fehlercodes-stufe1.test.ts`s Vollständigkeitscheck über `ALLE_IMP_CODES`) dadurch automatisch verlangt, dass JEDER neue Code einen `import.json`-Eintrag (`titel`/`beschreibung`/`was_tun`) bekommt — erledigt in diesem PR. | Keine. | — |

---

## 14. Import ausführen, Herkunft, Rücknahme (AP-1.5 PR-A, hueter-Auflage PR #62)

| ID | Befund / Frage | Entscheidung | Konsequenz | Offen |
|---|---|---|---|---|
| **U-1.5-imp502-503** | `56_Import_Vertrag.md` §4 Stufe 5 nennt IMP-502 („Mediendatei nicht kopierbar") und IMP-503 („Schnappschuss vor dem Import fehlgeschlagen") als Ausführungsfehler des ECHTEN Imports — aber `ALLE_IMP_CODES` (`src/shared/import/imp-codes.ts`) endet bei IMP-404: Stufe 5 ist kein Berichtsbefund eines Prüflaufs (die Sondierung ist zu diesem Zeitpunkt bereits erfolgreich durchlaufen), sondern ein Fehlerzustand der Anwendung während der Echtschreibung. | Als eigene `FehlerCode`s umgesetzt (dasselbe Trennungsmuster wie U-AP1.3a): `IMPORT_MEDIUM_NICHT_KOPIERBAR` (IMP-502, geworfen in `src/main/import/medienkopie.ts`) und `IMPORT_SCHNAPPSCHUSS_FEHLGESCHLAGEN` (IMP-503, geworfen in `src/main/befehle/import-ausfuehren.ts`), mit `titel`/`was_tun`-Einträgen in `src/shared/i18n/de/fehler.json` (§7). `ALLE_IMP_CODES` bleibt unverändert. | `test/einheit/i18n-vollstaendig.test.ts` verlangt beide Einträge automatisch — erledigt im selben PR. | Keine. |
| **U-1.5-medium-dateiname** | Der AP-1.5-Auftrag listete `src/main/repositories/medium-repo.ts` nicht unter den zu ändernden Dateien — aber das Ergebnis der Medienkopie (`medienkopie.ts`, Originaldateiname) braucht ein Ziel in der Datenbank, sonst wäre die Abnahme „Mediendateien werden kopiert" nicht nachweisbar. | Ergänzt: die Spalte `medium.dateiname` existiert bereits im ausgelieferten Schema (`docs/schema/0002_kern.sql`, Migration v2, seit AP-0.6 auf `main`) und war bisher ungenutzt (Kopfkommentar von `medium-repo.ts`: „entstehen erst beim Dateikopieren, AP-1.5"). Sie wird jetzt beim Import befüllt — **keine neue Migration nötig**, reine Nutzung einer schon vorhandenen Spalte. | `MediumEinfuegenEin` trägt zusätzlich `dateiname: string \| null`; bestehende Aufrufer/Tests (AP-1.3d, Default `null` ohne Medienauflösung) bleiben unverändert grün. | Keine. |
| **U-1.5-ipc-reconnect** | Nach einer Großimport-Rücknahme (`importZuruecknehmen()` in `src/main/journal/undo.ts`, ADR-019) schließt `undo()` das übergebene `db`-Handle intern (Datei-Wiederherstellungsweg: aktuelle Datei nach `snapshots/ersetzt-<Zeit>.sqlite`, Schnappschuss zurückkopieren) und öffnet es NICHT wieder. Der IPC-Handler `befehl:journal.undo` (`src/main/ipc/registrierung.ts`) reicht dieses jetzt geschlossene Handle danach unverändert an `journalStatusMelden(db)` weiter. | **Bewusst nicht in AP-1.5 PR-A behoben.** Der volle Reconnect (`src/main/projekt/projekt-dienst.ts` müsste seinen globalen `offenesProjekt`-Zustand nach einer Großimport-Rücknahme neu öffnen, analog zu `schnappschussWiederherstellen()`) ist ein Umbau der Projekt-Lebenszyklus-Verdrahtung — kein Teil des im AP-1.5-Auftrag genannten Dateiumfangs, und von keinem der vier geforderten Unit-Tests berührt (die rufen `importAusfuehren()`/`undo()` direkt auf einer eigenen Testverbindung auf, nicht über die IPC-/`registrierung.ts`-Schicht). | Betrifft ausschließlich den echten IPC-Pfad der laufenden Electron-App (Menü „Rückgängig" nach einem Großimport) — die vier AP-1.5-Einheitstests sind davon nicht betroffen und bleiben aussagekräftig. | **Offen — muss vor Nutzererreichbarkeit über einen IPC-/E2E-Nachzug geschlossen werden**, z. B. im Zuge von AP-1.4b/der Import-Ansicht: `befehl:journal.undo` muss nach einer erkannten Großimport-Rücknahme `projektSchliessen()`/`projektOeffnen()` erneut durchlaufen, statt das alte Handle weiterzureichen. **Stand AP-1.34 A2c:** die zurückkopierte Datei wird jetzt sofort migriert (auch ein Schnappschuss <v7, §31 U-1.34-H6) — das Handle-Problem bleibt unverändert bestehen. |

---

## 15. Listenansicht — Atome (AP-1.6 Stufe 2, CLAUDE.md §14)

| ID | Befund / Frage | Entscheidung | Konsequenz | Offen |
|---|---|---|---|---|
| **U-1.6-ladeschimmer-statisch** | §14 Fall 2: Der Atomname `Ladeschimmer` (`71` §2.1, Formen Zeile·Block·Kreis) impliziert eine über die Fläche laufende Animation — aber `71` §1.5 erlaubt ausdrücklich nur fünf bewegte Fälle (Baum ein-/ausklappen, Zoom, Zeitregler, Überlagerung, Seitenschublade) und verbietet wörtlich „einen Ladebalken, der sich bewegt, ohne Fortschritt zu kennen". Ein Lade-Skelett kennt seinen Fortschritt nie — die beiden Regeln widersprechen sich für genau dieses Atom. | `src/renderer/bausteine/ladeschimmer.tsx` ist **statisch**: eine ruhige Platzhalterfläche (`--wz-flaeche-hover`) ohne Animation, Grund im Kopfkommentar von `ladeschimmer.css` vermerkt. Der Name bleibt (Inventarname aus §2.1, keine neue Bezeichnung erfunden), das Verhalten weicht ab. | Erfüllt den „lädt"-Zustand aus S-05, ohne die Bewegungssperrliste zu verletzen. | Keine — reine Designentscheidung, keine Scope-Frage. Bei einer künftigen Designüberarbeitung von §1.5 (z. B. eine sechste erlaubte Bewegung „dezenter Ladeplatzhalter") ließe sich eine echte Schimmer-Animation nachrüsten. |
| **U-1.6-atome-scope** | Die Beispielliste möglicher Atome für Liste/Suche nannte u. a. „Ikonknopf" (`SchaltflaecheSymbol`), „Auswahlfeld", „Leerzustandsblock" und einen Trefferzähler. Gebaut wurden nur die sieben Atome, die S-05 (Konfidenzspalte, Platzhalter-/Privat-Filter, „lädt"-Zustand) unmittelbar braucht: `Text`, `Schaltflaeche`, `Eingabekoerper`, `Umschalter`, `KonfidenzPunkt`, `WiderspruchZeichen`, `Ladeschimmer`. | Bewusst **nicht** gebaut: `SchaltflaecheSymbol`/`Symbol` (kein Symbolsatz ausgeliefert, `71` §6 „Symbolsatz … Phase 0" — ein Icon-Atom bräuchte erfundene Icons, das widerspräche §14 „keine neue visuelle Sprache erfinden"); `Auswahlfeld`/`Leerzustandsblock` (beides **Moleküle** aus `71` §2.2, nicht Atome aus §2.1 — gehören zur Stufe, die die eigentlichen `tabelle.tsx`/`suchfeld.tsx`-Bausteine baut); `Zaehler` (hängt am Suchfeld-Trefferzähler, der noch keinen Konsumenten hat); `Kontrollkaestchen`/`Abzeichen`/`Trennlinie`/`TastenKappe`/`Fortschritt`/`Optionsfeld` (kein S-05-Pflichtbedarf in dieser Stufe). | Keine — nächste AP-1.6-Stufe (Moleküle/Organismen) entscheidet dort neu, mit echten Konsumenten. | Bei Bedarf vor der nächsten Stufe erneut gegen S-05 abgleichen. |

---

## 16. Listenansicht — Moleküle und Datentabelle (AP-1.6 Stufe 3, CLAUDE.md §14)

| ID | Befund / Frage | Entscheidung | Konsequenz | Offen |
|---|---|---|---|---|
| **U-1.6-spalten-datenvertrag** | `72_Screens_und_Flows.md` S-05 nennt als Spalten „Name · Lebensdaten · Geburtsort · Beruf · Konfidenz · Belege · Kinderzahl · benutzerdefinierte Felder". `PersonListeZeile` (`src/shared/schemata/person-liste.ts`, AP-1.6 Stufe 1) trägt aber nur `anzeigename`, `geburt_jahr`/`tod_jahr`, `geburt_ort_name` sowie die Konfidenz-/Widerspruchsfelder — Beruf, Belegzahl, Kinderzahl und benutzerdefinierte Felder gibt es im Abfragevertrag noch nicht. §14 Fall 1 (geplante Funktion ohne Datengrundlage), keine Design-Lücke, sondern eine Datenvertrags-Grenze. | **Gelöst (AP-1.10 PR-A, 19.09.2026):** `PersonListeZeile` trägt jetzt zusätzlich `beruf` (bevorzugte `praedikat='beruf'`-Aussage), `belegzahl` (COUNT `aussage_zitat` über alle Aussagen der Person) und `kinderzahl` (COUNT `elternschaft` über `elternteil_id`) — je eine gruppierte Nebenabfrage in `src/main/abfragen/person-liste.ts`, kein `SELECT` je Person. `datentabelle-spalten.ts` kennt die drei neuen Spalten als wählbar. | Sieben der acht S-05-Spalten sind jetzt im Vertrag; nur "benutzerdefinierte Felder" fehlt noch (eigenes Feldsystem, A-18). | Nicht vorgreifen (§10) — "benutzerdefinierte Felder" folgt mit A-18 (`feld_definition`/`feld_wert`), kein einzelnes `PersonListeZeile`-Feld und kein Scope-Punkt dieser Stufe. |
| **U-1.6-lebensdaten-unschaerfe** | S-05 verlangt explizit eine entworfene Beispielzeile „etwa 1890 – 1961" (Unschärfe-Modifikator sichtbar). `PersonListeZeile.geburt_jahr`/`tod_jahr` sind einfache `number \| null` ohne Modifikator-/Präzisionsinformation. | **Gelöst (AP-1.10 PR-A, 19.09.2026):** `PersonListeZeile` trägt jetzt zusätzlich die volle Datums-Spaltengruppe (`geburt_datum`/`tod_datum: PersonListeDatumsgruppe` — Modifikator/Präzision/Kalender/wert1/wert2/sort) für die bevorzugte `geburtsdatum`/`todesdatum`-Aussage. `src/renderer/bausteine/lebensdaten-anzeige.ts` baut daraus einen `Datumswert` und ruft `formatiere()` (AP-1.1, `src/core/datum/formatierer.ts`) — keine eigene Formatierlogik im Renderer, nur `t(schluessel, werte)` (Namensraum `datum`, jetzt in `src/renderer/i18n/einrichten.ts` registriert). | Die Zelle zeigt jetzt "etwa 1890 – 1961" wie in S-05 entworfen. | Keine offene Abweichung mehr. |
| **U-1.6-sortierspalten-kollaps** | `PersonListeSortierungEnum` kennt vier Sortierschlüssel (`nachname`, `vornamen`, `geburt`, `tod`), S-05 zeigt aber nur zwei sichtbare Spalten dafür (Name, Lebensdaten). Eine 1:1-Zuordnung Spalte↔Sortierschlüssel geht nicht auf. | Jede sichtbare Spalte bekommt genau einen Sortierschlüssel als Vorgabe (Name→`nachname`, Lebensdaten→`geburt`) — die jeweils andere Achse (`vornamen`/`tod`) ist über die Kopfzelle vorerst nicht erreichbar. §14 Fall 2: an das angepasst, was eine einzelne Kopfzelle eindeutig leisten kann. | Sortierung nach Vorname oder Sterbejahr bleibt möglich (der Vertrag trägt es), nur nicht per Klick auf genau diese beiden Spalten in dieser Stufe. | Eine spätere Stufe könnte eine Sortiermenü-Erweiterung (Rechtsklick/Chevron an der Kopfzelle) für die zweite Achse ergänzen — kein Scope-Punkt dieser Stufe. |
| **U-1.6-gestrichelt-dom** | `--wz-rahmen-gestrichelt-muster: 4px 2px` (`71` §1.1) ist laut Kommentar „Muster ist Teil des Tokens" — das liest sich wie eine SVG-`stroke-dasharray`-Spezifikation, aber `Tabellenzeile` ist eine DOM-Zeile. | `tabellenzeile.css` nutzt `border-style: dashed` mit der Rahmenfarbe des Tokens; das exakte 4px/2px-Muster wird nicht nachgebildet (kein `background-image`-Gradienten-Hack, der auf jedem Zoomlevel/Theme anders aussähe als andere gestrichelte Elemente). | Platzhalterzeilen sind sofort als gestrichelt erkennbar (A-17-Anforderung erfüllt), nur die exakte Strichlänge weicht vom Token-Kommentar ab. | Wird relevant, sobald die Baumansicht (Phase 2, SVG-Kanten) dasselbe Token für `stroke-dasharray` verwendet — dann zwei Konsumenten, ein Token, zwei Annäherungen; ggf. zwei Tokens (`--wz-rahmen-gestrichelt-svg`/`-dom`) im Designreview klären. |
| **U-1.6-leerzustand-ohne-symbol** | `71` §2.2 beschreibt `LeerzustandBlock` als „Symbol + Satz + Aktion". Kein Symbolsatz ausgeliefert (§6 Phase-0-Asset, wie schon in §15 für die Atome vermerkt). | `LeerzustandBlock` rendert ohne Symbol — Titel/Text/Aktion tragen die Bedeutung allein, kein erfundenes Icon. | Konsistent mit der Stufe-2-Entscheidung, kein Bruch der visuellen Sprache. | Nachrüstbar, sobald ein Symbolsatz existiert — keine Scope-Frage. |
| **U-1.6-zaehler-nicht-gebaut** | `71` §2.2 listet `Zaehler` als eigenständiges Molekül; der Auftrag nannte es „ggf." (optional). | Nicht als eigenes Molekül gebaut — Suchfeld (`suchfeld_treffer`) formatiert seine Trefferzahl direkt über i18n-Interpolation, ohne ein wiederverwendbares `Zaehler`-Molekül dazwischenzuschalten. | Keine zusätzliche, noch konsumentenlose Komponente (§10 „keine Moleküle, die S-05 nicht braucht"). | Sobald ein zweiter Konsument mit echtem Formatierungsbedarf (z. B. Fußzeile „1.284 Personen") ansteht, lohnt die Extraktion. |
| **U-1.6-filterleiste-vier-filter** | S-05 nennt als Filterleiste „Platzhalter, `privat`, Konfidenzstufe, „hat Widerspruch", Zeitraum, Ort, Strang" — sieben Filter. `PersonListeFilter` (Stufe 1) kennt nur die ersten vier. | **Nachgezogen (AP-1.10 PR-A, 19.09.2026): drei statt Strang.** `PersonListeFilter` trägt jetzt zusätzlich `zeitraumVon`/`zeitraumBis` (Geburtsjahr-Bereich gegen `person_flach.geburt_jahr`) und `ort` (Teilstring gegen `person_flach.geburt_ort_name`, `INSTR()` statt `LIKE`); `Filterleiste` baut sechs der sieben S-05-Filter. **Strang bleibt bewusst zurückgestellt**, nicht nachgezogen: er ist proband-relativ und graphabgeleitet (welche Kante zu welchem "Strang" gehört, hängt vom gewählten Ausgangspunkt im Baum ab) — keine Spalte, die eine flache `PersonListeZeile` ohne Baumkontext tragen kann. Das ist eine Produkt-/Datenmodellfrage, kein reiner Vertrags-Nachzug wie die anderen sechs Filter. | Sechs von sieben S-05-Filtern sind jetzt vertraglich getragen; Strang bleibt eine offene Frage statt einer erfundenen UI ohne Wirkung. | **Offen (Produkt-/Datenmodellfrage):** wie wird "Strang" für eine Personenliste (ohne gewählten Baum-Fokus) überhaupt definiert — relativ zu welchem Proband? Vermutlich erst sinnvoll beantwortbar, sobald eine Baumansicht mit gewähltem Proband existiert (Phase 2) und eine graphabgeleitete Spalte dafür entworfen ist — kein einfaches zusätzliches `PersonListeFilter`-Feld. |

---

## 17. Listenansicht — Verdrahtung (AP-1.6 Stufe 4, CLAUDE.md §14)

| ID | Befund / Frage | Entscheidung | Konsequenz | Offen |
|---|---|---|---|---|
| **U-1.6-suche-ohne-filter-sortierung-seite** | `abfrage:suche` (Stufe 1, `SucheEin`) kennt nur `text`/`grenze` — keine Filter, keine Sortierung, keine Seite. `ListenAnsicht` zeigte Filterleiste und Spaltenkopf-Sortierung anfangs auch während einer aktiven Suche klickbar, aber wirkungslos an (§14 Fall 2: kein Kontrollelement verschwindet mitten in der Bedienung). | **Gelöst (AP-1.10 PR-A, 19.09.2026 — ersetzt die Zwischenlösung vom 18.09.2026):** `abfrage:suche` (`SucheEin`) trägt jetzt `filter`/`sortierung`/`richtung`/`seite`/`proSeite` wie `abfrage:person.liste`; `SucheAus` trägt zusätzlich `gesamt`. `src/main/abfragen/suche.ts` wendet dieselben `filterBedingungen`/`vergleicheZeilen`/`zeilenLaden`/`zeileZuAusgabe` aus `src/main/abfragen/person-liste.ts` auf die per Volltext/Phonetik gefundenen Kandidaten an (`grenze` bleibt das Kandidatenfenster VOR Filter/Sortierung/Seite, kein Seitenzähler). Die sichtbare Deaktivierung (`Filterleiste.gesperrt`, `Datentabelle.sortierungGesperrt`) wurde darum vollständig zurückgebaut. | Filterleiste, Spaltenkopf-Sortierung und Blätterleiste bleiben jetzt auch während einer aktiven Suche durchgehend wirksam und sichtbar — dieselbe Bedienung wie ohne Suchtext, keine Sonderbehandlung mehr. | Keine offene Abweichung mehr. |
| **U-1.6-start-liste-zustand** | `72`/`70` treffen keine Aussage, WIE zwischen Start- und Listenansicht gewechselt wird (kein Router im Scope, CLAUDE.md §10 „kein Mehrbenutzerbetrieb" o. ä. nennt das nicht explizit, aber Phase 1 hat sonst keinen zweiten Bildschirmwechsel). | `App` hält einen einfachen `ProjektInfo \| null`-Zustand und rendert `StartAnsicht`/`ListenAnsicht` bedingt — kein React-Router, keine URL. Die Start-Ansicht meldet ein geöffnetes Projekt nur noch über einen Callback nach oben, statt selbst einen „Projekt offen"-Zustand zu zeigen. | Konsistent mit „kein Router erfinden, wenn ein einfacher Zustand reicht" (Auftragstext AP-1.6 Stufe 4). | Keine — reine Verdrahtungsentscheidung, kein Scope-Punkt. Eine spätere Ansicht (Profil, S-07) kann denselben Musteransatz (Zustand in `App` oder ein spezialisierter Ansichts-Stack) fortführen oder durch einen Router ablösen, sobald mehr als zwei Ansichten existieren. |
| **U-1.6-e2e-fixture** | `test/e2e/ablauf-01-import-und-liste.spec.ts` (Stufe 4) importierte ursprünglich zur Laufzeit eine abgeleitete tmp-Kopie von `fixtures/import/v1/gueltig/beispiel-3-interview.json`: die eingecheckte Originaldatei referenziert eine bereits vorhandene `db:018f2c44-…`-Person und eine nicht mitgelieferte Audiodatei (§2.1, IMP-202/IMP-208) und ist darum in einem frischen Projekt nicht fehlerfrei importierbar — der Einheitstest `test/einheit/import-schreiben-belege.test.ts` braucht aber genau diese `db:`-Referenz und darf die Datei nicht verlieren. | **Gelöst (18.09.2026):** eine eigene, self-contained e2e-Fixture `test/e2e/fixtures/import-erna-und-walter-wruck.json` (+ Platzhalter-Audiodatei `test/e2e/fixtures/audio/erna-wruck-interview.m4a`) eingecheckt — inhaltlich abgeleitet aus `beispiel-3-interview.json` (dieselben zwei Personen, Erna und Walter Wruck), aber mit `db:` → `tmp:erna` aufgelöst und mitgelieferter Audiodatei statt Laufzeit-Ableitung. Bewusst NICHT unter `fixtures/import/v1/gueltig/` (das `import-schema-zod-gleich.test.ts` und `import-fehlercodes-stufe1.test.ts` vollständig durchlaufen — eine zusätzliche Datei dort nähme ungewollt an deren Prüfungen teil), sondern unter `test/e2e/fixtures/`, das keine andere Suite einsammelt. Die eingecheckte Originaldatei bleibt unverändert. **Nachtrag (AP-1.27, 20.09.2026):** diese Begründung ist überholt — `gueltig/` ist seither selbst in `eigenstaendig/` (fehlerfrei gegen ein frisches Projekt) und `braucht-bestand/` (setzt eine vorhandene Person voraus) geteilt, s. `fixtures/import/v1/LIESMICH.md`. Die e2e-Fixture liegt jetzt bewusst UNTER `fixtures/import/v1/gueltig/eigenstaendig/import-erna-und-walter-wruck.json` (Windows-`core.symlinks`-Problem vermieden durch echtes Verschieben statt Symlink, CLAUDE.md §11) statt außerhalb davon — sie nimmt an `import-schema-zod-gleich.test.ts`/`import-fehlercodes-stufe1.test.ts` teil, was inzwischen unproblematisch ist, weil beide Tests keine Datei-für-Datei-Sonderfälle mehr kennen, sondern rekursiv über den ganzen (jetzt geteilten) Korpus laufen. Die Originaldatei `beispiel-3-interview.json` liegt seither unter `gueltig/braucht-bestand/`. | Der e2e-Test liest keine Laufzeit-generierte Datei mehr — die Fixture ist wie jede andere eingecheckte Testfixture versionierbar und diffbar. | Keine. |

---

## 18. Profilseite — Datenweg (AP-1.7 PR-A, hueter-Auflage PR #65)

| ID | Befund / Frage | Entscheidung | Konsequenz | Offen |
|---|---|---|---|---|
| **U-1.7-hatkonkurrierende** | E21 verlangt je Feld zwei separate Zeichen: einen Konfidenz-Indikator MIT Belegzahl UND ein davon UNABHÄNGIGES "es gibt konkurrierende Angaben"-Signal, das auch dann noch erscheinen muss, wenn der Widerspruch bereits aufgelöst ist (eine Aussage `ist_bevorzugt` → `hat_widerspruch=false`). Aus dem PR-A-Vertrag (nur `hat_widerspruch` + kollabierter `wert`) war das nicht ableitbar. | `PersonDetailGrunddatenFeld` trägt zusätzlich **`hatKonkurrierende: boolean`** (`anzahlUnterscheidbareWerte(wertTupel) >= 2`, `src/core/aussage/widerspruch.ts`) — bleibt `true`, auch wenn `hat_widerspruch` durch eine Bevorzugung bereits `false` ist (beispiel-2-Szenario: todesdatum 1961/1958, 1961 bevorzugt → `hat_widerspruch=false`, `hatKonkurrierende=true`). | `hatWiderspruch()` ist jetzt über `anzahlUnterscheidbareWerte()` formuliert (eine Tupelquelle statt zwei). Tests in `test/einheit/widerspruch-ableitung.test.ts`/`test/einheit/abfrage-person-detail.test.ts` erweitert. | Keine offene Abweichung mehr — als kanonisches E21-Signal in PR-A ergänzt (spart eine spätere Vertragsänderung). |
| **U-1.7-core-trigger-gegencheck** | Der volle Core↔Trigger-Gegen-Check (ein Test, der `hatWiderspruch()`/`anzahlUnterscheidbareWerte()` gegen echte SQLite-`REAL`→`TEXT`-Konvertierung des Triggers `abl_aussage_ai` verifiziert, insbesondere Grenzfälle wie `wert_zahl` mit Nachkommastellen) ist nicht Teil von PR-A. | Bewusst vertagt. `tupelSchluessel()` trägt einen Anti-Drift-Kommentar (die `CAST(wert_zahl AS TEXT)`-Form aus `docs/schema/0002_kern.sql:322` wird nicht "korrigiert"). | Keine Auswirkung auf PR-A — die bestehenden Tests decken die Tupelbildung mit `string`/`null`-Werten ab. | **Offen — Folgepunkt für PR-B:** ein Test, der `hatWiderspruch()` gegen den echten Trigger-Output (frische DB, `abl_aussage_ai`) auf identischen Zufalls-/Grenzfall-Aussagen gegenprüft. Von der UI-Stufe (PR-B, dieser Nachtrag) NICHT mitgeliefert — reiner Core/Abfrage-Gegen-Check ohne Oberflächenbezug, bleibt als eigener Folgepunkt offen. |

---

## 19. Profilseite — Oberfläche (AP-1.7 PR-B, §14)

| ID | Befund / Frage | Entscheidung | Konsequenz | Offen |
|---|---|---|---|---|
| **U-1.7-beziehung-platzhalter** | S-07 verlangt „Beziehungen mit Kantentyp … Platzhalter erkennbar" — aber der (gesperrte) PR-A-Vertrag `PersonDetailBeziehung` trug für die VERWANDTE Person kein `ist_platzhalter`-Feld, nur `person_id`/`anzeigename`/`richtung`/`kantentyp`. §14 Fall 1 (geplante Funktion ohne vollen Datengrundriss). | **Gelöst (AP-1.10 PR-B, 19.09.2026):** `PersonDetailBeziehung` trägt jetzt `ist_platzhalter: boolean` (JOIN auf `person.ist_platzhalter` der verwandten Person, `src/main/abfragen/person-detail.ts`, `elternLaden`/`kinderLaden`/`partnerLaden`). `BeziehungenAbschnitt` (`profil-ansicht.tsx`) erkennt Platzhalter jetzt über dieses echte Flag statt über einen leeren `anzeigename` — behebt BEIDE zuvor benannten Fehlrichtungen: ein Platzhalter OHNE Namen ist jetzt korrekt `ist_platzhalter: true`, eine echte, noch namenlose Person bleibt korrekt `ist_platzhalter: false`. Die Kennzeichnung ist außerdem FARBUNABHÄNGIG (A-17): Text `platzhalter_bezeichnung` + gestrichelter Rahmen (`.wz-profil-ansicht__beziehung--platzhalter`, analog `.wz-tabellenzeile--platzhalter`), gedämpfte Farbe nur als zusätzliches, nie einziges Zeichen. | Beide in dieser Zeile zuvor genannten Fehlrichtungen sind behoben; `test/einheit/abfrage-person-detail.test.ts` deckt beide Fälle (Platzhalter-Elternteil, echte namenlose Person) ab. | Keine offene Abweichung mehr. |
| **U-1.7-belegliste-zweistufig** | S-08 beschreibt das Belegdetail „dreistufig": Quelle (Titel/Typ/Archiv/Signatur) → Zitat (Seite/Eintragsnummer/Zugriffsdatum/Digitalisat) → Transkript, plus `unmittelbarkeit` bei mündlichen Quellen. Der PR-A-Vertrag (`PersonDetailBeleg`) lieferte nur `{ quelle: string, zitat: string \| null }` — bereits flach zusammengesetzt, keine separaten Archiv-/Signatur-/Unmittelbarkeits-Felder. §14 Fall 1. | **Gelöst (AP-1.10 PR-B, 19.09.2026):** `PersonDetailBeleg` ist jetzt DREISTUFIG — `quelle: PersonDetailBelegQuelle` (typ/titel/archiv_name/signatur/unmittelbarkeit), `zitat: PersonDetailBelegZitat` (seite/eintragsnummer/zugriffsdatum_wert1/digitalisat_url), `transkript`. `belegeJeAussageLaden` (`src/main/abfragen/person-detail.ts`) joint zusätzlich `archiv` (LEFT JOIN über `quelle.archiv_id`). Die neue `BelegEintrag`-Komponente (`beleg-liste.tsx`, von `Widerspruchsblock` mitbenutzt) zeigt alle drei Stufen; `unmittelbarkeit` nur bei `typ === 'muendlich'`. | Belegdetail entspricht jetzt dem S-08-Entwurf. `test/einheit/abfrage-person-detail.test.ts` prüft die volle dreistufige Struktur gegen das beispiel-2-Szenario. | Keine offene Abweichung mehr. |

---

## 20. Importansichten S-10…S-13 (AP-1.4b, §14)

| ID | Befund / Frage | Entscheidung | Konsequenz | Offen |
|---|---|---|---|---|
| **U-1.4b-bloecke** | `71` §2.3 und der Fließtext in `56` §6.2 sprechen von „sieben" Berichtsblöcken; die nummerierte Liste in `72` §S-11 und der Typ `Trockenlaufbericht` (`src/shared/import/trockenlauf-bericht.ts`) haben **acht** (Gesundheitsdaten als eigener Block). §14 Fall 2 (Design/Doku inkonsistent). | `TrockenlaufBericht` baut **acht** Blöcke — der Typ ist die Wahrheit, kein Codeabweichung. | Der Bericht zeigt einen Block mehr als der Fließtext nennt; deckungsgleich mit dem Datenmodell und `72`. | Fließtext in `56`/`71` bei nächster Doku-Runde auf „acht" ziehen (Wissen-SSOT). Kein Scope-Punkt dieses PRs. |
| **U-1.4b-einstieg** | Der F-03-Flow beginnt bei S-10, aber `72` nennt keine **Einstiegs-Affordanz** in den Import (kein Knopf/Menüpunkt spezifiziert). §14 Fall 1 (geplante Funktion ohne Baustein). | „Importieren …"-Schaltfläche im Kopf der Listenansicht, Assistent als überlagerte Vollseite (Muster `ProfilAnsicht`/T-Vollseite) — keine neue visuelle Sprache. | Der Import ist auffindbar; die Platzierung ist eine Vermutung, kein spezifizierter Entwurf. | Zum Design-Review: gehört der Einstieg zusätzlich in ein „Datei → Importieren"-Menü (`src/main/menue/`)? Nutzerentscheidung. |
| **U-1.4b-laeufe** | `72` §S-10 verlangt „die Liste der letzten Importe mit ihrer Prüfsumme — damit ein doppelter Import auffällt". Es gibt keinen Kanal/keine Abfrage über `import_lauf` (weder in AP-1.4a noch AP-1.5). §14 Fall 1. | S-10 zeigt den Abschnitt „Zuletzt importiert" als **Leerzustand** (`datei_letzte_leer`) statt einer erfundenen Liste. Der Dubletten-Schutz ist ohnehin doppelt vorhanden: der Bericht meldet `bereitsImportiertAm` (Stufe 4, Prüfsummenvergleich) sichtbar in der Zusammenfassung. | Die „letzte Importe"-Liste fehlt visuell; die eigentliche Schutzfunktion (Prüfsummen-Warnung) ist im Bericht vorhanden. | Ein `abfrage:import.laeufe`-Kanal über `import_lauf` (Datei, Prüfsumme, Datum) wäre die saubere Ergänzung — eigener kleiner Datenweg-PR, kein Scope von 1.4b. |
| **U-1.4b-schnappschuss** | S-13 verlangt bei großem Import „Schnappschuss wiederherstellen (mit Hinweis, was verloren geht)". `befehl:import.ausfuehren` gibt die Schnappschuss-ID im `Trockenlaufbericht` **nicht** zurück — die Ansicht kann `befehl:schnappschuss.wiederherstellen` nicht gezielt aufrufen. §14 Fall 2. | Kleiner Import (`ruecknahmeArt === 'undo'`): „Rückgängig" ist voll verdrahtet (`befehl:journal.undo`). Großer Import: statt eines toten Knopfs ein ehrlicher Hinweis, dass die Rücknahme über die Schnappschuss-Liste läuft. | Die direkte Ein-Klick-Rücknahme fehlt beim großen Import. | `befehl:import.ausfuehren` um `snapshot_pfad`/-ID im Ergebnis erweitern (Backend-Änderung) — dann kann S-13 die Wiederherstellung mit Bestätigungsdialog direkt anbieten. |
| **U-1.4b-textexport** | Entscheidung Nutzer (18.09.2026): `alsText()` bleibt unverändert. Der gespeicherte Bericht (Rückweg zum Skill, F-03) enthält damit **nicht** die vollen §5-„Was tun"-Zeilen je Befund — `alsText`/`formatiereBefund` gibt Code/Pfad/Kennung/Zeile aus, nicht die Handlungsanweisung. | So belassen; `berichtSpeichern` reicht den Bericht an `alsText`. AP-1.4b bleibt reine Anzeige + Dialog. | Der gespeicherte Text ist knapper als die Bildschirmanzeige (die die „Was tun"-Texte aus i18n zeigt). | Falls der Skill-Rückweg die vollen „Was tun"-Texte braucht: `alsText` erweitern — offen, ob 1.5 oder ein eigener kleiner PR. |

---

## 21. Symbolsatz + fehlende Atome + Zustandsbibliothek (AP-1.11, CLAUDE.md §14)

**Zuschnitt-Übersteuerung (Nutzer, 18.09.2026):** `57_Phase0_Arbeitspakete.md` §12.4 empfiehlt,
Fundamentpakete klein zu schneiden. AP-1.11 (Symbolsatz + acht fehlende Atome +
Zustandsbibliothek) wurde auf ausdrücklichen Nutzerwunsch **ungeteilt als ein Paket** umgesetzt,
weil es genau ein zusammenhängendes Fundament ist (ein Symbolsatz ohne die Atome, die ihn zeigen,
und eine Zustandsbibliothek ohne vollständige Atomwelle wären für sich unvollständige
Zwischenstände) — kein Scope-Punkt, reine Ablauf-/Schnittentscheidung.

| ID | Befund / Frage | Entscheidung | Konsequenz | Offen |
|---|---|---|---|---|
| **U-1.11-platzhalter-symbol** | Die Fachsymbol-Zuordnung (72 §6/Auftragstext) sah für „Platzhalter" `user-circle-dashed` vor — dieses Symbol existiert bei Phosphor (Tag `v2.0.8`) nicht (geprüft 18.09.2026). §14 Fall 2 (das Design bildet etwas ab, das nicht trägt). | Ersetzt durch `user-circle` (`skripte/symbole-holen.ts`, ADR-027-Nachtrag). Die gestrichelte Kennzeichnung von Platzhalterpersonen trägt bereits `tabellenzeile.css` (A-17, `border-style: dashed`) — das Symbol muss die Gestrichelung nicht zusätzlich abbilden. | Das Platzhalter-Symbol ist ein einfacher Kreis mit Person, nicht gestrichelt — die eigentliche Unterscheidung (gestrichelte Zeile) bleibt unverändert sichtbar. | Keine — reine Asset-Substitution, kein Scope-Punkt. |
| **U-1.11-caret-ergaenzt** | Auftragstext: „ersetze die Unicode-Platzhalter in `datentabelle*`". Tatsächlich gab es dort **keinen** Unicode-Platzhalter — nur `aria-sort` ohne sichtbares Zeichen (§14-Auflage: ehrlich benennen, nicht stillschweigend eine nicht vorhandene Ablösung behaupten). | `Datentabelle` bekommt ein **zusätzliches**, bisher nicht vorhandenes Sortier-Caret (`Symbol name="caret-up"/"caret-down"`, dekorativ) für die aktive Sortierspalte — eine Ergänzung, keine Ersetzung. `sortierCaretSymbol()` (`datentabelle-sortierung.ts`) bleibt von `ariaSortWert()` unabhängig testbar. | Sichtbare Sortierrichtung zusätzlich zu `aria-sort`, kein Verhaltensbruch. | Keine. |
| **U-1.11-tastenkappe-keine-plattformlogik** | `71` §2.1 beschreibt `TastenKappe` als zeige „`⌘K` bzw. `Ctrl+K`, plattformabhängig" — CLAUDE.md §11 verbietet aber jede Plattformabfrage im Renderer außerhalb von `src/main/menue/tastenkuerzel.ts` (das der Renderer wegen der Architekturgrenze (§2) ohnehin nicht importieren darf). | `TastenKappe` bleibt rein präsentational: sie rendert genau die Segmente (`segmente: readonly string[]`), die ihr der Aufrufer bereits plattformgerecht aufgelöst übergibt — keine eigene `navigator.platform`-/`metaKey`-Ermittlung. Es gibt noch keinen echten Bildschirm (Befehlspalette, `70_UX_Konzept.md` §7), der die Plattform tatsächlich auflösen müsste. | Das Atom ist heute ohne Bildschirm voll prüfbar (`test/einheit/tastenkappe.test.tsx`), ohne eine Plattformregel vorwegzunehmen. | Wenn die Befehlspalette (spätere AP) entsteht: dort entscheiden, WIE die Plattform ermittelt wird (z. B. ein schmaler, geprüfter IPC-Kanal analog `abfrage:version`), nicht rückwirkend hier. |
| **U-1.11-zustandsbibliothek-menue-e2e** | `test/e2e/zustandsbibliothek.spec.ts` löst „Entwicklung → Zustandsbibliothek" NICHT über einen echten Klick auf das native Electron-Menü aus (Playwrights `_electron`-API hat dafür kein belastbares Werkzeug), sondern sendet direkt den Kanal, den der Menüpunkt ohnehin nur weiterreicht. | Wie schon `ablauf-01-import-und-liste.spec.ts` (AP-1.6) für den fehlenden Import-Bildschirm: IPC-Bypass statt Menü-Simulation. Das Menü selbst (inkl. `!app.isPackaged`-Sichtbarkeit) prüft separat `test/einheit/menue-entwicklung.test.ts` ohne Electron. | Der e2e-Test beweist die Bildstrecke, nicht den nativen Menüklick als solchen. | Keine — dieselbe, bereits akzeptierte Testlücke wie bei AP-1.6. |

---

## 22. Bildvergleichs-Gate (AP-1.25, CLAUDE.md §13/§14)

`test/e2e/bildvergleich.spec.ts` + `test/golden/bilder/*.png` (geschützter Prüfpfad, ADR-025) +
`playwright.config.ts` (`snapshotPathTemplate`, `expect.toHaveScreenshot`-Schwellen) +
`package.json` (`bilder:erneuern`). Reines `test`/Config-Paket, **keine** `src/`-Änderung — das
Prüfpfad-Gate (`skripte/pruefpfad-pruefen.ts`) feuert deshalb nicht.

| ID | Befund / Frage | Entscheidung | Konsequenz | Offen |
|---|---|---|---|---|
| **U-1.25-schwelle** | `CLAUDE.md` §14/Auftrag verlangt „so streng wie deterministisch möglich" plus einen Rot-Beleg (Ein-Tokenschritt-Mutation `--wz-flaeche-grund` → `--wz-flaeche-vertieft`, #F3F0EA→#F0EDE6, Delta nur (3,3,4) je Kanal). Bei Playwrights **Standard**-`threshold` (0.2, Pixelmatch-Farbempfindlichkeit) blieb diese Mutation **vollständig unentdeckt** — 0 gemeldete Differenzpixel auf der gesamten sichtbaren Fläche, trotz falscher Fläche über die komplette Seite (geprüft 19.09.2026, mehrfach reproduziert). Ursache: 0.2 ist für YIQ-Farbdistanz kalibriert, die zwei benachbarten, absichtlich sehr ähnlichen Tokens liegen darunter. | **`threshold: 0`** (exakte Pixelfarbe, keine Toleranz) statt des Playwright-Standards, kombiniert mit **`maxDiffPixelRatio: 0.002`** als Toleranz für echtes Lauf-zu-Lauf-Rendering-Rauschen. Messwert für dieses Rauschen bei `threshold: 0`: nur die Zustandsbibliothek-Aufnahmen (am textreichsten) zeigen zwischen zwei Läufen derselben Maschine bis zu ~593 von ≈1.097 Mio. Pixeln (≈0,00054) Unterschied; Start-, Liste-, Profil- und Importansicht sind bei `threshold: 0` bitgleich. 0.002 ist der kleinste Zehnerschritt mit Sicherheitsrand über dem gemessenen Rauschen. Mit dieser Kombination wurde dieselbe Mutation erneut geprüft: **klar rot** (z. B. Liste-hell: 987.251 von 1.096.960 Pixeln unterschiedlich, ≈90 %). | Die Schwelle ist strenger als ein naives „nur `maxDiffPixelRatio` justieren" — ohne das `threshold`-Absenken wäre das Gate für genau diese Art Regression (benachbarte Tokens vertauscht) blind gewesen, unabhängig vom gewählten `maxDiffPixelRatio`. | Falls künftige, legitime Layoutänderungen öfter an der Zustandsbibliothek-Rauschgrenze kratzen: `maxDiffPixelRatio` dort motiv-spezifisch justieren (Playwright erlaubt Optionen je `toHaveScreenshot()`-Aufruf) statt global weiter zu lockern. |
| **U-1.25-profil-fixture** | Für das Profil-Motiv sollte laut Ablaufvorlage (`ablauf-02-profil.spec.ts`) `beispiel-2-widersprueche.json`/August Wruck dienen. Aufnahme zeigte dort im Abschnitt „Grunddaten" bei GEBURTSORT eine **rohe UUID** (`01a0b6c2-9f82-…`) statt eines Ortsnamens — sichtbarer, nichtdeterministischer Inhalt (UUID v7, ändert sich mit jedem Import), der eine Bildvergleichs-Baseline unbrauchbar macht. Ursache lokalisiert (nur gelesen, **nicht** behoben — außerhalb des AP-1.25-Umfangs und der harten `src/`-Grenze dieses Auftrags): `aussageWertAnzeige()` (`src/main/abfragen/person-detail.ts:154-159`) fällt für eine Aussage ohne `wert_text`/`datum_wert1`/`wert_zahl` auf `wert_ref_id` zurück und zeigt damit die rohe Fremdschlüssel-UUID an, statt den referenzierten Orts-/Personennamen aufzulösen — anders als die korrekt auflösende `ereignisseLaden()`-Abfrage in derselben Datei (JOIN auf `ortsname`), die für dieselbe Person „Kwidzyn" zeigt. | Profil-Motiv nutzt stattdessen **Walter Wruck** aus der bereits vorhandenen `test/e2e/fixtures/import-erna-und-walter-wruck.json` (dieselbe Fixture wie das Liste-Motiv, kein zusätzlicher Import nötig) — seine einzige Aussage (`beruf: Bergmann`) trägt `wert_text` und durchläuft den fehlerhaften Zweig nicht. Keine Fixture-Datei geändert, kein `src/` angefasst. | Das Profil-Motiv zeigt keinen Widerspruchsfall (S-09) mehr — das bleibt weiterhin durch `ablauf-02-profil.spec.ts` funktional geprüft, unabhängig vom Bildvergleichs-Gate. | **Gelöst (AP-1.10 PR-B, 19.09.2026):** `aussageWertAnzeige()` löst `wert_ref_id` jetzt PRÄDIKATGESTEUERT auf — `geburtsort`/`wohnort` (ortsbezogene Prädikate, `docs/import-vertrag.md` §3: „wert_ref zeigt auf einen Ort oder eine Person") gegen `ort`/`ortsname` (neue `ortsnamenLaden()`, analog `ereignisseLaden()`), jedes andere Prädikat mit `wert_ref_id` gegen `person_flach.anzeigename` (neue `personennamenLaden()`) — nie mehr die rohe UUID. Rot-zuerst-Tests in `test/einheit/abfrage-person-detail.test.ts` (ein ortsbezogener und ein personenbezogener Fall). Das Profil-Motiv bleibt bei Walter Wruck (kein Rückbau nötig, keine Regression in der Baseline). |
| **U-1.25-startansicht-clip** | Die Startansicht zeigt „Zuletzt geöffnet" aus einem echten, plattformweiten `electron-store` (keine isolierte `userData` in Tests) — jeder frühere lokale Lauf und jeder andere e2e-Spec in derselben Sitzung trägt dort einen weiteren Eintrag mit zufälligem `mkdtemp`-Pfad ein, nicht über eine Fixture stillstellbar, ohne `src/` anzufassen (keine Umgebungsvariable/kein Kanal für eine isolierte `userData` in Tests vorhanden). | `toHaveScreenshot()` erhält einen `clip` auf den oberen, vollständig statischen Teil der Ansicht (bis zur y-Position der Überschrift „Zuletzt geöffnet", zur Laufzeit per `boundingBox()` ermittelt) — rein testseitig, keine `src/`-Änderung. | Das Startansicht-Referenzbild deckt „Zuletzt geöffnet" nicht ab. | Eine spätere Testinfrastruktur-Ergänzung (isolierte `userData` je e2e-Lauf, z. B. über eine `ELECTRON_USER_DATA_DIR`-Umgebungsvariable in `src/main/index.ts`) würde den Clip überflüssig machen — eigener, separat zu begründender PR, kein Scope-Punkt von AP-1.25. |
| **U-1.25-windows-artefakt** | AP-1.25 könnte den Windows-Job (`pruefen`) nahelegen, ebenfalls alle fünf Motive als Artefakt abzulegen (statt nur des Hauptfensters). | **Bewusst nicht getan** (Auftragsvorgabe): Windows vergleicht ohnehin nicht pixelgenau (ADR-012, Schriftrasterung), der bestehende Ein-Fenster-Screenshot (`skripte/screenshot-hauptfenster.mjs`) bleibt unverändert das einzige Windows-Artefakt. | CI-Kosten bleiben unverändert; Windows-Rückmeldung bleibt auf ein Fenster begrenzt bis zur Windows-Testrunde (ADR-012, Ende Phase 2). | MVP-/CI-Kosten-Entscheidung, keine offene Frage — nur zur Nachvollziehbarkeit vermerkt. |
| **U-1.25-dev-ci-rasterung** | Selbst mit den deterministischen Rendering-Flags (`--force-color-profile=srgb`, `--disable-lcd-text`, `--font-render-hinting=none`, `--hide-scrollbars`) blieb zwischen einem lokal (macOS-dev) erzeugten Screenshot und einer auf dem CI-macOS-Runner erzeugten Baseline eine Restrasterung von ~1–3 % — bei `threshold: 0` (s. U-1.25-schwelle) reicht das, jeden dev-Lauf gegen eine CI-Baseline nichtdeterministisch rot zu machen, ohne dass sich am Bildschirm etwas geändert hat. | **Baselines werden auf dem CI-macOS-Referenzrunner erzeugt, und der Pixelvergleich läuft NUR dort.** `test/e2e/bildvergleich.spec.ts` überspringt den Vergleich außerhalb dieser Umgebung: `test.skip(process.platform !== 'darwin' \|\| !(process.env.CI \|\| process.env.BILDER_ERNEUERN), …)`. `pnpm bilder:erneuern` setzt `BILDER_ERNEUERN=1` inline, damit die Skip-Bedingung lokal trotzdem durchlässt und `--update-snapshots` greift. | Lokal erzeugte Baselines (z. B. dieser Kettenlauf) sind **Platzhalter zur Vorschau**, keine verbindliche Referenz — der Kettenführer/CI-Lauf ersetzt sie durch CI-gerenderte Aufnahmen. `pnpm test:e2e` überspringt den Bildvergleich lokal vollständig (die übrigen Specs bleiben grün); nur `CI=true` (GitHub Actions, Job „langsame Gates") oder `BILDER_ERNEUERN=1` schalten ihn scharf. | Der dritte, bessere Weg (ein eigener CI-Schritt, der bei Bedarf automatisch neue Referenzrunner-Baselines erzeugt und committet) ist nicht gebaut — das bleibt vorerst ein manueller Schritt (CI-Artefakt „bildvergleich-aufnahmen" bei Fehlschlag herunterladen, `test/golden/bilder/` ersetzen, committen). |
| **U-1.25-liste-profil-isolation** | Liste/Profil liefen ursprünglich in derselben Electron-Instanz wie das Importbericht-Trockenlauf-Motiv (Sondierung, endet mit `ROLLBACK`), NACH diesem, gefolgt vom echten Import über den UI-Assistenten. Beobachtet auf CI (PR #71): dieser geteilte Ablauf lud die Daten dort gelegentlich nicht zuverlässig (Walter Wruck nicht gefunden), während `ablauf-01-import-und-liste.spec.ts` im selben CI-Lauf mit einer eigenen, frischen Instanz + rohem `befehl:import.ausfuehren`-Aufruf zuverlässig grün importiert. | Liste/Profil laufen jetzt in einer **eigenen, isolierten Electron-Instanz mit eigenem Projekt**, die exakt die auf CI bewährte Sequenz aus `ablauf-01-import-und-liste.spec.ts` spiegelt (roher IPC-Aufruf `befehl:import.ausfuehren` statt UI-Assistent, `expect(...).toBeVisible()`-Wartungen statt fester Pausen). Der Importbericht-Trockenlauf bleibt in der ersten, zustandsfreien Instanz, ohne danach einen echten Import in dieser Instanz auszulösen. | Zwei `electron.launch()`-Aufrufe statt einem in `bildvergleich.spec.ts` — Reliabilität vor Geschwindigkeit (Auftragsvorgabe). Lokal deterministisch grün reproduziert (mehrfache Läufe von `pnpm bilder:erneuern`). | Ob dieselbe Isolation auf dem CI-Referenzrunner die Flakiness vollständig behebt, zeigt erst der nächste CI-Lauf — bei erneuter Instabilität dort wäre der nächste Schritt eine noch engere Nachbildung von `ablauf-01` (z. B. dieselbe Fixture-Instanziierung, kein gemeinsamer `tmpdir`-Präfix). |
| **U-1.13-felder-ausser-clip** | AP-1.13 PR-A fügt sieben Eingabefeld-Bausteine (formularfeld/textfeld/langtextfeld/zahlfeld/datumsfeld/konfidenzwaehler/vorschlagskarte) in die Zustandsbibliothek ein; PR-B ergänzt `personenwaehler`, PR-C ergänzt `ortsfeld`. Der 600-px-Bildvergleich-Ausschnitt (`zustandsbibliothek-*-darwin.png`) zeigt keinen der neun Bausteine (sie liegen unterhalb) — alle Baselines blieben bit-identisch. Damit haben die neuen Felder aktuell **keinen Pixel-Bildbeleg**; ihre Korrektheit ruht auf Einheitentests (rendert/Tastatur/gesperrt/kein Farbliteral/Trefferfläche) und dem Nutzerblick an Checkpoint 2. | Wie U-1.28-goldens-clip behandelt: kein Blind-Erneuern, Einheitentests + Checkpoint tragen. | Verstärkt den offenen Punkt aus U-1.28-goldens-clip (Ausschnitt zeigt nur den Seitenanfang). | Offen (eigener Infra-PR): ganzseitige oder motiv-weise Zustandsbibliothek-Aufnahme, damit alle Bausteine einen Bildbeleg bekommen. |
| **U-1.28-goldens-clip** | AP-1.28-Abnahme verlangt: „im Kontaktabzug ist die Pille als Pille erkennbar". Die Zustandsbibliothek-Goldens (`test/golden/bilder/zustandsbibliothek-*`) und der `pnpm bilder`-Kontaktabzug (`skripte/bilder.ts`) nehmen aber nur einen 600-px-Ausschnitt vom Seitenanfang auf — die Abschnitte `Umschalter`/`Kontrollkaestchen`/`Optionsfeld` folgen erst nach 13 weiteren Abschnitten und liegen strukturell außerhalb jedes Kontaktabzugs. Der tatsächliche Bildbeleg für die korrigierte Pille stammt daher aus der **Liste-Filterleiste** (`liste-hell/dunkel-darwin.png`), nicht aus dem in der Abnahme benannten Kontaktabzug; `Kontrollkaestchen`/`Optionsfeld` haben mangels Verwendung außerhalb der Zustandsbibliothek aktuell gar keinen Bildbeleg. | AP-1.28 belegt die Pille über die Liste-Referenzbilder (CI-gerendert), der Trefferflächen-/Maß-Beleg läuft deterministisch über `test/einheit/trefferflaeche.test.ts`. Die Zustandsbibliothek-Goldens wurden bewusst NICHT erneuert (bit-identisch vor/nach, Sektion nicht im Ausschnitt). | Die AP-Abnahmezeile „Kontaktabzug zeigt die Pille" gilt sinngemäß über den Liste-Beleg erfüllt, nicht wörtlich über den Zustandsbibliothek-Kontaktabzug. | Offen: ein Kontaktabzug/Golden, der die drei kleinen Bedienelemente tatsächlich zeigt (z. B. ganzseitige Zustandsbibliothek-Aufnahme oder ein eigenes Motiv je Baustein). Eigener, separat zu begründender PR — kein Scope-Punkt von AP-1.28. |
| **U-1.27-importansicht-pfad** | AP-1.27 verschiebt die Fixture `import-erna-und-walter-wruck.json` nach `fixtures/import/v1/gueltig/eigenstaendig/`. Der Trockenlauf-Bericht im Bildvergleich-Motiv `importansicht-*` zeigt den **absoluten Dateipfad** der gewählten Datei (`Datei: /Users/runner/work/wurzelwerk/wurzelwerk/…`), daher änderte sich das Referenzbild (~9500 px, ratio 0,02). | Referenzbilder `importansicht-hell/dunkel` bewusst erneuert (CI-Runner, referenzbilder.yml), im PR begründet. | **Zwei latente Punkte** dahinter: (1) Die Baseline ist an den CI-Checkout-Pfad gekoppelt (`/Users/runner/work/wurzelwerk/wurzelwerk/…`) — funktioniert nur, weil der CI-Pfad konstant ist; ein anderer Runner-Pfad bräche das Motiv. (2) Der Import-Assistent zeigt einen **rohen absoluten Pfad** — dieselbe UX-Frage wie AP-1.26 („Pfade lesbar, nicht roh"). | Offen (kein AP-1.27-Scope, src-Änderung): Import-Bericht sollte einen lesbaren/relativen Namen statt des absoluten Pfads zeigen; das entkoppelte die Baseline zugleich vom Runner-Pfad. Eigener PR. |
| **U-1.27-worker-restart-cascade** | Belegte Test-Architektur-Fragilität in `test/e2e/bildvergleich.spec.ts`: Der `beforeAll` von „Importansicht — Bericht (Trockenlauf)" setzt voraus, dass der Sibling-Test `test('Projekt anlegen')` zuvor ein Projekt geöffnet ließ. Schlägt IRGENDein früherer Test der Datei fehl (z. B. ein Screenshot bei `threshold:0` unter Last), verwirft Playwright den Worker (`retries:0`, `workers:1`) und startet einen frischen — der Sibling-Test läuft dort NICHT erneut, der `beforeAll` aber schon, in einer **projektlosen** Instanz → 30-s-Timeout auf „Importieren …". So maskiert ein früherer Fehlschlag sich als scheinbarer Klick-Hang (PID-Wechsel an der Fehlergrenze belegt). | Für AP-1.27 nicht nötig zu beheben: sobald `importansicht-hell` gegen die erneuerte Baseline grün ist, entfällt der auslösende Fehlschlag und damit der Worker-Neustart. | Der Klick-Hang ist ein **Folge**symptom, keine Renderer-/Klick-Ursache — `ListenAnsicht`/`app.tsx` sind unauffällig (tief geprüft). | Offen (eigener Test-only-PR, kein src): den `Importansicht`-`beforeAll` von der Sibling-Test-Voraussetzung entkoppeln (eigene Projektanlage) oder die Gruppe `test.describe.serial` fahren, damit ein früher Fehlschlag sauber überspringt statt in einen irreführenden 30-s-Hang zu laufen. |
| **U-1.25-baseline-mitwandern** | AP-1.25 legte die Referenzbilder unter `test/golden/bilder/` an — einem Ordner, den ADR-025 hart schützt (kein gleichzeitiger Vergleich mit `src/`). AP-1.25 selbst rührte `src/` nicht an, daher feuerte das Gate nicht (Notiz oben). Jede **künftige** UI-Änderung an einem bereits eingefrorenen Bildschirm hätte das Gate aber zwangsläufig ausgelöst: `src/`-Änderung + bewusst erneuertes Referenzbild im selben PR sind bei einer laufenden Oberflächen-Kette (`kette-ui.md` §3.3) der Normalfall, kein Ausnahmefall — ein Widerspruch zwischen ADR-025 und dem vorgesehenen Kettenablauf. | **ENTSCHIEDEN (Nutzer, ADR-028, 19.09.2026):** `test/golden/bilder/**` ist vom harten Mischverbot ausgenommen (`skripte/pruefpfad-pruefen.ts`, `BILDVERGLEICH_AUSNAHME`) — Sichtbaselines dürfen mit der UI mitwandern. Kontrolle über hueter-Review je PR + Nutzerblick am Checkpoint statt über das Gate. Layout-/Logik-Goldens (`test/golden/` außerhalb von `bilder/`) und `test/invarianten/` bleiben ausnahmslos gesperrt. | Bildschirm-Pakete dürfen `src/` + erneuertes Referenzbild im selben PR ändern, wie `kette-ui.md` §3.3 es vorsieht. | Keine offene Abweichung mehr — s. ADR-028. |
| **U-1.13-ortsfeld-scope-c** | AP-1.13 PR-C baut das `Ortsfeld` gegen `docs/71_Designsystem.md` §3.2 — der Entwurf zeigt je Vorschlag die VOLLE zeitabhängige Zugehörigkeitskette, politisch/kirchlich getrennt (ASCII-Skizze: „Marienwerder · Kreis Marienwerder · Westpreußen · Preußen bis 1945"). Diese Hierarchie-Pflege (Zugehörigkeitsketten anlegen/ändern, mehrere Namen je Ort mit Zeiträumen verwalten) ist Scope von AP-1.16, nicht von AP-1.13 (CLAUDE.md §10: nicht vorgreifen). | PR-C liefert bewusst NUR: `abfrage:ort.suche` (Tippsuche über jeden — auch historischen — Ortsnamen, liefert je Treffer den zu einem optionalen `jdn` datumsgültigen Namen über `src/core/ort/zeitbezug.ts::gueltigerOrtsname`) + `befehl:ort.anlegen` (EIN Ort + EIN primärer Ortsname, kein `typ`-Zwang). Der `Ortsfeld`-Baustein zeigt je Vorschlag nur diesen einen Namen, keine Zugehörigkeitskette. Zusätzlich bewusst zurückgestellt: die undo-bitgleich-**Generator**-Deckung für `ort.anlegen` (der generische Undo-Mechanismus selbst greift bereits automatisch, s. `test/einheit/befehl-ort.test.ts`; die zusätzliche property-basierte Generator-Abdeckung, wie sie andere `*.anlegen`-Befehle in `test/invarianten/` haben, bleibt AP-1.16 — geschützter Prüfpfad, ADR-025, nicht in derselben Iteration wie der Produktivcode). | `Ortsfeld` ist ab PR-C nutzbar (Suche + einfaches Neuanlegen), zeigt aber noch keine Hierarchie zur Unterscheidung mehrfach vorkommender Ortsnamen — das bleibt bis AP-1.16 auf `anzeigename` (+ still mitgeführtes `typ`, noch ungenutzt) beschränkt. | Offen: AP-1.16 baut die volle Ortsverwaltung (Zugehörigkeitsketten-Pflege, mehrere Namen mit Zeiträumen im UI bearbeiten) und erweitert `Ortsfeld` um die Hierarchiezeile aus §3.2 sowie die Generator-Deckung für `ort.anlegen`/neue `ort.aendern`/`ort.loeschen`-Befehle. |

---

## 23. Prüfhinweise im Bestand (AP-1.8 PR-A, F-07, CLAUDE.md §12.3)

| ID | Befund / Frage | Entscheidung | Konsequenz | Offen |
|---|---|---|---|---|
| **U-1.8-quittung-persistenz** | `57_Phase0_Arbeitspakete.md` „AP-1.8" verlangt: „Ein Hinweis ist abhakbar (‚geprüft, ist korrekt so') und kommt dann nicht wieder". Das braucht Persistenz einer Quittung — eine Datenmodell-Entscheidung, die die Kette anhält (§12.3), nicht in PR-A getroffen. | PR-A liefert bewusst nur die read-only-Anzeige (Anzahl + Liste + Sprung zur Person, `abfrage:pruefhinweise`, `src/main/abfragen/pruefhinweise.ts`). Kein Abhaken, keine neue Tabelle, keine Migration in diesem PR. | Ein Hinweis kommt nach jedem Neuladen der Liste wieder, auch wenn er bereits als „korrekt so" geprüft wurde — nach mehreren Importen wird die Liste zu Rauschen (genau die vom Auftrag benannte Gefahr). | Prüfhinweis-Quittung (Abhaken) — wie persistieren? Neue journalisierte Tabelle `pruefhinweis_quittung` (Migration) vs. `aussage`-Modellierung; stabile Hinweis-Signatur (Person-ID + Regelcode + Datenwerte), damit dieselbe Quittung nach erneutem Import nicht wiederkehrt, bei geänderten Daten aber schon. Datenmodell-Entscheidung, hält die Kette an (§12.3). |

---

## 24. Skill `wurzelwerk-import-vertrag` (AP-1.9, D-11)

Kein Code in `src/`: der Skill ist eine Markdown-Definition unter
`.claude/skills/wurzelwerk-import-vertrag/SKILL.md`, gegengeprüft durch
`test/einheit/skill-import-vertrag.test.ts`.

| ID | Befund / Frage | Entscheidung | Konsequenz | Offen |
|---|---|---|---|---|
| **U-1.9-distribution** | Wie kommt der Skill zum Endnutzer? `.claude/skills/…` ist ein Repository-Artefakt für Claude-Code-Sitzungen in diesem Repo — nicht automatisch etwas, das ein Endnutzer ohne Claude Code/Repo-Zugriff verwenden kann. `57_Phase0_Arbeitspakete.md` „AP-1.9" sagt nur „Claude-Skill", keine Aussage zur Auslieferung. | **Nicht entschieden — bewusst offengehalten, statt geraten.** Drei denkbare Wege: (a) reines Repo-Artefakt, nur für die Entwicklung/den Projektinhaber via Claude Code nutzbar; (b) eigenständig an Endnutzer ausgeliefert (z. B. als Copy-Paste-Prompt-Vorlage, unabhängig von Wurzelwerk selbst); (c) mit der App gebündelt und über eine eigene Oberfläche (Interview-Modus, A-15) angesteuert. `.claude/skills/wurzelwerk-import-vertrag/` ist der **Interimsort**, unabhängig vom Ausgang dieser Frage. | Keine — der Skill funktioniert im jetzigen Ort für den vorgesehenen Zweck (Trockenlauf-Rückweg, AP-1.9-Abnahme). | **Produktentscheidung, hier zu treffen:** Distribution/Laufzeit des Skills (Repo-Artefakt vs. Endnutzer-Auslieferung vs. App-Bündelung). Berührt A-15 (Interview-Modus) und die Frage, ob Wurzelwerk selbst je einen KI-Aufruf macht (bisher: nein, W-Prinzip „lokal, kein Cloud-Zwang"). |
| **U-1.9-manuelle-nachpruefung** | `57_Phase0_Arbeitspakete.md` „AP-1.9" verlangt als (nicht automatisierten) Test: drei echte Gespräche/Papiere je durch den Skill, je Trockenlauf, drei Belege stichprobenartig gegen das Original geprüft — plus einen Gegentest („Name unbekannt" → Platzhalter, kein erfundener Name). Diese Läufe brauchen echtes Nutzermaterial und ein bedientes Trockenlauf-Werkzeug; beides liegt außerhalb dieses Konsistenztest-PRs. | **Terminiert, nicht als erledigt markiert:** Die manuelle Nachprüfung läuft **spätestens bis Checkpoint 1** (`57_Phase0_Arbeitspakete.md`, „Erster Teil von Phase 1 ist fertig, wenn …"). Dieser PR liefert nur die maschinelle Konsistenzprüfung (Frontmatter, zehn Verbote wörtlich, Referenzen, Rückweg/§7.4 genannt) — **nicht** den Nachweis, dass der Skill an echtem Material tatsächlich brauchbare, erfindungsfreie Importdateien erzeugt. | Solange die drei Läufe + der Gegentest nicht protokolliert sind, ist unbewiesen, ob die zehn Verbote im Alltag ausreichen (genau die Frage, die `57_Phase0_Arbeitspakete.md` mit „Ergebnis in `80_Offene_Fragen.md` festhalten, falls sich Regeln als unzureichend erweisen" stellt). | **Offen bis Checkpoint 1:** drei reale Gespräche/Papiere durch den Skill laufen lassen, je Trockenlauf, je drei Belege gegen das Original stichproben; separat der Gegentest mit einer bewussten Namenslücke (muss Platzhalter oder `notizen_unverarbeitet` erzeugen, keinen erfundenen Namen). Ergebnis hier nachtragen — **nicht vorher als erledigt behandeln.** |

---

## 25. Startansicht (AP-1.26, CLAUDE.md §14)

| ID | Befund / Frage | Entscheidung | Konsequenz | Offen |
|---|---|---|---|---|
| **U-1.26-s04-s02-kopfzeile** | Die AP-1.26-Kopfzeile in `57_Phase0_Arbeitspakete.md` nennt „S-01, S-04" als Auftrag. S-04 ist im Design-Export der In-App-Leerzustand in der `T-Shell` (Suchleiste, Tabs Baum/Liste/Karte/Zeitleiste/Interview) — den gibt es im Code noch nicht, ihn zu bauen wäre ein Vorgriff auf Phase 2/3-Ansichten (§10). Der tatsächlich gemeinte zweite Bildschirm (Ordnerwahl-/Pfadvorschau vor dem Anlegen) ist im Design-Export eigentlich S-02, nicht S-04. §14 Fall 2 (Doku/Design-Kopfzeile inkonsistent). | Gebaut wird ausschließlich **S-01** (Startansicht mit inline-Namensfeld + Systemdialog-Ordnerwahl) — S-04 (`T-Shell`-Leerzustand) und S-02 (eigener Ordnerwahl-/Pfadvorschau-Bildschirm) bleiben ungebaut, beide außerhalb des Scopes dieses Pakets. | Die AP-1.26-Abnahme ist über S-01 vollständig erfüllt; S-02/S-04 bleiben offene, spätere Bildschirme. | Die Kopfzeile „S-01, S-04" in `57_Phase0_Arbeitspakete.md` bei nächster Doku-Runde auf „S-01" korrigieren (Wissen-SSOT). Kein Scope-Punkt dieses PRs. |
| **U-1.26-personenzahl** | Das S-01-Mockup zeigt je Zuletzt-Eintrag eine Personenzahl („2 041 Personen"). Das würde bedeuten, für jeden Eintrag der Zuletzt-Liste die zugehörige Projekt-Datenbank zu öffnen und zu zählen — beim bloßen Anzeigen der Startansicht, bevor der Nutzer überhaupt ein Projekt gewählt hat. §14 Fall 1 (Design zeigt mehr, als der vorhandene Datenweg trägt). | **Zurückgestellt.** `abfrage:projekt.zuletzt` öffnet keine fremden Projekt-Datenbanken — die Zuletzt-Zeile zeigt nur Name, Ort und (neu) `existiert`. | Die Zuletzt-Liste ist knapper als das Mockup; kein Datenbankzugriff auf nicht geöffnete Projekte. | Eine spätere Ergänzung müsste entweder eine mitgeführte Personenzahl im Zuletzt-Eintrag selbst cachen (bei jedem `projektSchliessen()`/`projektUebernehmen()` aktualisiert) oder bewusst einen Kurzzugriff auf jede Fremd-DB in Kauf nehmen — Datenmodell-/Kostenentscheidung, kein Scope-Punkt von AP-1.26. |
| **U-1.26-eintrag-entfernen** | Das S-01-Mockup zeigt je Zuletzt-Eintrag eine „entfernen"-Aktion (für einen dauerhaft verschwundenen Ordner). Das bräuchte einen neuen `befehl:`-Kanal (Zuletzt-Liste ist bisher nur `zuletztHinzufuegen`/`zuletztLesen`, kein Entfernen) — eine eigene IPC-Änderung außerhalb des in AP-1.26 vereinbarten Kanalumfangs (zwei Ordnerwahl-Kanäle). §14 Fall 1. | **Zurückgestellt.** Ein Eintrag mit `existiert: false` bleibt stehen (mit „nicht gefunden"-Abzeichen) und lässt sich unverändert weiter anklicken — ein Klick liefert die vertraute `PROJEKT_KEIN_WURZELWERK_ORDNER`-Fehlermeldung. | Ein dauerhaft verschwundenes Projekt lässt sich nicht aus der Liste entfernen, nur erkennen. | Ein `befehl:projekt.zuletztEntfernen`-Kanal (analog `zuletztHinzufuegen`) wäre die naheliegende Ergänzung — eigener kleiner PR, kein Scope von AP-1.26. |
| **U-1.26-ahnen-paketanmutung** | Der Import-Vertrag/das Datenmodell beschreiben `.ahnen` als eigenständiges „Paket" (eine Einheit, kein gewöhnlicher Ordner). Der native `openDirectory`-Dialog zeigt einen `.ahnen`-Ordner aber wie jeden anderen Ordner: navigierbar, mit sichtbarem Inhalt (`baum.sqlite`, Unterordner) — keine macOS-Paket-Anmutung (geschlossenes Symbol, kein Hineinnavigieren per Doppelklick). Das bräuchte eine UTI-Registrierung + `Info.plist`-Eintrag (`CFBundleDocumentTypes`/`UTExportedTypeDeclarations`) — eine Signierungs-/Packaging-Änderung außerhalb von AP-1.26 (Electron-Build-Konfiguration, nicht IPC/Renderer). §14 Fall 2 (das Design/Datenmodell suggeriert eine Anmutung, die der jetzige Systemdialog nicht trägt). | **So belassen.** `dialog.showOpenDialog({ properties: ['openDirectory'] })` bleibt ein gewöhnlicher Ordnerdialog — funktional korrekt (der Nutzer wählt den `.ahnen`-Ordner wie jeden anderen), nur ohne die Paket-Anmutung. | Ein `.ahnen`-Ordner sieht im Finder/Systemdialog aus wie ein normaler Ordner, nicht wie ein Dokument. | Eine UTI-Registrierung (`electron-builder`-`mac`-Konfiguration, `CFBundleDocumentTypes`) wäre die saubere Lösung — Packaging-Änderung, eigener PR, kein Scope von AP-1.26. |

---

## 26. Person bearbeiten — Kernfelder (AP-1.14a, CLAUDE.md §14)

| ID | Befund / Frage | Entscheidung | Konsequenz | Offen |
|---|---|---|---|---|
| **U-1.14a-lebensdaten-schreibweg** | S-20 verlangt ein Datumsfeld für Geburts-/Todesdatum in fünf Zuständen, mit Konfidenz+Beleg. Der Schreibweg dafür ist NICHT eindeutig: (a) `aussage.anlegen` mit `praedikat: 'geburtsdatum'`/`'todesdatum'` — dieselben Prädikate, die der Import-Orchestrator (`src/main/import/schreiben.ts`) UND `abl_person_ai` (`docs/schema/0003_abgeleitet.sql`, Ableitung von `person_flach.geburt_jahr`/`tod_jahr`) bereits als reservierten Ableitungspfad behandeln — ein Kommentar an `EreignisAnlegenEin` (`src/shared/schemata/befehle.ts`) warnt ausdrücklich vor einer Kollision zwischen frei erfassten Aussagen und den importseitig gesetzten. (b) `ereignis.anlegen` mit `typ: 'geburt'`/`'tod'` — fachlich sauberer (ein Lebensereignis ist ein `ereignis`, kein `aussage`-Prädikat), greift aber der Ereignis-Erfassungsmaske aus AP-1.15 vor (§10 „nicht vorgreifen"). Beide Wege sind Migrations-Risiko: eine falsch gewählte Modellierung hier bindet spätere Importe/Abfragen an eine Struktur, die dann nicht mehr änderbar ist (CLAUDE.md §6 „Migrationen laufen nur vorwärts"). | **Zurückgestellt — bewusst NICHT in AP-1.14a gebaut.** Der Grunddaten-Abschnitt (`GrunddatenBearbeitenAbschnitt`) enthält Geschlecht/Notiz/Platzhalter-Kennzeichen+Grund, aber KEIN Lebensdaten-Feld. `test/einheit/profil-bearbeiten-grunddaten.test.tsx` hält die Lücke über ein `it.todo(...)` sichtbar (CLAUDE.md §5 „additive Tests"), statt sie stillschweigend auszulassen. | Geburts-/Todesdatum bleiben in dieser Schreibmaske read-only (weiterhin über `abfrage:person.detail`/Grunddaten-Anzeige im Lesezweig sichtbar, nur nicht editierbar). | **Vor AP-1.14b zu klären (Nutzer/ADR):** `aussage.anlegen` (mit einer Absicherung gegen die Import-Ableitungskollision) vs. `ereignis.anlegen` (mit einem bewussten Teil-Vorgriff auf AP-1.15) — eine Entscheidung, die eine Migration nach sich ziehen kann, darum vor dem Bau, nicht danach. |
| **U-1.14a-struktur-colocation** | Bearbeiten könnte als eigener Ordner `person-bearbeiten/` (eigene Route/eigener Zustand) ODER als Zustand der bestehenden Profilseite entstehen. Ein eigener Ordner wäre die naheliegende Struktur für „eine neue Schreibmaske", widerspräche aber dem Auftrag: eine zweite Ansicht mit eigener Wahrheit (eigener `usePersonDetail`-Abruf, eigener Cache-Stand) wäre GENAU die Art Bug, die durch zwei parallele Wahrheiten entsteht (veraltete Anzeige in einer der beiden Ansichten). | Bearbeiten ist ein `modus: 'lesen' | 'bearbeiten'`-Zustand DERSELBEN `ProfilAnsicht` (`src/renderer/ansichten/profil/profil-ansicht.tsx`), co-lokalisiert unter `src/renderer/ansichten/profil/` (`profil-bearbeiten-namen.tsx`, `profil-bearbeiten-grunddaten.tsx`, `profil-bearbeiten-logik.ts`, `profil-bearbeiten-debounce.ts`). EIN `usePersonDetail`-Abruf speist beide Zweige; Bearbeiten schreibt über die AP-1.12-Befehle, `ereignis:datenGeaendert` invalidiert, der Lesezweig zeigt das Ergebnis. | Kein zweiter Cache-Stand, keine zwei Wahrheiten — aber die Datei `profil-ansicht.tsx` wächst (Kopfzeilen-Umschalter + Verzweigung), statt eine neue, kleinere Datei zu bekommen. | Keine — das ist die vom Auftrag geforderte Struktur, hier zur Nachvollziehbarkeit dokumentiert (§14 „nie stille Abweichung", auch wenn es hier keine Abweichung, sondern eine bewusste Bestätigung der Vorgabe ist). |
| **U-1.14a-fusszeile-bilddiff** | S-20 verlangt eine Fußzeile mit Speicherstatus-Hinweis (statt Speichern-Knopf, ADR-022). Es gibt keinen vorgefertigten „Fußzeile mit Statushinweis"-Baustein — §14 Fall 1 (geplante Funktion ohne fertigen Baustein). | Aus vorhandenen Bausteinen zusammengesetzt: eine `<footer>` (analog der `<header>`-Kopfzeile) mit `aria-live="polite"` und einem `Text`-Atom (`bearbeitungsstatus_hinweis`), nur im `bearbeiten`-Modus sichtbar. | Der Kopfzeilen-Umschalter „Bearbeiten"/„Fertig" UND die neue Fußzeile sind auch im LESE-Modus als potenzielle Layoutänderung wirksam (der Umschalter steht immer da, die Fußzeile nur im Bearbeiten-Modus) — die bestehenden `profil-hell`/`profil-dunkel`-Referenzbilder (`test/golden/bilder/**`, ADR-028) zeigen dadurch sehr wahrscheinlich NICHT mehr bitgleich dieselbe Kopfzeile wie vor diesem PR (ein zusätzlicher sichtbarer Knopf). | **Auf dem CI-Runner geprüft (mehrere `referenzbilder.yml`-Läufe, auch mit ergänztem `toBeVisible('Bearbeiten')`-Wait vor der Aufnahme): `profil-hell`/`profil-dunkel` bleiben bit-identisch zu main — der „Bearbeiten"-Umschalter erscheint im profil-Bildvergleich-Motiv (isolierte Instanz, Walter Wruck) NICHT im erfassten Bild, obwohl der `toBeVisible`-Wait erfüllt ist und der Knopf lokal (1000×600, boundingBox 769,16) sowie in `ablauf-03-person-bearbeiten.spec.ts` erreichbar/klickbar ist. Damit: KEINE Baseline-Änderung nötig; funktionale Deckung des Bearbeiten-Einstiegs über `ablauf-03` (Abnahmetest), aber eine visuelle Bildbeleg-Lücke im profil-Motiv. Offen (eigener Test-Infra-PR): Ursache des Nicht-Erscheinens im Motiv klären (Capture-/Instanz-/Fokus-Eigenheit) und dem Umschalter einen Pixel-Bildbeleg geben. |
| **U-1.14a-s20-katalogfelder** | S-20 listet zusätzlich Zeitraumfeld, Ortsfeld mit Vorschlagsliste, Personenwähler, Mehrfachauswahl mit Chips, Umschalter, Medienwähler, URL-Feld, Kalenderwahl als Teil des vollständigen Feldkatalogs — der AP-1.14a-Auftrag grenzt den Umfang ausdrücklich auf „Kernfelder" ein (Namen + Geschlecht/Notiz/Platzhalter). | **Bewusst ausgelassen** — kein Katalogfeld dieser Liste hat in AP-1.14a ein fachliches Gegenstück (Medienwähler/URL-Feld: keine `person`-Spalte dafür; Zeitraumfeld: nur bei Lebensdaten relevant, s. U-1.14a-lebensdaten-schreibweg). | Die Schreibmaske deckt nur Kernfelder ab — kein vollständiger S-20-Nachbau in diesem Paket. | Spätere Arbeitspakete (Beziehungen bearbeiten, Ereigniserfassung AP-1.15, Medien/Belege) bringen die fehlenden Feldtypen mit ihrem jeweiligen fachlichen Anlass, nicht vorgezogen ohne Datenmodell-Gegenstück (§10 „nicht vorgreifen"). |

---

## 27. Ereignisse mit Rollenbeteiligung (AP-1.15 PR-A, CLAUDE.md §14)

| ID | Befund / Frage | Entscheidung | Konsequenz | Offen |
|---|---|---|---|---|
| **U-1.15-variante-a-zuschnitt** | „Mehrere Beteiligte" ließe sich auf drei Arten abbilden: (a) ein eigener Schreibbefehl `beteiligung.anlegen`, der Beteiligte NACHTRÄGLICH zu einem bestehenden Ereignis ergänzt, (b) `ereignis.aendern` um eine Beteiligten-Verwaltung erweitern, (c) mehrere Beteiligte beim NEU-Anlegen in einem Formular sammeln und in EINEM `ereignis.anlegen`-Aufruf (der bereits eine `beteiligungen`-Liste kennt, AP-1.12) schreiben. (a)/(b) bräuchten je einen weiteren Befehl+Undo/Redo-Pfad+IPC-Kanal für einen Fall (nachträgliches Ergänzen), der außerhalb des freigegebenen Rahmens dieses Arbeitspakets liegt. | **Variante A gebaut:** NUR der neue Befehl `beteiligung.loeschen` (entfernt GENAU eine Teilnahme, das `ereignis` bleibt bestehen) plus das Ereignis-Neu-Formular, das mehrere Beteiligte sammelt und in EINEM `ereignis.anlegen`-Aufruf schreibt. Kein `beteiligung.anlegen`, keine `ereignis.aendern`-Beteiligten-UI, keine neue `abfrage:ereignis.beteiligte` (die bestehende `abfrage:person.detail`-Ereignisliste genügt für diesen Umfang). | Ein NACHTRÄGLICHES Ergänzen von Beteiligten zu einem bereits bestehenden Ereignis ist über die Oberfläche NICHT möglich — nur beim Neu-Anlegen. Ebenso räumt kein Befehl ein Ereignis auf, das nach `beteiligung.loeschen` ganz ohne Beteiligte zurückbleibt (bewusst nicht behandelt, s. Kommentar `BeteiligungLoeschenEin`). | Ein `beteiligung.anlegen` + eine Beteiligten-Verwaltung in `ereignis.aendern` (nachträgliches Ergänzen/Entfernen an bestehenden Ereignissen) ist ein eigenes, späteres Arbeitspaket — kein Scope-Punkt von AP-1.15 PR-A. |
| **U-1.15-generisches-zeuge** | `BeteiligungRolleEnum` (15 Werte: hauptperson/kind/vater/mutter/braeutigam/braut/pate/patenvertreter/trauzeuge/verstorbener/ehepartner/informant/pfarrer/hebamme/dienstherr) kennt kein generisches „Zeuge" — nur den spezifischen `trauzeuge`. Ein Taufzeuge, Testamentszeuge o. ä., der KEINE der bestehenden Rollen trägt, hat keine passende Option. Ein neuer Enum-Wert bräuchte eine Migration (CHECK-Constraint auf `beteiligung.rolle`, `docs/schema/0002_kern.sql`). | **Zurückgestellt.** Das Rollen-Auswahlfeld bleibt auf die 15 bestehenden `BeteiligungRolleEnum`-Werte begrenzt; `informant` ist der bei einer neuen Zeile vorbelegte, semantisch nächstliegende Standardwert (kein familiärer Vorbedeutungswert wie `kind`/`vater`). | Ein allgemeiner Zeuge ohne passende Spezialrolle muss vorerst über `informant` oder `trauzeuge` (je nach Kontext) erfasst werden — keine exakte Passform. | Ein `zeuge`-Enum-Wert (+ Migration, `pnpm fixture:db`, `pnpm trigger`) ist ein eigener, kleiner PR — kein Scope-Punkt von AP-1.15 PR-A. |
| **U-1.15-geburt-tod-grunddaten** (Ergänzung zu §26 U-1.14a-lebensdaten-schreibweg) | Das Ereignis-Neu-Formular lässt ALLE `EreignisTypEnum`-Werte zu, inklusive `geburt`/`tod` — ein Nutzer kann darüber ein Geburts- oder Todesereignis manuell erfassen. `person_flach.geburt_jahr`/`tod_jahr` (§2.7, `abl_person_ai`/`abl_aussage_ai`) werden aber AUSSCHLIESSLICH aus `aussage` (Prädikate `geburtsdatum`/`todesdatum`) abgeleitet, NICHT aus `ereignis`/`beteiligung`. | **So belassen (Variante A trifft keine neue Entscheidung, bestätigt nur den bestehenden Ist-Zustand aus §26).** Ein hier manuell angelegtes `geburt`/`tod`-Ereignis erscheint im Ereignis-Zeitstrahl (lesend UND im Bearbeiten-Abschnitt), füllt aber NICHT die Grunddaten-Anzeige/Sortierung (`person_flach.geburt_jahr` bleibt unverändert). | Ein Nutzer, der eine Geburt/einen Tod über dieses Formular statt über die (noch nicht existierende, s. §26) Lebensdaten-Schreibmaske erfasst, sieht die Angabe im Zeitstrahl, aber NICHT im Profilkopf/in der Listensortierung — ein potenzieller Verwirrungspunkt. | Bleibt an §26 U-1.14a-lebensdaten-schreibweg gebunden: die dortige Entscheidung (`aussage.anlegen` vs. `ereignis.anlegen` als Lebensdaten-Schreibweg, AP-1.14b) klärt auch, ob/wie ein `ereignis`-Eintrag künftig die Grunddaten mitspeist. |
| **U-1.15-formularfeld-a11y-namensberechnung** (Test-Infra-Nebenbefund) | `Formularfeld` umschließt mehrteilige Moleküle (`Datumsfeld`: Eingabefeld + Kalender-Umschaltknopf; ähnlich `Ortsfeld`/`Personenwaehler` mit eigenem `aria-label`) in EINEM `<label>`. Die A11y-Namensberechnung des Browsers sammelt dabei ALLE Textknoten innerhalb des `<label>` ein (nicht nur die sichtbare Beschriftung) — ein `Datumsfeld` unter einem `Formularfeld beschriftung="Datum"` bekommt dadurch den berechneten Namen „Datum Kalender" statt „Datum". `getByLabel('Datum')` (Playwright) löst darum nicht zuverlässig auf. | **Für `ablauf-05-ereignis-erfassen.spec.ts` umschifft:** Struktur-Selektoren (`.wz-datumsfeld input`, `.wz-ortsfeld input`, erstes `select` im Formular-Container) statt `getByLabel`. Kein Eingriff in `Formularfeld`/`Datumsfeld` selbst (Design-/Komponentenvertrag unverändert). | Künftige e2e-Specs, die ein `Datumsfeld`/`Ortsfeld`/`Personenwaehler` innerhalb eines `Formularfeld` ansteuern, brauchen denselben Struktur-Selektor-Umweg — kein `getByLabel` auf der sichtbaren Beschriftung. | Eine saubere Lösung wäre ein explizites `aria-labelledby` von `Datumsfeld`s Eingabefeld auf NUR die `Formularfeld__kopf`-Beschriftung (statt der impliziten `<label>`-Namensberechnung über den gesamten Inhalt) — Komponentenänderung an einem geteilten Baustein, kein Scope-Punkt von AP-1.15 PR-A, eigener kleiner PR. |

---

## 28. Volle Ortsverwaltung — Schreibbefehle (AP-1.16 PR-A, kopflos)

| ID | Befund / Frage | Entscheidung | Konsequenz | Offen |
|---|---|---|---|---|
| **U-1.16-kein-ort-loeschen** | AP-1.16 PR-A liefert die volle Ortshierarchie-Pflege (`ort.aendern`, `ortsname.anlegen/aendern/loeschen`, `ortszugehoerigkeit.anlegen/aendern/loeschen`, `ort-externe-id.anlegen/loeschen`) — bewusst **ohne** `ort.loeschen`. Ein `ort` kann Ziel mehrerer Fremdschlüssel sein (`ortsname.ort_id` CASCADE, `ortszugehoerigkeit.ort_id` CASCADE, `ortszugehoerigkeit.uebergeordnet_id` RESTRICT, `ort_externe_id.ort_id` CASCADE, plus `ereignis.ort_id`/`partnerschaft`-Bezüge SET NULL, `docs/schema/0002_kern.sql` §2.4). Welche Kaskade beim Löschen eines Orts fachlich richtig ist (z. B. ob untergeordnete Orte mitgelöscht werden dürfen oder verwaist zurückbleiben müssen) ist eine Datenmodell-/Produktentscheidung, kein Implementierungsdetail. | **Nicht gebaut** (nicht im Abnahme-Scope von AP-1.16 PR-A). Ein Ort, der nicht mehr gebraucht wird, bleibt vorerst bestehen; es gibt keinen Weg, ihn über die Anwendung zu entfernen. | Ein versehentlich angelegter oder dauerhaft überflüssiger Ort kann nicht gelöscht werden — nur seine Namen/Zugehörigkeiten/externen Kennungen einzeln. | Ein eigener, kleiner PR entscheidet die Kaskadenfrage (vermutlich RESTRICT bei bestehenden `ortszugehoerigkeit.uebergeordnet_id`-Verweisen — ein Ort mit Untergeordneten wird nicht gelöscht, bevor die Kette umgehängt ist) und baut `ort.loeschen` danach. |
| **U-1.16-anschluss-ortsfeld-scope-c** | §21 `U-1.13-ortsfeld-scope-c` verwies die volle Ortsverwaltung (Zugehörigkeitsketten-Pflege, mehrere Namen mit Zeiträumen, `ort.aendern`/neue Befehle) auf AP-1.16. | **PR-A liefert genau das — aber nur den Hauptprozess/Befehlsbus-Teil.** Alle sieben Befehle (`ort.aendern`, `ortsname.*`, `ortszugehoerigkeit.*`, `ort-externe-id.*`) sind über `befehl:*`-IPC-Kanäle erreichbar, mit Undo/Redo (generischer Journal-Mechanismus) und Zyklusschutz (`src/core/ort/zyklus.ts`, je `art` getrennt). Es gibt **noch keine Oberfläche**, die sie aufruft — `Ortsfeld` zeigt weiterhin nur `anzeigename`, keine Hierarchiezeile aus `docs/71_Designsystem.md` §3.2. | Die volle Ortsverwaltung ist ab PR-A fachlich vollständig, aber für Nutzer unsichtbar — nur über `test/einheit/befehl-ort*.test.ts` und (später) die Renderer-Oberfläche erreichbar. | **PR-B** (geschützter Prüfpfad, ADR-025): die zusätzliche property-basierte Undo-bitgleich-Generator-Deckung (`test/invarianten/`) für alle sieben Befehle, analog den bestehenden `*.anlegen`-Befehlen. **PR-C**: die Oberfläche (`ort.detail`-Ansicht, `Ortsfeld`-Hierarchiezeile, Orte-Pflege-Ansicht), die diese Befehle tatsächlich aufruft. |
| **U-1.16-oberflaeche-pr-c** | PR-C baut die Oberfläche aus §21/PR-A nach: neue Leseabfrage `abfrage:ort.detail`, die `Ortsfeld`-Hierarchiezeile (§3.2) und die Orte-Pflege-Ansicht. Vier Design-/Scope-Entscheidungen dabei, keine davon in `71`/`72` vorgezeichnet (CLAUDE.md §14). | 1) **`Ortsfeld` zeigt nur die POLITISCHE Kette** je Vorschlag (`OrtTreffer.politischeKette`, `src/core/ort/zeitbezug.ts::hierarchieZuDatum`); die KIRCHLICHE Kette bleibt der Orte-Pflege-Ansicht (`abfrage:ort.detail`) vorbehalten — der §3.2-Mock zeigt ohnehin nur eine Kette je Vorschlagszeile. 2) Die Kette erscheint NUR, wenn ein `jdn` übergeben wird (das Ereignis-Neu-Formular leitet es aus dem Ereignis-Datumstext ab, `ereignisEntwurfJdn()`) — ohne Datum gibt es keinen eindeutigen Gültigkeitszeitpunkt. 3) **Kein globaler Orte-Screen**: der Einstieg in die Pflege-Ansicht ist ein kontextueller „Ort bearbeiten"-Link direkt am `Ortsfeld`, sobald ein Ort gewählt ist (`profil-bearbeiten-ereignisse.tsx`) — der globale Header-„Orte"-Eintrag bleibt **S-35, Phase 2/3** (C-23, Orts-Verwaltungsansicht mit Dublettenerkennung/-Merge). 4) **Zwei §14-Anpassungen in der Pflege-Ansicht**: `istBevorzugt` nutzt ein `Auswahlfeld` (ja/nein) statt eines noch fehlenden `Checkbox`-Bausteins; `gueltigVon`/`gueltigBis` sind einfache Textfelder statt des vollen `Datumsfeld`-Kalenderumschalters — `parse()` rechnet `sortVon`/`sortBis` ohnehin immer gregorianisch, unabhängig von der gewählten Kalenderanzeige (s. Kopfkommentar `src/core/datum/parser.ts`), eine Kalenderwahl-Chrome dafür wäre Staffage ohne Wirkung. | Die volle Ortsverwaltung ist jetzt vollständig nutzbar, aber NUR über diesen einen Einstiegspunkt erreichbar — ein Ort ohne aktuell offenes Ereignis-Formular (z. B. ein importierter, nie in einem Ereignis verwendeter Ort) lässt sich vorerst nicht pflegen. | Der globale Orte-Screen (S-35) bleibt offen — er würde jeden Ort unabhängig von einem Ereigniskontext erreichbar machen UND die C-23-Dublettenerkennung/-Merge liefern, beides bewusst nicht Teil von AP-1.16. Ein eigener `Checkbox`-Baustein (§14 Fall 1) ist ebenfalls offen — sobald er existiert, kann `istBevorzugt` darauf umgestellt werden. |

---

## 29. Quellen, Zitate, Archive, Negativbefunde (AP-1.17, kopflos + §14)

**Migrationsfrei:** `archiv`, `quelle`, `zitat`, `negativbefund` existieren seit Schema v1, sind journalisiert, Trigger generiert — AP-1.17 baut nur Befehle + Oberfläche. Sub-PRs: A1 Archiv (#96), A2 Quelle (#97), A3 Zitat, A4 Negativbefund, C Oberfläche, B undo-bitgleich (geschützter Prüfpfad).

| ID | Befund / Frage | Entscheidung | Konsequenz | Offen |
|---|---|---|---|---|
| **U-1.17-kein-quelle-archiv-loeschen** | Wie bei `ort.loeschen` (§28) ist die Kaskade beim Löschen einer `quelle` (→ `zitat` CASCADE → `aussage_zitat`) bzw. eines `archiv` (→ `quelle.archiv_id`) eine Datenmodell-/Produktentscheidung. | **`quelle.loeschen`/`archiv.loeschen` nicht gebaut** (nicht im Abnahme-Scope). `zitat.loeschen`/`negativbefund.loeschen` sind unkritisch und werden gebaut. | Eine überflüssige Quelle/ein Archiv bleibt vorerst bestehen; nur einzelne Zitate/Negativbefunde entfernbar. | Ein eigener kleiner PR entscheidet die Kaskadenfrage und baut `quelle.loeschen`/`archiv.loeschen` — gemeinsam mit `ort.loeschen` (§28). |
| **U-1.17-fk-validierung-quelle** | `quelle.anlegen/aendern` prüft nur `archivId`-Existenz im Handler (→ `NICHT_GEFUNDEN_ARCHIV` mit Handlungsanweisung). `informantPersonId`/`audioMediumId` (beide FK `ON DELETE SET NULL`) werden NICHT vorab geprüft — eine falsche Referenz landet als generisches `DATENBANK_FREMDSCHLUESSEL` an der IPC-Hülle (hueter PR #97, Punkt B). Kein Korrektheits-Bug (Integrität bleibt DB-seitig erzwungen, Transaktion rollt zurück), aber eine UX-Inkonsistenz: falscher Informant erzeugt eine unspezifischere Meldung als falsches Archiv. | **Vorerst nur `archivId` validiert** — Archiv ist der primäre Anhänge-Workflow mit eigenem Picker (PR-C). Die im Auftrag genannte Begründung „weil ON DELETE SET NULL" trägt nicht (archiv_id ist ebenfalls SET NULL und wird trotzdem geprüft) — der echte Grund ist der Workflow-Vorrang. | Bei falschem `informantPersonId`/`audioMediumId` sieht der Nutzer eine generische Fremdschlüssel-Meldung ohne spezifische Handlungsanweisung. In der Praxis kaum auslösbar, solange die Oberfläche (PR-C) nur gültige IDs aus Pickern liefert. | Bei Bau des mündlich-Block-Pickers in PR-C entweder alle drei FKs für konsistente Meldungen prüfen (`NICHT_GEFUNDEN_PERSON`/`NICHT_GEFUNDEN_MEDIUM`) oder bewusst beim Workflow-Vorrang bleiben. |
| **U-1.17-oberflaeche-pr-c1** | PR-C1 baut die Quelle/Zitat/Archiv-Pflege-Ansicht (`quelle-bearbeiten.tsx`) aus §21/PR-A1..A3 nach. Fünf Design-/Scope-Entscheidungen dabei, keine in `71`/`72` vorgezeichnet (CLAUDE.md §14). | 1) **Kein globaler Quellen-Screen**: der Einstieg ist ein kontextueller „Quelle bearbeiten"/„Quelle anlegen"-Link am Belegapparat des Profils (`beleg-liste.tsx`, `BelegEintrag`/`BelegListe`) — analog zum „Ort bearbeiten"-Link (§28/AP-1.16 PR-C). Die eigenständige S-23-Baumansicht (globaler „Quellen"-Screen mit Dublettenerkennung) bleibt **S-35, Phase 2/3, C-23**. 2) **`PersonDetailBelegQuelle` um `id` ergänzt** (`person-detail.ts`, `src/main/abfragen/person-detail.ts`) — ohne die Quellen-ID hätte der Belegapparat gar nicht wissen können, WELCHE Quelle der „Quelle bearbeiten"-Link öffnen soll; additiv, kein Vertragsbruch (Test angepasst). 3) **`QuelleDetailKopf` um `informant_anzeigename`, `QuelleDetailZitat` um `zugriffsdatum_*`/`medium_id` ergänzt** — reine Durchreiche-Werte (kein eigenes Bearbeitungsfeld dafür), ohne die hätte ein Speichern der sichtbaren Felder (`quelle.aendern`/`zitat.aendern` patchen ALLE editierbaren Spalten, kein Teil-Patch) eine vorhandene Angabe stillschweigend gelöscht — dieselbe Gefahr, die `OrtsnameEntwurfWerte.sprache`/`.originalText` bereits vermeidet. 4) **Kein `audioMediumId`-Feld im mündlich-Block**: es gibt noch keinen Medien-Baustein/-Kanal im Projekt (§14 Fall 1, Baustein fehlt tragend) — bewusst weggelassen statt vorgetäuscht. 5) **`Archivfeld` als DRITTE Instanz** des Personenwähler/Ortsfeld-Musters (Eingabekörper + Vorschlagsliste + feste „… anlegen"-Schlusszeile) — kein neuer visueller Baustein. | Eine Quelle ohne aktuell offenen Beleg (z. B. importiert, aber noch keiner Aussage zugeordnet) ist über den „Quelle anlegen"-Link zwar anlegbar, aber eine NEU angelegte Quelle wird noch NICHT automatisch mit der auslösenden Aussage verknüpft — der Schreibweg `aussage_zitat.anlegen` existiert nicht. Ein `audioMediumId` lässt sich vorerst nicht über die Oberfläche setzen/ändern (nur Altbestand aus dem Import bleibt über den Durchreiche-Mechanismus erhalten). | Der globale Quellen-Screen (S-35) und `aussage_zitat.anlegen` (neue Quelle mit einer Aussage verknüpfen) bleiben offen. Ein Medien-Baustein/-Kanal (§14 Fall 1) ebenfalls offen — sobald er existiert, ergänzt `audioMediumId` den mündlich-Block. |
| **U-1.17-oberflaeche-pr-c2** | PR-C2 baut den Negativbefund-Abschnitt des Profils (`negativbefund-abschnitt.tsx`) aus PR-A4 nach. Zwei Design-/Scope-Entscheidungen dabei, keine in `71`/`72` vorgezeichnet (CLAUDE.md §14). | 1) **Kein Quellen-Auswahlfeld im „Negativbefund hinzufügen"-Formular**: es gibt (anders als `archiv.suche`/`ort.suche`) noch KEINEN Such-/Auswahl-Kanal für `quelle` — nur `abfrage:quelle.detail` (Einzelabruf über eine bereits bekannte ID). Ein Auswahlmuster nachzubauen hätte einen neuen Backend-Abfrage-Kanal gebraucht, außerhalb des UI-Auftrags dieses PRs (§14 Fall 1, Baustein/Kanal fehlt tragend) — bewusst weggelassen statt vorgetäuscht. Ein beim Import gesetztes `quelle_id` einer bestehenden Zeile wird beim Bearbeiten trotzdem unverändert durchgereicht (`negativbefund-abschnitt-logik.ts`, Muster `ZitatVerborgeneFelder`) und — falls gesetzt — mit Titel angezeigt (`abfrage:quelle.detail`-Einzelabruf je Zeile). 2) **Kein globaler Negativbefund-Screen, aber auch KEIN kontextueller Link wie „Quelle bearbeiten"**: `negativbefund.gesuchte_person_id` ist NOT NULL und personengebunden, aber an KEINE einzelne Aussage/kein einzelnes Grunddaten-Feld — der Abschnitt sitzt darum als fester, immer sichtbarer Teil direkt in `ProfilInhalt` (`profil-ansicht.tsx`, am Fuß nach „Notiz"), unabhängig vom Lese-/Bearbeiten-Umschalter der übrigen Profilseite (dieselbe sofortige Editierbarkeit wie die Quelle-Pflege-Ansicht, nur ohne eigene Seitenschublade). | Ein bestehendes `quelle_id` lässt sich über diese Oberfläche nicht ändern/entfernen, nur anzeigen — nur ein neu (importseitig oder künftig) gesetztes bleibt erhalten. Der Negativbefund-Abschnitt erscheint auf JEDER Profilseite, auch bei Personen ohne jeden Negativbefund (Leerzustand + festes „hinzufügen"-Formular) — anders als die übrigen, adaptiv ausblendenden Profilabschnitte. | Ein `abfrage:quelle.suche`-Kanal (analog `abfrage:archiv.suche`) ist die naheliegende Ergänzung, sobald ein Quellen-Auswahlfeld gebraucht wird (auch für PR-C1s „Zitat hinzufügen", falls dort später gewünscht) — eigener kleiner PR, kein Scope-Punkt von AP-1.17 PR-C2. |

---

## 30. Namensmodell zerlegen (AP-1.33, Migration 0006, CLAUDE.md §12/§13/§14)

| ID | Befund / Frage | Entscheidung | Konsequenz | Offen |
|---|---|---|---|---|
| **U-1.33-granulare-befehle** | Der AP-Umfang nennt Befehle `namensform.*`/`namensteil.*`. AP-1.33 liefert stattdessen eine flache `name.*`-Befehlsbrücke auf dem zerlegten Modell. Grund: die `abl_name_part_*`-Trigger pflegen die `suche_fts`-Normalform nur so weit, wie die von der Brücke ausgelösten Operationen es verlangen; granulares Einzelteil-Editieren (add/remove/reorder Vorname, Rufname setzen) braucht darüber hinausgehende inkrementelle FTS-Pflege, die ohne die auslösende UI blind gebaut würde. | **Granulare Befehle + die Vervollständigung der inkrementellen FTS-Pflege wandern nach AP-1.30** (Nutzer, diese Session). Das Modell ist bereits zerlegt (der eigentliche Zweck des harten Schnitts) — kein Doppelbau am Modell. Neuer Befehl `hauptname.wechseln` existiert bereits. | Bis AP-1.30 wird über die flache Brücke geschrieben (eine Form + zerlegte Teile je Schreibvorgang). Der Namen-Reiter (S-20, AP-1.30) baut die granulare Bearbeitung auf dem fertigen Modell. | AP-1.30 baut `namensform.*`/`namensteil.*` und schließt die inkrementelle FTS-Normalform-Pflege für Einzelteil-Änderungen ab. |
| **U-1.33-ride-along-pruefpfad** | Der harte `name→name_form/name_part`-Umzug zwang drei schema-abgeleitete Eingabe-/Fixture-Bauer im (indirekt) geschützten Prüfpfad zur Mitänderung: `test/invarianten/_journal-minimalzeilen.ts`, `test/invarianten/_modell-abgeleitet.ts`, `test/hilfsmittel/fixture-bauen.ts` — ADR-025 sperrt geschützten Pfad + Produktivcode im selben PR. | **ADR-030:** solche Bauer (keine Zusicherung) sind *schema-bedingt* geschützt (wandern nur mit Migration), nicht „immer" — eng begrenzte Ausnahme (exakte Pfade) in `pruefpfad-pruefen.ts`, analog ADR-028. Keine Zusicherung wurde berührt; die zwei Invarianten `undo-bitgleich`/`abgeleitet-gleich` fingen dabei zwei echte Produktivbugs, behoben in Produktivcode. | Der geschützte Ride-along ist im PR-A enthalten (adversariales hueter-Review je PR statt Folge-PR), begrenzt auf die drei benannten Dateien. | Ein künftiges Paket, das einen Schreibbefehl umbenennt/entfernt und dadurch `_befehlsfolge-generator.ts` ändern muss, ist ein eigener Fall (ADR-030 gibt ihn nicht frei). |
| **U-1.33-abl-block-0006** | Die generierten `abl_*`-Trigger lagen eingebettet in der eingefrorenen Migration `0003`; das Quellmodell der Projektion (`name`) ändert sich. | **ADR-029:** der generierte `abl`-Triggerblock wandert in die jeweils jüngste Migration (hier 0006). `0003` bleibt byte-identisch (Prüfsumme). | Künftige projektions-berührende Migrationen legen eine Marker-Region an und setzen `ZIEL_DATEI` auf sich. | — |
| **U-1.33-invariante-genau-ein-hauptname** | „Genau ein Hauptname je Person" ist DB-erzwungen (partieller UNIQUE-Index + Constraint-Trigger) und im Migrationstest strukturell + per Gegenprobe gedeckt. Eine eigenständige Korpus-weite Invariante fehlt noch. | **Folge-PR (PR-B, geschützter Prüfpfad):** neue Invariante „genau ein Hauptname je Person" über den Fixture-Korpus + Anzeigename-Rückfallkette als Property — getrennt von PR-A (ADR-025). | **Gelöst (PR #109, 24.09.2026):** `test/invarianten/genau-ein-hauptname.test.ts` (Korpus + jeder Schritt/Undo einer Befehlsfolge, Generator um `hauptname.wechseln`/`nameWeitereFormAnlegen`), `test/invarianten/anzeigename-rueckfallkette.test.ts`. Drei vom hueter gefundene überlebende Mutationen (Text leer, Nachname/Vorname vertauscht, Nachrücken „höchste id") mit dem Folge-PR `ap/1.33c-folgepunkte` geschlossen. | — |
| **U-1.33-anzeigename-unbenutzt** | `anzeigenameFuer`/`sortierName` (`src/core/name/anzeigename.ts`) werden im Produktivcode **nirgends** aufgerufen, nur in Tests. AP-1.33 verlangt „einmal implementiert, von Liste, Karte, Suche und Export benutzt — nicht zweimal". `person_flach.anzeigename`/`sortier_*` entstehen weiter in der SQL-Projektion (`abl_name_*`-Trigger). Die Property sichert damit die Kernfunktion, nicht das, was Liste/Suche heute zeigen. | **Offen — Owner-Entscheidung (CLAUDE.md §12):** (a) Leser auf `anzeigenameFuer` umstellen (die Projektion bleibt für Sortierung/FTS, der sichtbare Text kommt aus dem Kern), oder (b) SQL-Projektion bleibt die Wahrheit und eine Gleichheits-Invariante Kern ↔ `person_flach` (geschützter Prüfpfad) verhindert das Auseinanderlaufen, oder (c) Kernfunktion entfernen. | Bis zur Entscheidung gibt es zwei Implementierungen derselben Regel, und sie **weichen schon heute ab**: die SQL-Projektion (`0006_namensformen.sql` §7, `abl_*`) bildet `anzeigename` nur aus Vornamen + Nachname der bevorzugten Form — ohne Wunschsprache, ohne Umschrift-Stufe, ohne Titel/Präfix/Zusatz („Florian von Gutnoff" zeigt die Liste als „Florian Gutnoff", der Kern korrekt). Eine Gleichheits-Invariante nach (b) wäre darum sofort rot; (b) setzt voraus, dass zuerst entschieden wird, welche Seite richtig ist. | Spätestens vor AP-1.30 (Namen-Reiter mit Vorschau/Rückfallkette) entscheiden. |
| **U-1.33-lateinische-umschrift** | Design/Doku nennen die zweite Stufe „lateinische Umschrift"; `anzeigenameFuer` wertet dafür nur `umschrift_von IS NOT NULL` aus, nicht `schrift = 'latn'`. Eine Umschrift in eine nicht-lateinische Schrift würde ebenso gewählt. | **Offen:** Reicht `umschrift_von` (jede Umschrift ist per Konvention lateinisch, ISO 9/DIN 1460), oder soll die Stufe zusätzlich `schrift = 'latn'` verlangen? | Heute nur theoretisch: alle Umschriftnormen (`iso9`/`din1460`) zielen auf Latein, `manuell` kann aber alles sein. | Mit U-1.33-anzeigename-unbenutzt entscheiden. |
| **U-1.33-vatersname-anzeige** | `rekonstruiereFlach` (`src/core/name/zerlegung.ts`) und damit der Anzeigetext von `anzeigenameFuer` übergehen `name_part(art='vatersname')` vollständig — „Iwan Petrowitsch Iwanow" erscheint als „Iwan Iwanow". | **Offen:** gewollt (Vatersname nur im Detail, nicht in der Kurzanzeige) oder Lücke? Der Text-Property-Test (`anzeigename-rueckfallkette.test.ts`) nimmt `vatersname` bewusst nicht auf, um das nicht stillschweigend festzuschreiben. | Betrifft den kyrillisch-polnischen Korpus und jeden importierten ostslawischen Namen. | Mit AP-1.30 (Namen-Reiter) klären; bei „Lücke" Produktivfix mit rotem Test zuerst. |

---

## 31. Querschnitt des Editors (AP-1.34, Migration 0007, CLAUDE.md §12/§13)

Nutzerentscheidungen vom 24.09.2026 (E5/E6/E7/E12 ausdrücklich, übrige als Empfehlung angenommen). Kennung und Offene Punkte: Vorgaben §2.2/§5.5 (keine A-ID); Textanker: B-01; Plausibilität: F-07.

| ID | Befund / Frage | Entscheidung | Konsequenz | Offen |
|---|---|---|---|---|
| **U-1.34-E1-kennung-format** | Speicherform und Anzeige der Personen-Kennung. | `person.kennung INTEGER`; Anzeige im Kern `kennungAnzeige(n)` → `P-0142` (4-stellig aufgefüllt, ab 10000 länger). | Kein Text in der DB, Format ist reine Anzeige. NULL → „–“ (E13); eine Zahl < 1 oder eine Nicht-Ganzzahl wirft `RangeError` (aus der DB nicht erreichbar: CHECK `kennung >= 1`, STRICT, `person.detail` prüft beim Lesen) — ein stilles „–“ würde sie mit „keine Kennung vergeben“ verwechseln. | Umgesetzt C1b: `src/core/person/kennung.ts` (`kennungAnzeige`), Test `test/einheit/kennung-anzeige.test.ts`. |
| **U-1.34-E2-nachtrag** | Reihenfolge des Nachtrags für den Bestand. | Lückenlos 1..n in `ORDER BY id` (UUID v7 ≈ Anlagereihenfolge). | 0007 nummeriert per `ROW_NUMBER()`; Zähler danach n+1. R4: 20.000 Personen 6→7 in ≈ 90 ms (lokal, macOS). | — |
| **U-1.34-E3-feld** | Wertliste von `aussage_zitat.feld`. | Nullable, NULL = ganze Aussage; kein DB-CHECK, Wertliste als Zod-Enum im Code. | Neue Feldwerte ohne Migration. | Umgesetzt C1b: `BelegFeldEnum` + `BELEG_FELDER_JE_SUBJEKT` in `src/shared/schemata/aussage-zitat.ts` (Liste s. U-1.34-F1). |
| **U-1.34-E4-anker-invalidieren** | Was passiert mit einem Textanker, wenn sich das Transkript ändert? | Anker bleibt, wenn der Ausschnitt `[von, bis)` gleich ist, sonst beide NULL — in derselben Transaktion. | DB-CHECK „beide oder keine", „bis > von", „von ≥ 0" (0007). Umsetzung wörtlich: Anker bleibt genau dann, wenn er im alten Transkript gültig war, das neue Transkript nicht NULL ist, `bis ≤ neu.length` und `neu.slice(von, bis) === alt.slice(von, bis)` (Position UND Text, kein Suchen/Verschieben); sonst `textanker_von = textanker_bis = NULL`, `feld` bleibt, `geaendert_am` neu. Einheit UTF-16-Codeeinheiten, halboffen. Weggelassenes Transkript → alle Anker NULL. Zusätzlich gilt F4 auch für den verbleibenden Anker (eine Grenze, die im neuen Transkript ein Ersatzpaar teilte, entwertet ihn — nur mit einsamen Surrogaten erreichbar). | Umgesetzt C1a: `src/core/beleg/textanker.ts` (`ankerPruefen`/`ausschnitt`/`ankerBleibt`), `src/main/befehle/zitat-aendern.ts` + `aussage-repo.ts` (`ankerJeZitatLesen`, `ankerAufheben`); Tests `test/einheit/textanker.test.ts` (K1–K3), `befehl-aussage-zitat-anker.test.ts` (A1–A6), `zitat-aendern-anker.test.ts` (Z1–Z8, u. a. Undo/Redo bitgleich über `jrn_aussage_zitat_au`). |
| **U-1.34-F1-feld-attribut** | Was bedeutet `aussage_zitat.feld`, und wer prüft den Wert? | `feld` ist ein Attribut des Subjekts der Aussage, je `subjekt_typ` eine eigene Wertliste (Zod-Enum); der Handler prüft, dass `feld` zum `subjekt_typ` passt; unbekannte Werte werden beim Lesen toleriert (Nutzer 24.09.2026). | Kein DB-CHECK (E3); ein alter oder künftiger Wert bricht das Lesen nicht. **Bestätigt (Nutzer 24.09.2026)** — Wertliste je `subjekt_typ` (Namen = Spalte bzw. Datumsgruppe der Subjekt-Entität, eine Datumsgruppe ist EIN Feld): `person`: `geschlecht`, `lebend_status` · `ereignis`: `datum`, `ort`, `beschreibung` · `elternschaft`: `typ` · `partnerschaft`: `beginn`, `ende`, `ende_grund` · `ort`: `koordinaten`, `existiert_von`, `existiert_bis` · `name`: `vornamen`, `rufname`, `nachname`, `praefix`, `titel_vor`, `zusatz_nach` · `diagnose`, `risikofaktor`: keine (nur NULL). Begründung: bei `person` sind Geburts-/Sterbedaten und -orte eigene Aussagen (`praedikat` = `geburtsdatum` …) und damit schon feldgenau, `privat`/`notiz`/`gesperrt_bis`/Platzhalter sind Verwaltung; `notiz` ist nirgends belegbar (eigene Notiz der Forschenden); `name` folgt der flachen Namenssicht (`PersonDetailName`, §2.2 — `vatersname` aus `name_part` fehlt dort und darum hier); `diagnose`/`risikofaktor` bleiben leer (M-08, kein Editorweg, nicht vorgreifen). Unbekannte Werte beim Lesen: **durchreichen** als `string` (nicht auf NULL abbilden — das verlöre den Wert und ein folgendes `aendern` mit „unverändert“ würde ihn still löschen); die Anzeige prüft mit `BelegFeldEnum.safeParse`. | Umgesetzt C1b: `src/shared/schemata/aussage-zitat.ts` (`BelegFeldEnum`, `BELEG_FELDER_JE_SUBJEKT`, `belegFeldPasst`), Handler-Prüfung `belegFeldPruefen` in `src/main/befehle/aussage-zitat-anlegen.ts` (`VALIDIERUNG_WERTEBEREICH`); Tests `test/einheit/beleg-feld.test.ts` (F1a–F1f), `befehl-aussage-zitat-feld.test.ts` (FA1–FA5). `feld` ≠ NULL nur an Existenz-Aussagen: U-1.34-C1b-feld-praedikat. |
| **U-1.34-F2-aussage-zitat-aendern** | Wie ändert der Nutzer `feld` und Textanker einer bestehenden Verknüpfung? | Neuer Befehl `aussage_zitat.aendern`, der `feld` und `textanker` gemeinsam ersetzt (Nutzer 24.09.2026) — statt Lösen und neu Anlegen. | Zweiter UPDATE-Weg auf `aussage_zitat` (nach `ankerAufheben`), mit derselben Ankerprüfung wie `aussage_zitat.anlegen`. Nutzlast `{ aussageId, zitatId, feld, textanker }`, `feld` und `textanker` **Pflicht**, `null` = entfernen (ein vergessener Schlüssel löscht nichts still). No-op bei gleicher Eingabe (AP-0.22), fehlende Verknüpfung → `NICHT_GEFUNDEN_AUSSAGE_ZITAT`, ohne Koaleszenz (ein Befehl = ein Undo-Schritt), Journaltext „Beleg geändert“. | Umgesetzt C1b: `src/main/befehle/aussage-zitat-aendern.ts`, `aussage-repo.ts` (`verknuepfungLesen`, `verknuepfungAktualisieren`), Kanal `befehl:aussage_zitat.aendern`; Test `test/einheit/aussage-zitat-aendern.test.ts` (AE1–AE11, u. a. Undo/Redo bitgleich). Nicht im Generator `befehlsfolgeArbitrary` (s. U-1.34-C1b-generator). |
| **U-1.34-F3-person-detail** | Was braucht die Profilseite, um Belege mit Feld und Anker zu zeigen? | `person.detail` liefert je Beleg `zitat_id`, `feld`, `textanker_von/_bis`, dazu `kopf.kennung`; Anzeige über `kennungAnzeige` im Kern (Nutzer 24.09.2026). | Die Rückrichtung (vom Zitat-Ausschnitt zur Aussage) gehört zu AP-1.31b. Form: `PersonDetailBeleg` trägt `zitat_id`, `feld` (`string \| null`, durchgereicht), `textanker` (`{ von, bis } \| null` statt zwei Einzelwerten — ein halber Anker ist so typseitig unmöglich); `PersonDetailKopf.kennung` ist die rohe Zahl oder NULL. | Umgesetzt C1b: `src/shared/schemata/person-detail.ts`, `src/main/abfragen/person-detail.ts`; Test `test/einheit/abfrage-person-detail-beleg.test.ts` (PD1–PD5). Rückrichtung AP-1.31b. |
| **U-1.34-F4-ersatzpaar** | Darf eine Ankergrenze zwischen High- und Low-Surrogate eines UTF-16-Ersatzpaares liegen (z. B. mitten in „👶")? | Nein, ablehnen (Nutzer 24.09.2026). | `aussage_zitat.anlegen` wirft `VALIDIERUNG_WERTEBEREICH` (`ankerPruefen` → `teilt_ersatzpaar`), nichts wird geschrieben. Graphem-Cluster (z. B. Emoji mit Hautton-Modifikator, kombinierende Akzente) prüft die Regel bewusst **nicht** — nur Ersatzpaare. | Umgesetzt C1a (Tests K1, A5b). |
| **U-1.34-R1-korrektur-vor-anker** | Eine Korrektur **vor** dem Anker (z. B. ein Buchstabe eingefügt) verschiebt den Text und entwertet den Anker, obwohl der Ausschnitt selbst unverändert ist. | So gewollt (E4 wörtlich: kein Suchen/Verschieben). Eine Nachverfolgung (Anker mitschieben) wäre eine neue Entscheidung. | `zitat.aendern` meldet die Anzahl entwerteter Anker nicht zurück (Rückgabe weiter `null`); ein Hinweis „n Textanker entfernt" bräuchte eine erweiterte Rückgabe. | Folgepunkt; ggf. AP-1.30. |
| **U-1.34-R2-ui-einheit** | Die UI darf Transkript und Auswahl nicht normalisieren (NFC/NFD, Zeilenenden) oder in einer anderen Einheit (Code Points, Grapheme, Bytes) übergeben — sonst stimmen die Indizes nicht mehr mit dem gespeicherten Transkript überein. | Vertragshinweis: Anker sind UTF-16-Indizes des unveränderten gespeicherten Transkripts (JS-`selectionStart`/`selectionEnd` auf genau diesem Text). | Ein Verstoß fällt serverseitig nur auf, wenn er ein Ersatzpaar teilt oder über die Länge hinausläuft — sonst entsteht ein stiller Fehlanker. | Vertragshinweis für AP-1.30/AP-1.31b. |
| **U-1.34-C1a-generator** | `befehlsfolgeArbitrary` (`test/invarianten/_befehlsfolge-generator.ts`) erzeugt `aussage_zitat.anlegen` ohne Anker und `zitat.aendern` ohne Anker-Wirkung — der neue UPDATE-Weg auf `aussage_zitat` ist in `undo-bitgleich` nicht gedeckt (in C1a nur durch Z8). | Generator um Textanker erweitern (Anker gegen das Transkript gültig, dazu Transkriptänderungen im/hinter dem Ausschnitt). | Geschützter Prüfpfad (ADR-025): eigener PR nach dem Merge von C1a, nicht in derselben Iteration. | **Gelöst (PR-B2):** `_befehlsfolge-beleg.ts` — Transkripte mit Umlaut/Kyrillisch/Emoji, `aussage_zitat.anlegen` mit gegen das Transkript berechnetem Anker, `zitat.aendern` mit abgeleiteten Transkripten (gleich/hinter/im/vor/gekürzt/weggelassen); Invariante `test/invarianten/textanker-gueltig.test.ts` (I1–I3), Deckungszähler in beiden Tests (ADR-009-Nachtrag 24.09.2026, PR-B2). |
| **U-1.34-C1b-generator** | Wie U-1.34-C1a-generator: `befehlsfolgeArbitrary` erzeugt weder `aussage_zitat.anlegen` mit `feld` noch `aussage_zitat.aendern` — der zweite UPDATE-Weg auf `aussage_zitat` ist in `undo-bitgleich` nicht gedeckt (in C1b nur durch AE11). | Generator um `feld` (passend zum Subjekttyp) und `aussage_zitat.aendern` erweitern. | Geschützter Prüfpfad (ADR-025): eigener PR nach dem Merge von C1b, zusammen mit PR-B2. | **Gelöst (PR-B2):** `feld` an Existenz-Aussagen aus `BELEG_FELDER_JE_SUBJEKT`, Aktion `aussageZitatAendern` (setzen/ersetzen/entfernen/No-op ohne Transaktion), Ablehnungs-Aktion für ungültigen Anker/`feld` (E-B2-2); `feld` an `person`/`name` nicht erreichbar (U-1.34-B2-feld-person-name). |
| **U-1.34-C1b-feld-praedikat** | `feld` wird nur gegen den `subjekt_typ` geprüft (Entscheidung F1), nicht gegen das `praedikat`. Fachlich trägt `feld` vor allem an der Existenz-Aussage (ADR-026: „das Zitat belegt das Datum dieses Ereignisses“); an einer Aussage `praedikat='beruf'` über eine Person ist `feld='geschlecht'` zulässig, aber sinnlos. | Entschieden (Nutzer 24.09.2026): `feld` ≠ NULL nur bei `praedikat='existenz'`; `feld` = NULL bleibt an jeder Aussage erlaubt. | Zusätzliche Handlerprüfung in `belegFeldPruefen` (`src/main/befehle/aussage-zitat-anlegen.ts`), gilt für `aussage_zitat.anlegen` und `.aendern` → `VALIDIERUNG_WERTEBEREICH`, nichts geschrieben. Keine Migration; das Lesen bleibt tolerant (ein Roh-Wert an einer Nicht-Existenz-Aussage bricht `person.detail` nicht ab). | Umgesetzt C1b: Tests FA6 (`befehl-aussage-zitat-feld.test.ts`), AE8b (`aussage-zitat-aendern.test.ts`). |
| **U-1.34-B2-mutationen** | Mutationsproben PR-B2 gegen `textanker-gueltig` (T) und `undo-bitgleich` (U), lokal in `src/`, danach zurückgesetzt: M1 Entwertungsschleife in `zitat-aendern.ts` weg — T rot (I1: Anker [0, 1) in leerem Transkript), U rot nur über Zähler; M2 `ankerAufheben` ohne `AND aussage_id` — T rot (I2: Verknüpfung ohne Anker geändert), U nur Zähler; M3a `ankerBleibt` immer true — T rot (I1), U nur Zähler; M3b nur Längenprüfung — T rot (I1: Grenze teilt Ersatzpaar), U nur Zähler; M4 immer false — T rot (I2: Anker hätte bleiben müssen), U nur Zähler; M5 `ankerAufheben` setzt `feld = NULL` — T rot (I2), **U grün**; M6a `verknuepfungAktualisieren` ohne `feld` — T rot (I2: angeforderte Werte), U nur Zähler; M6b von/bis vertauscht — beide rot (DB-CHECK); M7 `ankerAufheben` unter `journalAus` — beide rot (Undo stellt nicht her); M8a `belegAnkerPruefen` wirkungslos, M8b `belegFeldPruefen` wirkungslos — beide rot (Ablehnungs-Aktion); M8c nur die Existenz-Regel entfernt — zuerst **T grün** (die Ablehnung für „feld an Nicht-Existenz-Aussage" wählte ein Feld, das schon an der Typprüfung scheiterte), nach Korrektur des Generators beide rot; M9 No-op-Zweig in `aussage-zitat-aendern.ts` weg — beide rot (Generator: No-op erzeugte Transaktion). | Bestätigt die Befunde vor PR-B2: `undo-bitgleich` fängt keine Entwertungs-Logikfehler (Journal nimmt jedes UPDATE zurück, rot wird es dort nur über die Deckungszähler), dafür I2. **Zweite Runde nach hueter PR #119 (Profile, H3–H5):** alle Proben in T rot, dazu MA (anlegen speichert `feld` nicht, I2 neue Zeile) und MC (`bis`-Seite der Ersatzpaarprüfung entfernt, Ablehnungs-Aktion). In U (Profil `bestand`) rot: M1, M3a (Undo-Entwertung-Mindestzahl), M6b, M7, M8a, M9; grün: MA, MC, M2, M3b, M4, M5, M6a, M8b, M8c — gewollt, die feinen Beleg-Prüfungen liegen in T. | — | Erledigt (PR-B2). |
| **U-1.34-B2-hueter119** | hueter-Review PR #119 (Freigabe mit Auflagen): H1 die Beleg-Gewichte dünnten 13 Bestandsbefehle in `undo-bitgleich` aus; H2 Windows-CI `undo-bitgleich` 138,9 s (main 111,5 s); H3 anlegen-Zweige an der Absicht statt am DB-Ergebnis gezählt, I2 prüfte neue Zeilen nicht; H4 verschwundene Zeilen ungeprüft; H5 Ersatzpaar-Ablehnung nur auf der `von`-Seite; H6 knappe Zähler (`ablehnung.ankerOhneTranskript` u2/t1, `feldNichtExistenz` t2); H7 kein Schutz gegen einen neuen Generator-Befehl ohne Pflichteintrag. | Eigentümer 24.09.2026: zwei Generator-Profile — `bestand` (main-Gewichte, Standard, `undo-bitgleich`) und `beleg` (Beleg-Gewichte, nur `textanker-gueltig`, 250 Läufe); in `undo-bitgleich` je Befehl, Demote und Nachrücken ≥ 50 % des main-Werts (Literal-Tabelle, gemessen auf d0a095b mit Seed 20260910 ×300), neue Befehle und Undo einer Entwertung mit fester Mindestzahl. | H1/H2 umgesetzt (lokal `undo-bitgleich` 27,2 s gegenüber main 25,7–26,5 s); H3 Zeile nach dem Anlegen zurückgelesen, I2 prüft neue Zeilen gegen die Anforderung (Probe MA rot); H4 Verschwinden nur nach Lösch-/Kaskadenbefehl; H5 Ablehnung abwechselnd `{i, i+1}`/`{i−1, i}` (Probe MC rot); H7 beobachtete `befehl:*` ⊆ Schwellentabelle; H6 vermerkt — mit Profil `beleg` jetzt `ankerOhneTranskript` 4 und `feldNichtExistenz` 14 (t), in `undo-bitgleich` keine Beleg-Zähler mehr; ohne Puffer bzw. knapp über der Schwelle (zweites hueter-Review): `undo.entwertung` 1 bei Mindestzahl 1, `hauptname.wechseln` 6 bei Schwelle 5, `aussage_zitat.loeschen` 3 bei Schwelle 2 (u) — mit festem Seed stabil, aber jede Generator-Änderung kann sie kippen. Windows-CI cbc9189: `undo-bitgleich` 129,3 s (main 111,5 s), `textanker-gueltig` 53,9 s. | Erledigt (PR-B2); H6 bleibt knapp, aber mit festem Seed stabil. |
| **U-1.34-B2-H4-verschwinden-breit** | I2 (`textanker-gueltig`) erlaubt das Verschwinden einer Verknüpfung, sobald im Schritt irgendein Lösch-/Kaskadenbefehl lief — auch für nicht betroffene Zeilen. Ein zu breiter Löschbefehl (z. B. `zitatLoesen` ohne `AND aussage_id`) fällt heute nur über die Generator-Konsistenzprüfung (`verknuepfungLesen`) auf, nicht über I2 selbst. | Offen: Verschwinden gegen die tatsächlich betroffene `aussage_id`/`zitat_id` des Befehls prüfen. | Geschützter Prüfpfad, eigener PR. | Folgepunkt (zweites hueter-Review PR #119). |
| **U-1.34-B2-feld-person-name** | Der Generator erreicht `feld` an `person`/`name` nicht: `feld` ist nur an Existenz-Aussagen zulässig (C1b-feld-praedikat), Existenz-Aussagen entstehen nur über `elternschaft`/`partnerschaft`/`ereignis.anlegen`. Die Listen `person` (`geschlecht`, `lebend_status`) und `name` (sechs Bestandteile) sind damit über keinen Schreibbefehl belegbar. | Eigentümer-Entscheidung E-B2-4 (a): im Generator nicht decken, hier benennen. | Offen bleibt die fachliche Frage, ob die beiden Listen tot sind (keine Existenz-Aussage über Person/Name) oder ob es eine Existenz-Aussage dafür geben soll — dann erst Generator nachziehen. | Folgepunkt (PR-B2); vor AP-1.30 entscheiden. |
| **U-1.34-B2-H1-kennung-lesepruefung** | Die Leseprüfung der Kennung über `KennungSchema` in `src/main/abfragen/person-detail.ts` ist ungetestet (per CHECK `kennung >= 1` aus der DB nicht herstellbar). | Offen: Test mit roh eingeschleustem ungültigem Wert (Tabelle ohne CHECK nachbauen oder Schema-Funktion direkt prüfen). | — | Folgepunkt (hueter PR #118, H1). |
| **U-1.34-B2-H2-kennung-formatsymbole** | `src/core/person/kennung.ts` enthält die sichtbaren Zeichen `–` (keine Kennung) und `P-` (Präfix) als Literale — CLAUDE.md §4: sichtbare Texte in `src/renderer/i18n/de/`. | Offen: als sprachneutrale Formatsymbole begründen (analog Zahlformat) oder mit AP-1.30 über i18n führen. | — | Folgepunkt (hueter PR #118, H2); mit AP-1.30. |
| **U-1.34-B2-H3-beleg-pruefung-ort** | `belegFeldPruefen`/`belegAnkerPruefen` liegen in `src/main/befehle/aussage-zitat-anlegen.ts` und werden von `aussage-zitat-aendern.ts` importiert — ein Befehl importiert aus einem anderen. | Offen: in `src/main/befehle/_beleg-pruefung.ts` ziehen (reine Verschiebung, keine Verhaltensänderung). | Nicht im geschützten Prüfpfad; eigener kleiner PR. | Folgepunkt (hueter PR #118, H3). |
| **U-1.34-B2-H5-feld-typ** | Die bestätigte `feld`-Liste (U-1.34-F1) enthält `typ` nur bei `elternschaft`, nicht bei `ereignis` und `partnerschaft`, obwohl beide eine Spalte `typ` haben. | Offen: bewusst (der Typ eines Ereignisses/einer Partnerschaft ist kein eigens zu belegendes Attribut) oder Lücke? | Eine Ergänzung braucht keine Migration (E3), aber eine Nutzerentscheidung. | Folgepunkt (hueter PR #118, H5). |
| **U-1.34-C1b-vollstaendigkeit** | Mutationsprobe beim Einbau von `befehl:aussage_zitat.aendern`: eine fehlende `registriere(...)`-Zeile in `src/main/ipc/registrierung.ts` und ein fehlender Eintrag in `journal.json` ließen `pnpm typen` und alle Tests grün — der Compile-Zeit-Wächter in `kanaele.ts` sichert nur Vertrag ⊆ Weißliste. | Zwei generische Tests: `test/einheit/ipc-registrierung-vollstaendig.test.ts` (registrierte Kanäle == `ALLE_KANAELE`) und `journal-beschreibung-vollstaendig.test.ts` (jeder Beschreibungsschlüssel hat einen Text). | Gilt für jeden künftigen Kanal/Befehl. | Erledigt (C1b). |
| **U-1.34-E5-todesort** | Sterbeort: eigene Aussage oder Ort des Tod-Ereignisses? | Beides: Aussage `todesort` ist führend, Rückfall auf `ereignis(typ='tod').ort_id`; Import-Writer schreibt `todesort` wie `geburtsort`. | Importvertrag v1 unverändert, kein Datenumzug; `person_flach` unberührt. | Umgesetzt C2a: Import-Writer `src/main/import/schreiben.ts` (Tod-Zweig, nur mit Datum und Ort); Auswahl `sterbeortAufloesen` in `src/core/person/sterbeort.ts` (bevorzugte vor kleinster id, Aussage nur mit `wert_text` zählt; Rückfall nur Tod-Ereignisse mit Rolle `verstorbener`); `person.detail` liefert `sterbeort` und `kopf.lebend_status`, `todesort` löst als Ortsverweis auf; Prädikatschlüssel `praedikat_todesort` („Sterbeort“). Tests `test/einheit/sterbeort-aufloesen.test.ts`, `import-schreiben-todesort.test.ts`, `abfrage-person-detail-sterbeort.test.ts`. Folgepunkte: U-1.34-C2a-*. |
| **U-1.34-C2-O1-feldwarnungen** | Woraus entstehen die Feldwarnungen der Profilseite, und wie wird `zyklus` geprüft? | Nutzer 25.09.2026: Feldwarnungen aus dem Umfeld der Person (Person, Eltern, Kinder, Partner); eigene Vorfahren-Prüfung für `zyklus`; ein Konsistenztest gegen `pruefhinweise` sichert, dass beide dasselbe melden. | Keine zweite Regelquelle ohne Gegenprobe. | Umgesetzt C2b: `person.detail` liefert `warnungen: {code, reiter, feld}[]`. Umfeld-Lader `src/main/abfragen/_person-umfeld.ts` (Person und Kinder mit Daten, eigene Elternkanten, Partnerschaften der Eltern, eigene Ereignisse mit Ort und Datum samt allen Beteiligungen), darauf `pruefeBestand`; `zyklus` über `istEigenerVorfahre` (`src/core/graph/zyklus.ts`) auf den Vorfahrenkanten ohne Platzhalter (rekursiver CTE). Zuordnung `feldZielFuer` in `src/core/plausibilitaet/feldwarnungen.ts`, Reiter-IDs `REITER` in `src/core/person/reiter.ts`. Gegenprobe: `test/einheit/person-umfeld.test.ts` und `abfrage-person-detail-warnungen.test.ts` W6 (seed-gesteuerte Fehlerbestände, je Person gleiche Multimenge wie `pruefhinweise`). Folgepunkte: U-1.34-C2b-*. |
| **U-1.34-C2-O2-elternplaetze** | Wie werden Eltern auf die zwei Elternplätze verteilt? | Nutzer 25.09.2026: zwei Elternplätze; Geschlecht M → Vater, F → Mutter, weitere Eltern füllen den freien Platz. Meldung je leerem Platz („Vater fehlt“/„Mutter fehlt“), sonst „Elternteil“. | Keine dritte Platzart. | PR-C2c + PR-D. |
| **U-1.34-C2-O3-widerspruch** | Wann gilt „Widerspruch vorhanden“? | Nutzer 25.09.2026: ungelöster Aussage-Widerspruch (E21) und zusätzlich jede Feldwarnung, je Feld. | — | PR-C2c. |
| **U-1.34-C2-O4-platzhalter** | Bekommen Platzhalterpersonen offene Punkte? | Nutzer 25.09.2026: nein. | Wie E7 (Platzhalter ausgenommen). | PR-C2c. |
| **U-1.34-C2-O5-kind-ohne-partnerschaft** | Wann meldet „Kind ohne Partnerschaft“? | Nutzer 25.09.2026: streng — wenn kein anderer Elternteil des Kindes mit P in einer Partnerschaft steht, auch wenn P der einzige Elternteil ist. | — | PR-C2c. |
| **U-1.34-C2b-feldzuordnung** | Das Design nennt für die Plausibilitätsregeln kein Feld (§5.6 nur „Widerspruch am Feld"). | Gestaltungsfüllung (CLAUDE.md §14), Feld = dort, wo der Nutzer den Fehler behebt: `tod_vor_geburt`, `bestattung_vor_tod`, `alter_ueber_110` → Reiter `person`, Feld `todesdatum`; `mutter_alter`, `vater_alter` → `beziehungen`/`kinder` (Hinweis am Elternteil); `kind_vor_ehe`, `zyklus` → `beziehungen`/`eltern` (Hinweis am Kind bzw. an der Person auf dem Zyklus); `ereignis_vor_ortsexistenz` → `leben`/`ereignisse`. Exhaustiver `switch`: ein neuer Code ist ein Typfehler. | `bestattung_vor_tod` hängt am Todesdatum, obwohl auch das Bestattungsereignis falsch sein kann — das Todesdatum ist das Feld im Reiter „Person", die Bestattung lebt im Reiter „Leben". | Zum Design-Review. |
| **U-1.34-C2b-mehrfach** | Zwei Kinder, beide mit auffälligem Elternalter: eine oder zwei Warnungen? | Zwei — Multimenge wie `pruefhinweise` (Zähler je Reiter zählt Anzahl, Vorgaben §3.1). Reihenfolge: Regelreihenfolge `BESTAND_HINWEIS_CODES`, innerhalb eines Codes Ladereihenfolge (`ORDER BY id`). | Die Warnung trägt keinen Bezug (welches Kind, welches Ereignis) — `pruefeBestand` liefert ihn nicht. | Offen für C2c/PR-D: braucht der Sprung einen Bezug (`bezug_id`), muss `BestandHinweis` erweitert werden (dann auch `pruefhinweise`). |
| **U-1.34-C2b-zyklus-mehr** | `pruefhinweise` meldet über `findeZyklusKnoten` nur den ersten Zyklus des Gesamtbestands; `person.detail` prüft je Person, ob sie eigener Vorfahre ist. | Bewusst: am Feld soll jeder betroffene Datensatz die Warnung zeigen. Konsistenztest nur „mindestens so viel" für `zyklus`, genau eine Warnung je Person. Person unterhalb eines Zyklus bekommt keine. | Die Fußzeilen-Zahl (`pruefhinweise.anzahl`) kann bei mehreren unabhängigen Zyklen kleiner sein als die Summe der Feldwarnungen. Praktisch selten: `elternschaft.anlegen` verhindert Zyklen (`wuerdeZyklusErzeugen`), nur Import/Altbestand. | — |
| **U-1.34-C2b-texte** | Welche Texte zeigt die Feldwarnung? | Keine neuen Texte in C2b: der Renderer leitet den Text aus `code` über `pruefhinweis-schluessel.ts` ab. | Die bestehenden Texte sind nicht parametrisiert (kein Alter, kein Datum, kein Kindname). | Folgepunkt: parametrisierte Texte (z. B. „Mutter war bei der Geburt 8 Jahre alt") zusammen mit AP-1.30; braucht die Werte in der Warnung (s. U-1.34-C2b-mehrfach). |
| **U-1.34-C2b-budget** | Kostet die Prüfung jede Profilantwort zu viel? | Neues Budget `abfrage:person.detail` < 50 ms bei 2000 Personen (`test/budget/leistung.test.ts`, gemessen an den Eltern mit den meisten Kindern). Lokal (macOS) Median ≈ 1,4 ms. | — | — |
| **U-1.34-C2b-blockiert-nie** | Blockiert eine Warnung einen Befehl? | Nein (Vorgaben §1): Warnungen entstehen nur in der Abfrage, kein Befehl ruft `pruefeBestand`. Test W7: `aussage.anlegen` mit Todesdatum vor Geburt und `elternschaft.anlegen` mit zu junger Mutter melden Erfolg, die Daten stehen, die Warnung erscheint. | Die Zyklusfreiheit in `elternschaft.anlegen` bleibt eine harte Sperre (ADR-009 Punkt 2) — eine Invariante, keine Warnung. | — |
| **U-1.34-C2a-bekannte-praedikate** | `BEKANNTE_PRAEDIKATE` (IMP-304) enthält weder `geburtsort` noch `todesort`. | Bewusst unverändert: Vertrag v1 bleibt, beide Prädikate entstehen importintern aus Ereignissen. Ein direkt in `aussagen[]` geliefertes `todesort` meldet weiter IMP-304 (Hinweis, kein Fehler). | — | Offen bis Vertrag v2. |
| **U-1.34-C2a-ereignis-anlegen** | `befehl:ereignis.anlegen` schreibt weder `todesdatum` noch `todesort`. | Risiko benannt, kein Umbau in C2a: der Sterbeort eines in der UI angelegten Tod-Ereignisses erscheint nur über den Rückfall (`sterbeort.herkunft = 'ereignis'`), nicht als Grunddatenfeld. | Todesdatum aus der UI fehlt weiter in `person_flach`. | Offen, eigenes Paket. |
| **U-1.34-C2a-rolle-hauptperson** | Ein Tod-Ereignis, an dem die Person nur als `hauptperson` (nicht `verstorbener`) beteiligt ist. | Zählt nicht: `sterbeort` bleibt leer, wie der Import-Writer, der nur `verstorbener` liest. | — | — |
| **U-1.34-C2a-bericht** | Der Import-Bericht zählt bei einem Tod-Ereignis mit Datum und Ort eine Aussage mehr, auch bei `db:`-Personen (Ergänzung an einer Bestandsperson). | Gewollt; Trockenlauf und echter Import bleiben gleich (`import-schreiben-todesort.test.ts` T4). Keine Fixture des Korpus hat einen Tod mit Ort, die bestehenden Zahlen bleiben. | — | — |
| **U-1.34-C2a-fixture-todesort** | Die Invariante „Trockenlauf-Bericht gleich echtem Import" (`test/invarianten/trockenlauf-gleich-import.test.ts`) läuft den neuen `todesort`-Pfad nie durch: kein Korpus-Fixture hat ein Tod-Ereignis mit Datum und Ort. Gedeckt nur vom Einheitstest T4 (`test/einheit/import-schreiben-todesort.test.ts`). | Offen: gültiges Fixture mit Tod samt Datum und Ort unter `fixtures/import/v1/gueltig/eigenstaendig/` ergänzen; dann deckt die Invariante den Pfad ohne Änderung. U-1.34-C2a-bericht danach anpassen. | Eigener PR (hueter PR #120, H1). | Folgepunkt. |
| **U-1.34-C2a-wert-text-verdraengt** | Eine `todesort`-Aussage nur mit `wert_text` verdrängt einen strukturierten Ereignisort: `sterbeort.ort_id`/`ort_name` sind dann `null`, obwohl das Tod-Ereignis einen Ort hätte (S6, E5 „Aussage führend"). | Bewusst so (E5). PR-D (Vollständigkeit) muss diesen Fall als „Sterbeort vorhanden" werten. | — | Hinweis für PR-D (hueter PR #120, H5). |
| **U-1.34-E6-elternteil** | Welche Elternschaft zählt für „Elternteil nicht zugeordnet"? | Jeder Elternschaftstyp; ein Platzhalter-Elternteil gilt als zugeordnet. | — | PR-C2. |
| **U-1.34-E7-kernangaben** | Nenner der Vollständigkeit. | Name (belegt über Hauptform), Geschlecht (nur vorhanden), Geburtsdatum/-ort, Todesdatum/-ort nur bei `verstorben`, Vater und Mutter getrennt; Platzhalter ausgenommen; abrunden. | ADR-031. | PR-D. Vorarbeit C2a: `person.detail` liefert `kopf.lebend_status` und `sterbeort`. |
| **U-1.34-E8-kein-portraet** | Regel „kein Porträt" jetzt schon? | Bauen, aber `aktiv: false`. | — | PR-C2. |
| **U-1.34-E9-name** | Spaltenname und Sichtbarkeit. | Spalte `kennung`, im Code `personKennung`; nicht in `person_flach`/Suche. | `abl_*`-Trigger unverändert. | — |
| **U-1.34-E10-rueckgabe** | Gibt `person.anlegen` die Kennung zurück? | Nein, weiter `{ id }`. | Kennung über `person.detail` (`kopf.kennung`, umgesetzt C1b). | — |
| **U-1.34-E11-weitere-regeln** | Weitere Regeln aus Vorgaben §5.6. | Später. | — | Eigenes Paket. |
| **U-1.34-E12-nie-neu-vergeben** | Die Rücknahme eines Großimports per Schnappschuss setzte den Zähler auf den Stand vor dem Import zurück. | „Nie neu vergeben" gilt ausnahmslos: `importZuruecknehmen` sichert den Zählerstand und zieht ihn danach nur vor, wenn der wiederhergestellte kleiner ist (der „nur vorwärts"-Trigger feuert nie). | Umgesetzt in PR-A (`src/main/journal/undo.ts`, Test in `import-schreiben-kennungen.test.ts`); seit A2c auch für Import-Schnappschüsse <v7 (Migration mit Kennungsübernahme, H6). | Erledigt: Schnappschuss-Wiederherstellen A2a/A2b, Import-Rücknahme <v7 A2c. |
| **U-1.34-E13-null-kennung** | Undo eines Journaleintrags von vor 0007 setzt eine Person ohne Kennung wieder ein. | NULL zulassen; UI zeigt „–". | `person.kennung` bleibt nullable, UNIQUE erlaubt mehrere NULL. Gilt auch nach A2b weiter (Undo alter Journaleinträge); die Kennungsübernahme beim Wiederherstellen eines Schnappschusses <v7 vergibt dagegen jeder Person des Schnappschusses eine Kennung. | UI mit AP-1.30. |
| **U-1.34-E14-ausnahmeliste** | Der Undo-Bitgleich-Abzug nimmt Tabellen pauschal aus. | Ausnahmeliste in PR-B fest pinnen (siehe B1); einzige fachliche Ausnahme `kennung_zaehler`. | ADR-009-Nachtrag 24.09.2026; `architektur.md` §4.9. | Erledigt (PR-B). |
| **U-1.34-B1-pauschale-ausnahme** | `test/invarianten/_kanonischer-abzug.ts` nimmt alle `NICHT_JOURNALISIERT`-Tabellen pauschal aus — `kennung_zaehler` fällt damit ohne Gegenprobe heraus; PR-A ist darum ohne PR-B grün. | PR-B pinnt die Liste explizit und prüft gegen (`undo-bitgleich-ausnahmen`, `kennung-nie-neu-vergeben`). | Reihenfolge PR-A → PR-B zwingend; PR-A ändert `test/invarianten` nicht. Mit PR-B zusätzlich korrigiert: `schema_migration`, `merge_protokoll`, `id_alias` fielen ebenfalls still heraus und werden jetzt verglichen. | Erledigt (PR-B). |
| **U-1.34-B2-nullable** | NOT NULL für `person.kennung`? | Nein: kein NOT NULL per ALTER, kein Tabellenneubau, kein NOT-NULL-Trigger (brächen das Undo alter Journaleinträge über `rohEinfuegen`). | Garantie über den Schreibweg `personRepo.einfuegen` → `kennungZiehen` + Tests. Seit A2b ein zweiter Schreibweg: die Kennungsübernahme im Migrations-Hook (`personKennungenUebernehmen`, nur beim Wiederherstellen eines Schnappschusses <v7, O-1); sie vergibt jeder Person eine Kennung und zieht den Zähler nur vor. | — |
| **U-1.34-A2-schnappschuss-wiederherstellen** | Gilt „nie neu vergeben" (E12) auch, wenn der Nutzer einen Schnappschuss wiederherstellt (`src/main/schnappschuss/wiederherstellen.ts`)? | Ja (Nutzer 24.09.2026): wie E12 — der Zählerstand wird gesichert und nach der Wiederherstellung nur vorgezogen, nie zurückgesetzt. | Eine Kennung aus der Zeit nach dem Schnappschuss wird nicht erneut vergeben. | A2a umgesetzt (≥v7; `src/main/schnappschuss/wiederherstellen.ts` + `kennung-angleichen.ts`, Tests A2-T1..T3 in `test/einheit/wiederherstellen.test.ts`). A2b umgesetzt (<v7: Migration vor dem Öffnen mit Kennungsübernahme, `wiederhergestellteDateiAngleichen`, Tests H6b-T1..T4/F). A2c umgesetzt (Import-Rücknahme <v7, H6). |
| **U-1.34-O1-uebernahme-hook** | Wo werden bei einem Schnappschuss <v7 die Kennungen übernommen (A2b)? | Per Aufrufer-Hook im Migrationslauf (`src/main/datenbank/migration/laeufer.ts`), innerhalb der Migrations-Klammer, kein neuer `journalAus` (Nutzer 24.09.2026). | Die Übernahme läuft atomar mit Migration 0007. | Umgesetzt A2b: `LaeufenOptionen.nachMigrationsSql` in `src/main/datenbank/migration/laeufer.ts` (Test `test/einheit/laeufer-hook.test.ts`); `architektur.md` §9.3. |
| **U-1.34-O2-ohne-treffer** | Welche Kennung bekommen Personen des Schnappschusses (<v7) ohne Eintrag in der Zuordnung `id → kennung` aus der ersetzten Datei? | Fortlaufend ab dem gesicherten Zählerstand, Reihenfolge `ORDER BY id` (Nutzer 24.09.2026). | Deterministisch wie E2-Nachtrag. Zähler danach `max(vorher + k, größter Treffer + 1)`, nie gesenkt. | Umgesetzt A2b: `personKennungenUebernehmen` in `src/main/repositories/kennung-repo.ts` (Test `test/einheit/kennung-uebernehmen.test.ts`). |
| **U-1.34-O3-schnappschuss-ab-v7** | Werden bei einem Schnappschuss ≥v7 Kennungen aus der ersetzten Datei übernommen? | Nein: der Schnappschuss behält seine Kennungen, nur der Zähler wird nachgezogen (Nutzer 24.09.2026). Schränkt H6b auf Schnappschüsse <v7 ein. | Kein Umnummern in A2a. | Umgesetzt mit A2a. |
| **U-1.34-O4-sync-ordner** | Das Wiederherstellen ruft `projektOeffnen` auf und ignoriert dessen Rückgabe `sync_warnung`. | Eigener Folgepunkt, nicht Teil von A2 (Nutzer 24.09.2026). | Liegt das Projekt in einem Sync-Ordner, ist es nach dem Wiederherstellen heute still geschlossen. | Folgepunkt. |
| **U-1.34-H6b-kennung-ersetzte-datei** | Welche Kennungen tragen Personen nach der Wiederherstellung, wenn die ersetzte Datei bereits Kennungen vergeben hatte? | Kennungen übernehmen (Nutzer 24.09.2026): Zuordnung `id → kennung` aus der ersetzten Datei; übrige Personen nach dem Zähler neu nummerieren. Gilt nur für Schnappschüsse <v7 (O-3). | Dieselbe Person behält ihre Nummer über die Wiederherstellung hinweg. | Umgesetzt A2b (`src/main/schnappschuss/wiederherstellen.ts` + `kennung-angleichen.ts`, Tests H6b-T1..T4/F in `test/einheit/wiederherstellen.test.ts`). Ohne zusätzlichen Schnappschuss vor der Migration: der Vor-Stand ist die unveränderte Quelldatei (`architektur.md` §6.2/§9.2). |
| **U-1.34-H6-import-zuruecknehmen** | Dieselbe Frage bei `importZuruecknehmen` (Schnappschuss-Pfad des Großimports). Befund: bis A2c öffnete/migrierte die Rücknahme eine v6-Datei gar nicht; erst das nächste `projektOeffnen` nummerierte 1..m und verletzte damit E12. | Gleich behandeln wie H6b (Nutzer 24.09.2026). | `importZuruecknehmen` sichert Zähler und `id → kennung` und ruft `wiederhergestellteDateiAngleichen` (Migration sofort, Hook bei 0007); `undo()` bekommt `UndoOptionen` (`schemaBasis`, `appVersion`), die Aufrufer in `src/main/ipc/registrierung.ts` und `src/main/menue/menue.ts` übergeben `schemaBasisverzeichnis()`/`app.getVersion()` ausdrücklich. `zaehlerNachziehen` entfällt. | Erledigt (A2c; Tests H6-T1/H6-F/H6-F2 in `test/einheit/import-schreiben-kennungen.test.ts`, E12 als H6-T2). |
| **U-1.34-A2c-undo-verdrahtung** | Die Produktiv-Aufrufer von `undo()` müssen `schemaBasis`/`appVersion` übergeben; ohne sie fällt `undo()` auf `cwd` zurück — unter Vitest grün, im gepackten Build scheitert jede Großimport-Rücknahme (auch ≥v7: die Prüfsummenprüfung liest die Migrations-SQL vor dem No-op). Die Mutation „Optionen entfernen“ überlebte alle Tests. | Statischer AST-Scan `test/einheit/undo-aufrufer-verdrahtet.test.ts`: jeder Aufruf des aus `journal/undo` importierten `undo` (auch Alias/Namensraum) übergibt ein Objektliteral mit beiden Eigenschaften; erwartet genau `src/main/ipc/registrierung.ts` und `src/main/menue/menue.ts` (nicht leer grün). | `wiederhergestellteDateiAngleichen` braucht keinen Scan: `AngleichenOptionen` hat Pflichtfelder, ein fehlendes Argument ist ein Typfehler. | Erledigt (hueter PR #115, H1). |
| **U-1.34-A2c-fremdfehler** | Der Umpack-Zweig in `importZuruecknehmen` (Nicht-`WurzelFehler` → `INTERN_UNERWARTET` + Rückrollen) war ungetestet (Mutation `throw u` überlebte). | Test H6-F2 in `test/einheit/import-schreiben-kennungen.test.ts`: Kennungskollision im Hook (wie H6b-F) → `INTERN_UNERWARTET`, alter Stand, keine `ersetzt-`-Datei, Zähler unverändert. | — | Erledigt (hueter PR #115, H3). |
| **U-1.34-A2a-fehlercode** | Kaputte Schnappschuss-Datei beim Wiederherstellen: bisher `DATENBANK_INTEGRITAET` beim Öffnen (Projekt danach kaputt), seit A2a Rückrollen mit `INTERN_UNERWARTET` (wie `importZuruecknehmen`). | Teilweise erledigt: `schnappschussWiederherstellen` reicht seit A2b (hueter PR #114, H3) typisierte Fehler nach dem Rückrollen durch — `DATENBANK_INTEGRITAET` (`quick_check`), `PROJEKT_NEUERE_SCHEMAVERSION`, `PROJEKT_MIGRATION_GEAENDERT`; belegt durch die Tests H2/H3 in `test/einheit/wiederherstellen.test.ts`. Seit A2c reicht auch `importZuruecknehmen` (`src/main/journal/undo.ts`) typisierte Fehler durch (Test H6-F in `test/einheit/import-schreiben-kennungen.test.ts`). Offen nur noch: eine Datei, die gar nicht als SQLite öffnet (`SQLITE_NOTADB` schon in `oeffnen`), endet in beiden Pfaden weiter in `INTERN_UNERWARTET` — passenderen Code (`DATENBANK_INTEGRITAET` o. ä.) wählen. | Nutzersichtbare Meldung. Vor A2b endete ein Fehler des Schnappschusses beim Öffnen mit dem Projekt festgesteckt auf der Schnappschuss-Datei; seit A2b immer Rückrollen. | Folgepunkt (hueter PR #113, H4; Stand hueter PR #114, H3, A2c). |
| **U-1.34-A2a-handle-test** | Dass `zaehlerNachziehen` (seit A2c entfallen) den Handle nach erfolgreichem Öffnen auch bei einem Wurf schließt (Windows-rename), deckt kein Test (Mutation „close entfernt" überlebt). | Offen: Test mit beschreibgeschütztem/`RAISE`-Zähler im Schnappschuss, danach Datei ohne offenen Handle umbenennbar. | Gilt seit A2b ebenso für `wiederhergestellteDateiAngleichen` (seit A2c auch im Pfad `importZuruecknehmen`; H6b-F prüft Rückrollen nach einem Wurf im Hook, auf macOS aber nicht den geschlossenen Handle). Ergänzung (hueter PR #115, H5): wirft im Fehlerzweig das Rückroll-`renameSync` selbst (z. B. Windows-Sperre), geht der Ursprungsfehler verloren und unter `dbPfad` bleibt die kopierte, ggf. teilweise migrierte Datei liegen — in `importZuruecknehmen` und `schnappschussWiederherstellen` gleich; Ursprungsfehler erhalten (z. B. als `cause`) und mit eigenem Test belegen. | Folgepunkt (hueter PR #113, H1; PR #115, H5). |
| **U-1.34-A2b-schnappschuss-kollision** | `projektOeffnen` erzeugt vor einer Migration über `schnappschussVor` einen Schnappschuss mit sekundengenauem `Date.now`-Namen. Existiert schon einer aus derselben Sekunde (z. B. vom Anlegen des Projekts), scheitert `VACUUM INTO` mit „output file already exists"; das Projekt bleibt geschlossen auf dem alten Schemastand. | Offen: eindeutiger Dateiname (Zusatz bei Kollision) oder Kollision als „Schnappschuss schon vorhanden" behandeln — mit eigenem rotem Test. | Beim Wiederherstellen seit A2b nicht mehr erreichbar (Hook-Pfad ohne `schnappschussVor`); im allgemeinen Öffnen-Pfad praktisch selten. | Folgepunkt (Fund umsetzer A2b). |
| **U-1.34-A2b-ohne-personenzaehler** | `wiederhergestellteDateiAngleichen` überspringt die Kennungsübernahme im Hook still, wenn der gesicherte Stand der ersetzten Datei keinen `person`-Zähler enthält — obwohl eine Datei auf v7 ihn stets hat. | Offen: fail-closed werfen (`INTERN_UNERWARTET` o. ä.) statt still ohne Übernahme zu migrieren — mit eigenem rotem Test. | Praktisch unerreichbar (das offene Projekt ist stets auf `SCHEMA_VERSION`, 0007 legt die Zählerzeile an); ein stiller Rückfall nummerierte die Personen aber ohne Rücksicht auf vergebene Kennungen. | Folgepunkt (hueter PR #114, H4). |
| **U-1.34-A2b-uebernahme-schreibmenge** | Bei einem Schnappschuss <v7 wird `person` dreimal angefasst: 0007 füllt `kennung` selbst, der Hook liest alle IDs, setzt alle Kennungen auf NULL und schreibt danach jede Zeile neu. | Offen: nur die Zeilen auf NULL setzen/neu schreiben, deren Kennung von der Zuordnung abweicht — Verhalten unverändert, belegt durch die bestehenden H6b-Tests. | Reine Leistungsfrage (einmalig beim Wiederherstellen eines alten Schnappschusses); relevant erst bei großen Beständen. | Folgepunkt (hueter PR #114, H5). |
| **U-1.34-B3-hilfsmittel-abzug** | `test/hilfsmittel/kanonischer-abzug.ts` (nicht geschützt; genutzt von `import-undo-klein/gross`, `trockenlauf-ohne-wirkung`, `generator-deterministisch`) nimmt weiter `...NICHT_JOURNALISIERT` pauschal aus — dieselbe Lücke wie B1. | Offen: auf die gepinnte Liste aus PR-B umstellen oder begründet anders pinnen. | Betrifft Import-Undo- und Trockenlauf-Invarianten. | Folgepunkt nach PR-B (hueter PR #112, H2). |
| **U-1.34-B4-abgeleitet-nach-undo** | Abgeleitete Tabellen sind im Undo-Bitgleich-Vergleich ausgenommen; gegen Neuaufbau werden sie nur ohne Undo geprüft (`abgeleitet-gleich`), nach Undo nur `person_flach` beispielhaft. | Offen: in `undo-bitgleich.test.ts` nach vollständigem Undo alle abgeleiteten Tabellen gegen `alleAbgeleitetenNeuAufbauen` vergleichen. | Bestand vor PR-B; Risiko gering (Undo schreibt mit aktiven `abl_*`-Triggern). | Folgepunkt, geschützter Pfad, eigener PR (hueter PR #112, H1). |
| **U-1.34-B5-merge-tabellen** | `merge_protokoll`/`id_alias` sind NICHT_JOURNALISIERT, werden seit PR-B aber bitgleich verglichen. Heute schreibt kein Befehl hinein. | Sobald ein Merge-Befehl in `befehlsfolgeArbitrary` kommt, wird `undo-bitgleich` rot (gewollt, fail-closed). Dann **entscheiden**: journalisieren oder ADR-009-Nachtrag — **nicht** einfach auf die Ausnahmeliste setzen. | — | Offen bis zum Merge-Befehl (hueter PR #112, H5). |

---
