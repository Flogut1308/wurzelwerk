# Design-Grundlagen „Person bearbeiten & Medien“

Eingefrorene Kopie aus dem Design-Export vom 22.09.2026, gegen die AP-1.29 bis AP-1.34 gebaut
werden (`CLAUDE.md` §14: gebaut wird gegen die versionierte Kopie, nicht gegen den Live-Link).

| Datei | Inhalt |
|---|---|
| `Wurzelwerk Person bearbeiten.dc.html` | Artboards 1a–1d, 2a–2c, 3a–3c |
| `Entwicklungsvorgaben Person bearbeiten & Medien.md` | Datenmodell, Verhalten, Akzeptanzkriterien |
| `support.js` | Laufzeit des Design-Werkzeugs, nur damit sich die HTML-Datei öffnen lässt (generiert, nicht bearbeiten) |

Bei Widerspruch zwischen beiden gilt das Vorgabendokument. Vorrang vor beiden haben die
Nutzerentscheidungen und §14-Abweichungen in `../arbeitspakete.md` bzw. `Wissen/57`
(Abschnitt „Person bearbeiten und Medien — neuer Entwurf“).

Die HTML-Datei lädt React und Babel beim Öffnen von unpkg.com — sie ist Anschauungsmaterial,
kein Teil der App.
